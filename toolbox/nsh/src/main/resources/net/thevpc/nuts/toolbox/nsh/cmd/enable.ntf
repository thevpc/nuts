Enable and disable builtin shell commands

##) SYNOPSIS:
```sh enable <command-name> ...```
Enable and disable builtin shell commands
    ```sh -n ```: disable commands
    ```sh -a ```: display all builtins

##) EXAMPLES:

```sh enable ls```
    enable ls builtin

```sh enable -n ls```
    disable ls builtin
