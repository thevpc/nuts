check out a ```sh nuts``` to a local folder to be modified

##) SYNOPSIS:
```sh checkout <nuts-id> folder-path ```sh

checks out a ```sh nuts``` to a local folder to be modified. The version of the checked out ```sh nuts``` is suffixed with "-CHECKED-OUT"
Only "zip" ```sh nuts``` could be checked out as they are unzipped to the folder.

##) EXAMPLES:

```sh checkout net.vpc:myapp /to/my/folder```
   creates folder /to/my/folder and checks out net.vpc:myapp sources into the folder


