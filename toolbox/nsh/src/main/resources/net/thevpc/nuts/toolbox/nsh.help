nsh (or ##nuts## bash) is a portable shell, bash like (mostly compatible), implemented in java.

##) SYNOPSIS
```sh
nsh [<options>]...
```
    run interactive shell
```sh
nsh [<options>]... -c <command>
```
    run command
```sh
nsh [<options>]... <file>
```
    run file

##) OPTIONS
```sh
nsh -c <command>
```
    run command instead of running file

