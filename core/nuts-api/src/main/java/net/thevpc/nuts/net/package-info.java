/**
 * Networking and HTTP request/response abstractions.
 *
 * <p>It provides parsed connection strings and builders, an HTTP client contract,
 * HTTP methods, status codes, cookies, request bodies, responses, URL encoding, and
 * response-specific errors.</p>
 */
package net.thevpc.nuts.net;
