/**
 * Expression syntax trees, evaluation contexts, operators, and templates.
 *
 * <p>The package models literal, word, variable, call, conditional, operator, and
 * interpolated-string nodes. Resolvers and contexts supply variables, functions, and
 * operators; evaluators compile or execute expressions and templates. Parsing and
 * evaluation failures are reported by the package-specific exceptions.</p>
 */
package net.thevpc.nuts.expr;
