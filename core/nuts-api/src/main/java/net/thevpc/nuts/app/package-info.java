/**
 * Application lifecycle contracts for applications hosted by a Nuts session.
 *
 * <p>The package models application metadata, modes and main arguments, and
 * defines the handler callbacks for initialization, installation, execution,
 * update, uninstallation, and completion. {@code NApplicationBuilder} is the
 * entry point for assembling an application definition.</p>
 */
package net.thevpc.nuts.app;
