/**
 * Logging contracts, records, configuration, scopes, and message intent.
 *
 * <p>{@code NLog} and {@code NLogger} provide the client-facing logging façade;
 * {@code NLogRecord}, {@code NLogConfig}, and {@code NLogScope} carry logging data
 * and context. Logging-provider integration is exposed through the supported SPI in
 * {@code net.thevpc.nuts.spi}, not through this package.</p>
 */
package net.thevpc.nuts.log;
