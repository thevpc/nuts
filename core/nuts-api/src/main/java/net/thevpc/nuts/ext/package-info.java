/**
 * Extension discovery, registration, and lifecycle contracts.
 *
 * <p>{@code NExtensions} and {@code NExtensionCatalog} expose extensions registered
 * in a Nuts runtime, while {@code NServiceLoader} abstracts service discovery.
 * Extension metadata, lifecycle callbacks, and related exceptions support integration
 * without coupling callers to a particular runtime implementation.</p>
 */
package net.thevpc.nuts.ext;
