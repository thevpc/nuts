---
id: pkg-net-thevpc-nuts-artifact
title: net.thevpc.nuts.artifact
order: 30
---

# Package net.thevpc.nuts.artifact

<a id="pkg-net-thevpc-nuts-artifact"></a>

## Interfaces Summary

| Interface | Description |
| :--- | :--- |
| [NArtifactCall](#cls-net-thevpc-nuts-artifact-NArtifactCall) | artifact call descriptor used to define executor and installer call definitions. |
| [NArtifactCallBuilder](#cls-net-thevpc-nuts-artifact-NArtifactCallBuilder) | NutsArtifactCallBuilder is responsible for building instances of NutsArtifactCall to be used as NutsDescriptor executor or installer. |
| [NClasspathBuilder](#cls-net-thevpc-nuts-artifact-NClasspathBuilder) |  |
| [NClasspathEntry](#cls-net-thevpc-nuts-artifact-NClasspathEntry) |  |
| [NDefinition](#cls-net-thevpc-nuts-artifact-NDefinition) | Definition is an **immutable** object that contains all information about a artifact identified by it's Id. |
| [NDefinitionBuilder](#cls-net-thevpc-nuts-artifact-NDefinitionBuilder) | NDefinitionBuilder interface. |
| [NDefinitionFilter](#cls-net-thevpc-nuts-artifact-NDefinitionFilter) | Descriptor filter |
| [NDependencies](#cls-net-thevpc-nuts-artifact-NDependencies) | Package dependencies representation |
| [NDependency](#cls-net-thevpc-nuts-artifact-NDependency) | NutsDependency is an **immutable** object that contains all information about a package's dependency. |
| [NDependencyBuilder](#cls-net-thevpc-nuts-artifact-NDependencyBuilder) | Dependency Builder (mutable). |
| [NDependencyFilter](#cls-net-thevpc-nuts-artifact-NDependencyFilter) | Dependency filter |
| [NDependencySolver](#cls-net-thevpc-nuts-artifact-NDependencySolver) | Nuts descriptors solver responsible to building traversable dependency tree |
| [NDependencyTreeNode](#cls-net-thevpc-nuts-artifact-NDependencyTreeNode) | Dependency tree node |
| [NDescriptor](#cls-net-thevpc-nuts-artifact-NDescriptor) | Nuts descriptors define an **immutable** image to all information needed to execute an artifact. |
| [NDescriptorBuilder](#cls-net-thevpc-nuts-artifact-NDescriptorBuilder) | Nuts descriptors define a **mutable** image to all information needed to execute an artifact. |
| [NDescriptorContributor](#cls-net-thevpc-nuts-artifact-NDescriptorContributor) | NDescriptorContributor interface. |
| [NDescriptorContributorBuilder](#cls-net-thevpc-nuts-artifact-NDescriptorContributorBuilder) | NDescriptorContributorBuilder interface. |
| [NDescriptorLicense](#cls-net-thevpc-nuts-artifact-NDescriptorLicense) | NDescriptorLicense interface. |
| [NDescriptorLicenseBuilder](#cls-net-thevpc-nuts-artifact-NDescriptorLicenseBuilder) | NDescriptorLicenseBuilder interface. |
| [NDescriptorMailingList](#cls-net-thevpc-nuts-artifact-NDescriptorMailingList) | NDescriptorMailingList interface. |
| [NDescriptorMailingListBuilder](#cls-net-thevpc-nuts-artifact-NDescriptorMailingListBuilder) | NDescriptorMailingListBuilder interface. |
| [NDescriptorOrganization](#cls-net-thevpc-nuts-artifact-NDescriptorOrganization) | NDescriptorOrganization interface. |
| [NDescriptorOrganizationBuilder](#cls-net-thevpc-nuts-artifact-NDescriptorOrganizationBuilder) | NDescriptorOrganizationBuilder interface. |
| [NDescriptorParser](#cls-net-thevpc-nuts-artifact-NDescriptorParser) | Descriptor Parser |
| [NDescriptorProperty](#cls-net-thevpc-nuts-artifact-NDescriptorProperty) | Descriptor Property |
| [NDescriptorPropertyBuilder](#cls-net-thevpc-nuts-artifact-NDescriptorPropertyBuilder) | NDescriptorPropertyBuilder interface. |
| [NEnvCondition](#cls-net-thevpc-nuts-artifact-NEnvCondition) | Nuts environment condition, used to check against the current environment |
| [NEnvConditionBuilder](#cls-net-thevpc-nuts-artifact-NEnvConditionBuilder) | NEnvConditionBuilder interface. |
| [NFilters](#cls-net-thevpc-nuts-artifact-NFilters) | Filters helper |
| [NId](#cls-net-thevpc-nuts-artifact-NId) | Immutable Artifact id information. |
| [NIdBuilder](#cls-net-thevpc-nuts-artifact-NIdBuilder) | Mutable Artifact id information used to create instance of NId |
| [NIdFilter](#cls-net-thevpc-nuts-artifact-NIdFilter) | Class for filtering Artifact Ids |
| [NIdWriter](#cls-net-thevpc-nuts-artifact-NIdWriter) | Class responsible of manipulating NId instances: - formatting (in Nuts Stream Format) - parsing |
| [NTypedFilters](#cls-net-thevpc-nuts-artifact-NTypedFilters) | Filters helper |
| [NVersion](#cls-net-thevpc-nuts-artifact-NVersion) | this class represents an **immutable** string representation of a version parsed as a suite of alternating numbers and words. |
| [NVersionComparator](#cls-net-thevpc-nuts-artifact-NVersionComparator) | NVersionComparator interface. |
| [NVersionFilter](#cls-net-thevpc-nuts-artifact-NVersionFilter) | version interval is a version filter that accepts interval ranges of versions. |
| [NVersionInterval](#cls-net-thevpc-nuts-artifact-NVersionInterval) |  |
| [NVersionPart](#cls-net-thevpc-nuts-artifact-NVersionPart) | NVersionPart interface. |

## Classes Summary

| Class | Description |
| :--- | :--- |
| [NDescriptorEffectiveConfig](#cls-net-thevpc-nuts-artifact-NDescriptorEffectiveConfig) | NDescriptorEffectiveConfig class. |
| [NIdLocation](#cls-net-thevpc-nuts-artifact-NIdLocation) | This class is used in NDescriptor to describe locations/mirrors to download artifact content instead of the regular location. |

## Enums Summary

| Enum | Description |
| :--- | :--- |
| [NClasspathEntryType](#cls-net-thevpc-nuts-artifact-NClasspathEntryType) | Supported dependency scope lists |
| [NDependencyScope](#cls-net-thevpc-nuts-artifact-NDependencyScope) | Supported dependency scope lists |
| [NDependencyScopePattern](#cls-net-thevpc-nuts-artifact-NDependencyScopePattern) | Supported dependency scope pattern. |
| [NDescriptorFlag](#cls-net-thevpc-nuts-artifact-NDescriptorFlag) | Formats supported by Nuts |
| [NDescriptorStyle](#cls-net-thevpc-nuts-artifact-NDescriptorStyle) | Descriptor Style |
| [NIdType](#cls-net-thevpc-nuts-artifact-NIdType) | Artifacts are organized according to NutsIdType to reflect how the artifact should be managed by the workspace. |
| [NVersionPartType](#cls-net-thevpc-nuts-artifact-NVersionPartType) | NVersionPartType enum. |

## Exceptions Summary

| Exception | Description |
| :--- | :--- |
| [NArtifactNotFoundException](#cls-net-thevpc-nuts-artifact-NArtifactNotFoundException) | Exception thrown when the package could not be resolved |


---

<a id="cls-net-thevpc-nuts-artifact-NArtifactCall"></a>

## NArtifactCall

```java
public interface NArtifactCall extends Serializable, NBlankable
```

artifact call descriptor used to define executor and installer call definitions.

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NId` | [id()](#m-NArtifactCall-id) | artifact id |
| `List&lt;String&gt;` | [arguments()](#m-NArtifactCall-arguments) | execution arguments |
| `String` | [scriptName()](#m-NArtifactCall-scriptName) | Script name. |
| `String` | [scriptContent()](#m-NArtifactCall-scriptContent) | Script content. |

### Method Details


---

<a id="m-NArtifactCall-id"></a>

#### id

```java
NId id()
```

artifact id

**Returns:**

artifact id



---

<a id="m-NArtifactCall-arguments"></a>

#### arguments

```java
List<String> arguments()
```

execution arguments

**Returns:**

execution arguments



---

<a id="m-NArtifactCall-scriptName"></a>

#### scriptName

```java
String scriptName()
```

Script name.

**Returns:**

script name result



---

<a id="m-NArtifactCall-scriptContent"></a>

#### scriptContent

```java
String scriptContent()
```

Script content.

**Returns:**

script content result



---

<a id="cls-net-thevpc-nuts-artifact-NArtifactCallBuilder"></a>

## NArtifactCallBuilder

```java
public interface NArtifactCallBuilder extends Serializable, NComponent
```

NutsArtifactCallBuilder is responsible for building instances of `NutsArtifactCall` to be used
as NutsDescriptor executor or installer.
To get an instance of NutsArtifactCallBuilder you can use `workspace.descriptor().callBuilder()`

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NArtifactCallBuilder` | [of()](#m-NArtifactCallBuilder-of) | Creates a new instance of NArtifactCallBuilder. |
| `String` | [scriptName()](#m-NArtifactCallBuilder-scriptName) | Script name. |
| `NArtifactCallBuilder` | [scriptName(String)](#m-NArtifactCallBuilder-scriptName) | Script name. |
| `String` | [scriptContent()](#m-NArtifactCallBuilder-scriptContent) | Script content. |
| `NArtifactCallBuilder` | [scriptContent(String)](#m-NArtifactCallBuilder-scriptContent) | Script content. |
| `NId` | [id()](#m-NArtifactCallBuilder-id) | return artifact id |
| `NArtifactCallBuilder` | [id(NId)](#m-NArtifactCallBuilder-id) | update artifact id |
| `List&lt;String&gt;` | [arguments()](#m-NArtifactCallBuilder-arguments) | return call arguments |
| `NArtifactCallBuilder` | [arguments(String)](#m-NArtifactCallBuilder-arguments) | update arguments |
| `NArtifactCallBuilder` | [arguments(List&lt;String&gt;)](#m-NArtifactCallBuilder-arguments) | Updates the call arguments with the provided list of values. |
| `NArtifactCallBuilder` | [copyFrom(NArtifactCallBuilder)](#m-NArtifactCallBuilder-copyFrom) | initialize this instance from the given value |
| `NArtifactCallBuilder` | [copyFrom(NArtifactCall)](#m-NArtifactCallBuilder-copyFrom) | initialize this instance from the given value |
| `NArtifactCallBuilder` | [clear()](#m-NArtifactCallBuilder-clear) | reset this instance to default (null) values |
| `NArtifactCall` | [build()](#m-NArtifactCallBuilder-build) | create an immutable instance of NArtifactCall initialized with all of this attributes. |

### Method Details


---

<a id="m-NArtifactCallBuilder-of"></a>

#### of

```java
static NArtifactCallBuilder of()
```

Creates a new instance of `NArtifactCallBuilder`.

**Returns:**

a new instance of `NArtifactCallBuilder`.



---

<a id="m-NArtifactCallBuilder-scriptName"></a>

#### scriptName

```java
String scriptName()
```

Script name.

**Returns:**

script name result



---

<a id="m-NArtifactCallBuilder-scriptName"></a>

#### scriptName

```java
NArtifactCallBuilder scriptName(String scriptName)
```

Script name.

**Parameters:**

- `scriptName` - script name

**Returns:**

script name result



---

<a id="m-NArtifactCallBuilder-scriptContent"></a>

#### scriptContent

```java
String scriptContent()
```

Script content.

**Returns:**

script content result



---

<a id="m-NArtifactCallBuilder-scriptContent"></a>

#### scriptContent

```java
NArtifactCallBuilder scriptContent(String scriptContent)
```

Script content.

**Parameters:**

- `scriptContent` - script content

**Returns:**

script content result



---

<a id="m-NArtifactCallBuilder-id"></a>

#### id

```java
NId id()
```

return artifact id

**Returns:**

artifact id



---

<a id="m-NArtifactCallBuilder-id"></a>

#### id

```java
NArtifactCallBuilder id(NId value)
```

update artifact id

**Parameters:**

- `value` - artifact id

**Returns:**

`this` instance



---

<a id="m-NArtifactCallBuilder-arguments"></a>

#### arguments

```java
List<String> arguments()
```

return call arguments

**Returns:**

call arguments



---

<a id="m-NArtifactCallBuilder-arguments"></a>

#### arguments

```java
NArtifactCallBuilder arguments(String value)
```

update arguments

**Parameters:**

- `value` - arguments

**Returns:**

`this` instance



---

<a id="m-NArtifactCallBuilder-arguments"></a>

#### arguments

```java
NArtifactCallBuilder arguments(List<String> value)
```

Updates the call arguments with the provided list of values.

**Parameters:**

- `value` - the list of arguments to set

**Returns:**

`this` instance



---

<a id="m-NArtifactCallBuilder-copyFrom"></a>

#### copyFrom

```java
NArtifactCallBuilder copyFrom(NArtifactCallBuilder value)
```

initialize this instance from the given value

**Parameters:**

- `value` - copy from value

**Returns:**

`this` instance



---

<a id="m-NArtifactCallBuilder-copyFrom"></a>

#### copyFrom

```java
NArtifactCallBuilder copyFrom(NArtifactCall value)
```

initialize this instance from the given value

**Parameters:**

- `value` - copy from value

**Returns:**

`this` instance



---

<a id="m-NArtifactCallBuilder-clear"></a>

#### clear

```java
NArtifactCallBuilder clear()
```

reset this instance to default (null) values

**Returns:**

`this` instance



---

<a id="m-NArtifactCallBuilder-build"></a>

#### build

```java
NArtifactCall build()
```

create an immutable instance of [NArtifactCall](#cls-NArtifactCall)
initialized with all of this attributes.

**Returns:**

immutable instance of [NArtifactCall](#cls-NArtifactCall)



---

<a id="cls-net-thevpc-nuts-artifact-NArtifactNotFoundException"></a>

## NArtifactNotFoundException

```java
public class NArtifactNotFoundException extends NException
```

Exception thrown when the package could not be resolved

**Since:** 0.5.4

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private final NId` | `id` |  |
| `private Set&lt;NIdInvalidLocation&gt;` | `locations` |  |
| `private Set&lt;NIdInvalidDependency&gt;` | `missingDependencies` |  |

### Constructors

#### `NArtifactNotFoundException(NId id)`

```java
public NArtifactNotFoundException(NId id)
```

Constructs a new NArtifactNotFoundException exception

**Parameters:**

- `id` - artifact id


#### `NArtifactNotFoundException(NId id, Throwable cause)`

```java
public NArtifactNotFoundException(NId id, Throwable cause)
```

Constructs a new NArtifactNotFoundException exception

**Parameters:**

- `id` - artifact id
- `cause` - cause


#### `NArtifactNotFoundException(NId id, NIdInvalidDependency[] dependencies, NIdInvalidLocation[] locations, Throwable cause)`

```java
public NArtifactNotFoundException(NId id, NIdInvalidDependency[] dependencies, NIdInvalidLocation[] locations, Throwable cause)
```

Constructs a new NArtifactNotFoundException exception

**Parameters:**

- `id` - artifact id
- `dependencies` - dependencies
- `locations` - locations
- `cause` - cause


#### `NArtifactNotFoundException(NId id, NMsg message, Throwable cause)`

```java
public NArtifactNotFoundException(NId id, NMsg message, Throwable cause)
```

Constructs a new NArtifactNotFoundException exception

**Parameters:**

- `id` - artifact id
- `message` - message
- `cause` - cause


#### `NArtifactNotFoundException(NId id, NMsg message)`

```java
public NArtifactNotFoundException(NId id, NMsg message)
```

Constructs a new NArtifactNotFoundException exception

**Parameters:**

- `id` - artifact id
- `message` - message


### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `private static NMsg` | [prepareMessage(NId, NIdInvalidDependency[], NIdInvalidLocation[], Throwable)](#m-NArtifactNotFoundException-prepareMessage) | Prepare message. |
| `private static NMsg` | [prepareMessage(NId, NMsg, Throwable)](#m-NArtifactNotFoundException-prepareMessage) | Prepare message. |
| `protected static String` | [dependenciesToString(NIdInvalidDependency[])](#m-NArtifactNotFoundException-dependenciesToString) | Dependencies to string. |
| `protected static String` | [dependenciesToString(String, NIdInvalidDependency)](#m-NArtifactNotFoundException-dependenciesToString) | Dependencies to string. |
| `NIdInvalidDependency` | [toInvalidDependency()](#m-NArtifactNotFoundException-toInvalidDependency) | artifact id |
| `NId` | [id()](#m-NArtifactNotFoundException-id) | Id. |
| `Set&lt;NIdInvalidDependency&gt;` | [missingDependencies()](#m-NArtifactNotFoundException-missingDependencies) | Missing dependencies. |
| `Set&lt;NIdInvalidLocation&gt;` | [locations()](#m-NArtifactNotFoundException-locations) | Locations. |

### Method Details


---

<a id="m-NArtifactNotFoundException-prepareMessage"></a>

#### prepareMessage

```java
private static NMsg prepareMessage(NId id, NIdInvalidDependency[] dependencies, NIdInvalidLocation[] locations, Throwable cause)
```

Prepare message.

**Parameters:**

- `id` - id
- `dependencies` - dependencies
- `locations` - locations
- `cause` - cause

**Returns:**

prepare message result



---

<a id="m-NArtifactNotFoundException-prepareMessage"></a>

#### prepareMessage

```java
private static NMsg prepareMessage(NId id, NMsg message, Throwable cause)
```

Prepare message.

**Parameters:**

- `id` - id
- `message` - message
- `cause` - cause

**Returns:**

prepare message result



---

<a id="m-NArtifactNotFoundException-dependenciesToString"></a>

#### dependenciesToString

```java
protected static String dependenciesToString(NIdInvalidDependency[] dependencies)
```

Dependencies to string.

**Parameters:**

- `dependencies` - dependencies

**Returns:**

dependencies to string result



---

<a id="m-NArtifactNotFoundException-dependenciesToString"></a>

#### dependenciesToString

```java
protected static String dependenciesToString(String prefix, NIdInvalidDependency d)
```

Dependencies to string.

**Parameters:**

- `prefix` - prefix
- `d` - d

**Returns:**

dependencies to string result



---

<a id="m-NArtifactNotFoundException-toInvalidDependency"></a>

#### toInvalidDependency

```java
public NIdInvalidDependency toInvalidDependency()
```

artifact id

**Returns:**

artifact id



---

<a id="m-NArtifactNotFoundException-id"></a>

#### id

```java
public NId id()
```

Id.

**Returns:**

id result



---

<a id="m-NArtifactNotFoundException-missingDependencies"></a>

#### missingDependencies

```java
public Set<NIdInvalidDependency> missingDependencies()
```

Missing dependencies.

**Returns:**

missing dependencies result



---

<a id="m-NArtifactNotFoundException-locations"></a>

#### locations

```java
public Set<NIdInvalidLocation> locations()
```

Locations.

**Returns:**

locations result



---

<a id="cls-net-thevpc-nuts-artifact-NClasspathBuilder"></a>

## NClasspathBuilder

```java
public interface NClasspathBuilder extends NComponent, Iterable<NClasspathEntry>
```

**Since:** 1.0.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NClasspathBuilder` | [of()](#m-NClasspathBuilder-of) | Creates a new instance. |
| `NClasspathBuilder` | [add(NClasspathEntry)](#m-NClasspathBuilder-add) | Adds the given element. |
| `NClasspathBuilder` | [add(NDefinition)](#m-NClasspathBuilder-add) | Adds the given element. |
| `NClasspathBuilder` | [add(NId)](#m-NClasspathBuilder-add) |  |
| `NClasspathBuilder` | [add(NPath)](#m-NClasspathBuilder-add) |  |
| `NClasspathBuilder` | [solver(String)](#m-NClasspathBuilder-solver) |  |
| `NClasspathBuilder` | [ignoreCurrentEnvironment(boolean)](#m-NClasspathBuilder-ignoreCurrentEnvironment) |  |
| `NClasspathBuilder` | [dependencyFilter(NDependencyFilter)](#m-NClasspathBuilder-dependencyFilter) |  |
| `NClasspathBuilder` | [repositoryFilter(NRepositoryFilter)](#m-NClasspathBuilder-repositoryFilter) |  |
| `List&lt;NClasspathEntry&gt;` | [resolve()](#m-NClasspathBuilder-resolve) |  |
| `boolean` | [isEmpty()](#m-NClasspathBuilder-isEmpty) | Checks if this is empty. |
| `int` | [size()](#m-NClasspathBuilder-size) | Size. |
| `NStream&lt;NClasspathEntry&gt;` | [stream()](#m-NClasspathBuilder-stream) | Stream. |
| `List&lt;NClasspathEntry&gt;` | [toList()](#m-NClasspathBuilder-toList) | Converts to a list. |

### Method Details


---

<a id="m-NClasspathBuilder-of"></a>

#### of

```java
static NClasspathBuilder of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NClasspathBuilder-add"></a>

#### add

```java
NClasspathBuilder add(NClasspathEntry def)
```

Adds the given element.

**Parameters:**

- `def` - def

**Returns:**

add result



---

<a id="m-NClasspathBuilder-add"></a>

#### add

```java
NClasspathBuilder add(NDefinition def)
```

Adds the given element.

**Parameters:**

- `def` - def

**Returns:**

add result



---

<a id="m-NClasspathBuilder-add"></a>

#### add

```java
NClasspathBuilder add(NId id)
```



---

<a id="m-NClasspathBuilder-add"></a>

#### add

```java
NClasspathBuilder add(NPath path)
```



---

<a id="m-NClasspathBuilder-solver"></a>

#### solver

```java
NClasspathBuilder solver(String solver)
```



---

<a id="m-NClasspathBuilder-ignoreCurrentEnvironment"></a>

#### ignoreCurrentEnvironment

```java
NClasspathBuilder ignoreCurrentEnvironment(boolean ignoreCurrentEnvironment)
```



---

<a id="m-NClasspathBuilder-dependencyFilter"></a>

#### dependencyFilter

```java
NClasspathBuilder dependencyFilter(NDependencyFilter filter)
```



---

<a id="m-NClasspathBuilder-repositoryFilter"></a>

#### repositoryFilter

```java
NClasspathBuilder repositoryFilter(NRepositoryFilter filter)
```



---

<a id="m-NClasspathBuilder-resolve"></a>

#### resolve

```java
List<NClasspathEntry> resolve()
```



---

<a id="m-NClasspathBuilder-isEmpty"></a>

#### isEmpty

```java
boolean isEmpty()
```

Checks if this is empty.

**Returns:**

is empty result



---

<a id="m-NClasspathBuilder-size"></a>

#### size

```java
int size()
```

Size.

**Returns:**

size result



---

<a id="m-NClasspathBuilder-stream"></a>

#### stream

```java
NStream<NClasspathEntry> stream()
```

Stream.

**Returns:**

stream result



---

<a id="m-NClasspathBuilder-toList"></a>

#### toList

```java
List<NClasspathEntry> toList()
```

Converts to a list.

**Returns:**

to list result



---

<a id="cls-net-thevpc-nuts-artifact-NClasspathEntry"></a>

## NClasspathEntry

```java
public interface NClasspathEntry
```

**Since:** 1.0.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NClasspathEntry` | [of(NId)](#m-NClasspathEntry-of) | Creates a new instance. |
| `static NClasspathEntry` | [of(NDependency)](#m-NClasspathEntry-of) | Creates a new instance. |
| `static NClasspathEntry` | [of(NDefinition)](#m-NClasspathEntry-of) | Creates a new instance. |
| `static NClasspathEntry` | [of(NPath)](#m-NClasspathEntry-of) | Creates a new instance. |
| `NId` | [id()](#m-NClasspathEntry-id) |  |
| `NDependency` | [dependency()](#m-NClasspathEntry-dependency) |  |
| `NDefinition` | [definition()](#m-NClasspathEntry-definition) |  |
| `NPath` | [path()](#m-NClasspathEntry-path) |  |
| `NClasspathEntryType` | [type()](#m-NClasspathEntry-type) | Type. |

### Method Details


---

<a id="m-NClasspathEntry-of"></a>

#### of

```java
static NClasspathEntry of(NId id)
```

Creates a new instance.

**Parameters:**

- `id` - id

**Returns:**

of result



---

<a id="m-NClasspathEntry-of"></a>

#### of

```java
static NClasspathEntry of(NDependency dependency)
```

Creates a new instance.

**Parameters:**

- `dependency` - dependency

**Returns:**

of result



---

<a id="m-NClasspathEntry-of"></a>

#### of

```java
static NClasspathEntry of(NDefinition definition)
```

Creates a new instance.

**Parameters:**

- `definition` - definition

**Returns:**

of result



---

<a id="m-NClasspathEntry-of"></a>

#### of

```java
static NClasspathEntry of(NPath path)
```

Creates a new instance.

**Parameters:**

- `path` - path

**Returns:**

of result



---

<a id="m-NClasspathEntry-id"></a>

#### id

```java
NId id()
```



---

<a id="m-NClasspathEntry-dependency"></a>

#### dependency

```java
NDependency dependency()
```



---

<a id="m-NClasspathEntry-definition"></a>

#### definition

```java
NDefinition definition()
```



---

<a id="m-NClasspathEntry-path"></a>

#### path

```java
NPath path()
```



---

<a id="m-NClasspathEntry-type"></a>

#### type

```java
NClasspathEntryType type()
```

Type.

**Returns:**

type result



---

<a id="cls-net-thevpc-nuts-artifact-NClasspathEntryType"></a>

## NClasspathEntryType

```java
public enum NClasspathEntryType implements NEnum
```

Supported dependency scope lists

**Author:** thevpc

**Since:** 0.5.4

### Enum Constants

| Constant | Description |
| :--- | :--- |
| `DEPENDENCY` |  |
| `DEFINITION` |  |
| `PATH` |  |

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private final String` | `id` | lower-cased identifier for the enum entry |

### Constructors

#### `NClasspathEntryType()`

```java
NClasspathEntryType()
```

default constructor


### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;NClasspathEntryType&gt;` | [parse(String)](#m-NClasspathEntryType-parse) | Parse. |
| `String` | [id()](#m-NClasspathEntryType-id) | lower cased identifier. |

### Method Details


---

<a id="m-NClasspathEntryType-parse"></a>

#### parse

```java
public static NOptional<NClasspathEntryType> parse(String value)
```

Parse.

**Parameters:**

- `value` - value

**Returns:**

parse result



---

<a id="m-NClasspathEntryType-id"></a>

#### id

```java
public String id()
```

lower cased identifier.

**Returns:**

lower cased identifier



---

<a id="cls-net-thevpc-nuts-artifact-NDefinition"></a>

## NDefinition

```java
public interface NDefinition extends Serializable, Comparable<NDefinition>
```

Definition is an **immutable** object that contains all information about a artifact identified by it's Id.

**Author:** thevpc

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NId` | [id()](#m-NDefinition-id) | artifact id |
| `NDescriptor` | [descriptor()](#m-NDefinition-descriptor) | return artifact descriptor |
| `NOptional&lt;Set&lt;NDescriptorFlag&gt;&gt;` | [effectiveFlags()](#m-NDefinition-effectiveFlags) | Effective flags. |
| `NOptional&lt;NPath&gt;` | [content()](#m-NDefinition-content) | return artifact content file path. |
| `NOptional&lt;NInstallInformation&gt;` | [installInformation()](#m-NDefinition-installInformation) | return artifact install information. |
| `NOptional&lt;NDescriptor&gt;` | [effectiveDescriptor()](#m-NDefinition-effectiveDescriptor) | return artifact effective descriptor. |
| `NOptional&lt;NDependencies&gt;` | [dependencies()](#m-NDefinition-dependencies) | return all or some of the transitive dependencies of the current Nuts as List result of the search command this is an **optional** property. |
| `NId` | [apiId()](#m-NDefinition-apiId) | return target api id (included in dependency) for the current id. |
| `int` | [compareTo(NDefinition)](#m-NDefinition-compareTo) | Compares this object with the specified definition for order. |
| `String` | [repositoryUuid()](#m-NDefinition-repositoryUuid) | id of the repository providing this id. |
| `NDependency` | [dependency()](#m-NDefinition-dependency) | the current id as a dependency that holds information about conditions, scope and optionality |
| `String` | [repositoryName()](#m-NDefinition-repositoryName) | name of the repository providing this id. |
| `NDefinitionBuilder` | [builder()](#m-NDefinition-builder) | Builder. |

### Method Details


---

<a id="m-NDefinition-id"></a>

#### id

```java
NId id()
```

artifact id

**Returns:**

artifact id



---

<a id="m-NDefinition-descriptor"></a>

#### descriptor

```java
NDescriptor descriptor()
```

return artifact descriptor

**Returns:**

artifact descriptor



---

<a id="m-NDefinition-effectiveFlags"></a>

#### effectiveFlags

```java
NOptional<Set<NDescriptorFlag>> effectiveFlags()
```

Effective flags.

**Returns:**

effective flags result



---

<a id="m-NDefinition-content"></a>

#### content

```java
NOptional<NPath> content()
```

return artifact content file path.

**Returns:**

artifact content file info

**Throws:**

- `NElementNotFoundException` - if the property is not requested



---

<a id="m-NDefinition-installInformation"></a>

#### installInformation

```java
NOptional<NInstallInformation> installInformation()
```

return artifact install information.

**Returns:**

artifact install information

**Throws:**

- `NElementNotFoundException` - if the property is not requested



---

<a id="m-NDefinition-effectiveDescriptor"></a>

#### effectiveDescriptor

```java
NOptional<NDescriptor> effectiveDescriptor()
```

return artifact effective descriptor.

**Returns:**

artifact effective descriptor

**Throws:**

- `NElementNotFoundException` - if the property is not requested



---

<a id="m-NDefinition-dependencies"></a>

#### dependencies

```java
NOptional<NDependencies> dependencies()
```

return all or some of the transitive dependencies of the current Nuts as List
result of the search command
this is an **optional** property.

**Returns:**

all or some of the transitive dependencies of the current Nuts as List
result of the search command.

**Throws:**

- `NElementNotFoundException` - if the property is not requested



---

<a id="m-NDefinition-apiId"></a>

#### apiId

```java
NId apiId()
```

return target api id (included in dependency) for the current id.
This is relevant for runtime, extension and companion ids.
For other regular ids, this returns null.

**Returns:**

target (included in dependency) api id for the current id



---

<a id="m-NDefinition-compareTo"></a>

#### compareTo

```java
@Override
int compareTo(NDefinition other)
```

Compares this object with the specified definition for order.
This is equivalent to comparing subsequent ids.

**Parameters:**

- `other` - other definition to compare with

**Returns:**

a negative integer, zero, or a positive integer as this object
is less than, equal to, or greater than the specified object.



---

<a id="m-NDefinition-repositoryUuid"></a>

#### repositoryUuid

```java
String repositoryUuid()
```

id of the repository providing this id.

**Returns:**

id of the repository providing this id.



---

<a id="m-NDefinition-dependency"></a>

#### dependency

```java
NDependency dependency()
```

the current id as a dependency that holds information about conditions, scope and optionality

**Returns:**

the current id as a dependency that holds information about conditions, scope and optionality



---

<a id="m-NDefinition-repositoryName"></a>

#### repositoryName

```java
String repositoryName()
```

name of the repository providing this id.

**Returns:**

name of the repository providing this id.



---

<a id="m-NDefinition-builder"></a>

#### builder

```java
NDefinitionBuilder builder()
```

Builder.

**Returns:**

builder result



---

<a id="cls-net-thevpc-nuts-artifact-NDefinitionBuilder"></a>

## NDefinitionBuilder

```java
public interface NDefinitionBuilder extends Serializable, Comparable<NDefinitionBuilder>
```

NDefinitionBuilder interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NDependency` | [dependency()](#m-NDefinitionBuilder-dependency) | Dependency. |
| `NDefinitionBuilder` | [dependency(NDependency)](#m-NDefinitionBuilder-dependency) | Dependency. |
| `NDefinition` | [build()](#m-NDefinitionBuilder-build) | Build. |
| `String` | [repositoryUuid()](#m-NDefinitionBuilder-repositoryUuid) | Repository uuid. |
| `String` | [repositoryName()](#m-NDefinitionBuilder-repositoryName) | Repository name. |
| `NDefinitionBuilder` | [id(NId)](#m-NDefinitionBuilder-id) | Id. |
| `NId` | [id()](#m-NDefinitionBuilder-id) | Id. |
| `boolean` | [isTemporary()](#m-NDefinitionBuilder-isTemporary) | Checks if this is temporary. |
| `NDescriptor` | [descriptor()](#m-NDefinitionBuilder-descriptor) | Descriptor. |
| `NDefinitionBuilder` | [copy()](#m-NDefinitionBuilder-copy) | Copy. |
| `NOptional&lt;NPath&gt;` | [content()](#m-NDefinitionBuilder-content) | Content. |
| `NOptional&lt;NDescriptor&gt;` | [effectiveDescriptor()](#m-NDefinitionBuilder-effectiveDescriptor) | Effective descriptor. |
| `NOptional&lt;NInstallInformation&gt;` | [installInformation()](#m-NDefinitionBuilder-installInformation) | Install information. |
| `NOptional&lt;NDependencies&gt;` | [dependencies()](#m-NDefinitionBuilder-dependencies) | Dependencies. |
| `NDefinitionBuilder` | [content(NPath)](#m-NDefinitionBuilder-content) | Content. |
| `NDefinitionBuilder` | [descriptor(NDescriptor)](#m-NDefinitionBuilder-descriptor) | Descriptor. |
| `NDefinitionBuilder` | [effectiveFlags(Set&lt;NDescriptorFlag&gt;)](#m-NDefinitionBuilder-effectiveFlags) | Effective flags. |
| `NDefinitionBuilder` | [effectiveDescriptor(NDescriptor)](#m-NDefinitionBuilder-effectiveDescriptor) | Effective descriptor. |
| `NDefinitionBuilder` | [installInformation(NInstallInformation)](#m-NDefinitionBuilder-installInformation) | Install information. |
| `NDefinitionBuilder` | [dependencies(NDependencies)](#m-NDefinitionBuilder-dependencies) | Dependencies. |
| `NDefinitionBuilder` | [apiId(NId)](#m-NDefinitionBuilder-apiId) | Api id. |
| `NId` | [apiId()](#m-NDefinitionBuilder-apiId) | Api id. |
| `NDefinitionBuilder` | [repositoryUuid(String)](#m-NDefinitionBuilder-repositoryUuid) | Repository uuid. |
| `NDefinitionBuilder` | [repositoryName(String)](#m-NDefinitionBuilder-repositoryName) | Repository name. |
| `NOptional&lt;Set&lt;NDescriptorFlag&gt;&gt;` | [effectiveFlags()](#m-NDefinitionBuilder-effectiveFlags) | Effective flags. |

### Method Details


---

<a id="m-NDefinitionBuilder-dependency"></a>

#### dependency

```java
NDependency dependency()
```

Dependency.

**Returns:**

dependency result



---

<a id="m-NDefinitionBuilder-dependency"></a>

#### dependency

```java
NDefinitionBuilder dependency(NDependency dependency)
```

Dependency.

**Parameters:**

- `dependency` - dependency

**Returns:**

dependency result



---

<a id="m-NDefinitionBuilder-build"></a>

#### build

```java
NDefinition build()
```

Build.

**Returns:**

build result



---

<a id="m-NDefinitionBuilder-repositoryUuid"></a>

#### repositoryUuid

```java
@NGetter
String repositoryUuid()
```

**Accessor:** Property getter.

Repository uuid.

**Returns:**

repository uuid result



---

<a id="m-NDefinitionBuilder-repositoryName"></a>

#### repositoryName

```java
@NGetter
String repositoryName()
```

**Accessor:** Property getter.

Repository name.

**Returns:**

repository name result



---

<a id="m-NDefinitionBuilder-id"></a>

#### id

```java
@NSetter
NDefinitionBuilder id(NId id)
```

**Mutator:** Property setter.

Id.

**Parameters:**

- `id` - id

**Returns:**

id result



---

<a id="m-NDefinitionBuilder-id"></a>

#### id

```java
NId id()
```

Id.

**Returns:**

id result



---

<a id="m-NDefinitionBuilder-isTemporary"></a>

#### isTemporary

```java
boolean isTemporary()
```

Checks if this is temporary.

**Returns:**

is temporary result



---

<a id="m-NDefinitionBuilder-descriptor"></a>

#### descriptor

```java
@NGetter
NDescriptor descriptor()
```

**Accessor:** Property getter.

Descriptor.

**Returns:**

descriptor result



---

<a id="m-NDefinitionBuilder-copy"></a>

#### copy

```java
NDefinitionBuilder copy()
```

Copy.

**Returns:**

copy result



---

<a id="m-NDefinitionBuilder-content"></a>

#### content

```java
NOptional<NPath> content()
```

Content.

**Returns:**

content result



---

<a id="m-NDefinitionBuilder-effectiveDescriptor"></a>

#### effectiveDescriptor

```java
NOptional<NDescriptor> effectiveDescriptor()
```

Effective descriptor.

**Returns:**

effective descriptor result



---

<a id="m-NDefinitionBuilder-installInformation"></a>

#### installInformation

```java
NOptional<NInstallInformation> installInformation()
```

Install information.

**Returns:**

install information result



---

<a id="m-NDefinitionBuilder-dependencies"></a>

#### dependencies

```java
NOptional<NDependencies> dependencies()
```

Dependencies.

**Returns:**

dependencies result



---

<a id="m-NDefinitionBuilder-content"></a>

#### content

```java
NDefinitionBuilder content(NPath content)
```

Content.

**Parameters:**

- `content` - content

**Returns:**

content result



---

<a id="m-NDefinitionBuilder-descriptor"></a>

#### descriptor

```java
NDefinitionBuilder descriptor(NDescriptor descriptor)
```

Descriptor.

**Parameters:**

- `descriptor` - descriptor

**Returns:**

descriptor result



---

<a id="m-NDefinitionBuilder-effectiveFlags"></a>

#### effectiveFlags

```java
NDefinitionBuilder effectiveFlags(Set<NDescriptorFlag> effectiveFlags)
```

Effective flags.

**Parameters:**

- `effectiveFlags` - effective flags

**Returns:**

effective flags result



---

<a id="m-NDefinitionBuilder-effectiveDescriptor"></a>

#### effectiveDescriptor

```java
NDefinitionBuilder effectiveDescriptor(NDescriptor effectiveDescriptor)
```

Effective descriptor.

**Parameters:**

- `effectiveDescriptor` - effective descriptor

**Returns:**

effective descriptor result



---

<a id="m-NDefinitionBuilder-installInformation"></a>

#### installInformation

```java
NDefinitionBuilder installInformation(NInstallInformation install)
```

Install information.

**Parameters:**

- `install` - install

**Returns:**

install information result



---

<a id="m-NDefinitionBuilder-dependencies"></a>

#### dependencies

```java
NDefinitionBuilder dependencies(NDependencies dependencies)
```

Dependencies.

**Parameters:**

- `dependencies` - dependencies

**Returns:**

dependencies result



---

<a id="m-NDefinitionBuilder-apiId"></a>

#### apiId

```java
NDefinitionBuilder apiId(NId apiId)
```

Api id.

**Parameters:**

- `apiId` - api id

**Returns:**

api id result



---

<a id="m-NDefinitionBuilder-apiId"></a>

#### apiId

```java
NId apiId()
```

Api id.

**Returns:**

api id result



---

<a id="m-NDefinitionBuilder-repositoryUuid"></a>

#### repositoryUuid

```java
NDefinitionBuilder repositoryUuid(String repositoryUuid)
```

Repository uuid.

**Parameters:**

- `repositoryUuid` - repository uuid

**Returns:**

repository uuid result



---

<a id="m-NDefinitionBuilder-repositoryName"></a>

#### repositoryName

```java
NDefinitionBuilder repositoryName(String repositoryName)
```

Repository name.

**Parameters:**

- `repositoryName` - repository name

**Returns:**

repository name result



---

<a id="m-NDefinitionBuilder-effectiveFlags"></a>

#### effectiveFlags

```java
NOptional<Set<NDescriptorFlag>> effectiveFlags()
```

Effective flags.

**Returns:**

effective flags result



---

<a id="cls-net-thevpc-nuts-artifact-NDefinitionFilter"></a>

## NDefinitionFilter

```java
public interface NDefinitionFilter extends NFilter
```

Descriptor filter

**Author:** thevpc

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NDefinitionFilter` | [ofNonnull(NFilter)](#m-NDefinitionFilter-ofNonnull) | Creates a new instance of nonnull. |
| `static NDefinitionFilter` | [ofAlways()](#m-NDefinitionFilter-ofAlways) | Creates a new instance of always. |
| `static NDefinitionFilter` | [ofNever()](#m-NDefinitionFilter-ofNever) | Creates a new instance of never. |
| `static NDefinitionFilter` | [ofAll(NFilter)](#m-NDefinitionFilter-ofAll) | Creates a new instance of all. |
| `static NDefinitionFilter` | [ofAny(NFilter)](#m-NDefinitionFilter-ofAny) | Creates a new instance of any. |
| `static NDefinitionFilter` | [ofNot(NFilter)](#m-NDefinitionFilter-ofNot) | Creates a new instance of not. |
| `static NDefinitionFilter` | [ofNone(NFilter)](#m-NDefinitionFilter-ofNone) | Creates a new instance of none. |
| `static NDefinitionFilter` | [ofFrom(NFilter)](#m-NDefinitionFilter-ofFrom) | Creates a new instance of from. |
| `static NDefinitionFilter` | [ofAs(NFilter)](#m-NDefinitionFilter-ofAs) | Creates a new instance of as. |
| `static NDefinitionFilter` | [of(String)](#m-NDefinitionFilter-of) | Creates a new instance. |
| `static NDefinitionFilter` | [ofArch(Collection&lt;NArchFamily&gt;)](#m-NDefinitionFilter-ofArch) | accept only dependencies that match the given archs |
| `static NDefinitionFilter` | [ofArch(NArchFamily)](#m-NDefinitionFilter-ofArch) | accept only dependencies that match the given archs |
| `static NDefinitionFilter` | [ofArch(String)](#m-NDefinitionFilter-ofArch) | accept only dependencies that match the given arch |
| `static NDefinitionFilter` | [ofOsFamily(Collection&lt;NOsFamily&gt;)](#m-NDefinitionFilter-ofOsFamily) | accept only dependencies that match the given OSes |
| `static NDefinitionFilter` | [ofOsFamily(NOsFamily)](#m-NDefinitionFilter-ofOsFamily) | Creates a new instance of os. |
| `static NDefinitionFilter` | [ofArchFamily(NArchFamily)](#m-NDefinitionFilter-ofArchFamily) | Creates a new instance of arch. |
| `static NDefinitionFilter` | [ofOsDist(String)](#m-NDefinitionFilter-ofOsDist) | accept only dependencies that match the given OsDist list |
| `static NDefinitionFilter` | [ofOsDist(Collection&lt;String&gt;)](#m-NDefinitionFilter-ofOsDist) | accept only dependencies that match the given OsDist list |
| `static NDefinitionFilter` | [ofCurrentDesktopEnvironmentFamily()](#m-NDefinitionFilter-ofCurrentDesktopEnvironmentFamily) | accept only dependencies that match the current Desktop Environment |
| `static NDefinitionFilter` | [ofCurrentArch()](#m-NDefinitionFilter-ofCurrentArch) | accept only dependencies that match the current Architecture |
| `static NDefinitionFilter` | [ofCurrentOsFamily()](#m-NDefinitionFilter-ofCurrentOsFamily) | accept only dependencies that match the current OS |
| `static NDefinitionFilter` | [ofCurrentEnv()](#m-NDefinitionFilter-ofCurrentEnv) | accept only dependencies that match the current environment (OS, arch, etc...) |
| `static NDefinitionFilter` | [ofDesktopEnvironmentFamily(NDesktopEnvironmentFamily)](#m-NDefinitionFilter-ofDesktopEnvironmentFamily) | accept only dependencies that match the given Desktop Environment |
| `static NDefinitionFilter` | [ofDesktopEnvironment(String)](#m-NDefinitionFilter-ofDesktopEnvironment) | Creates a new instance of desktop. |
| `static NDefinitionFilter` | [ofDesktopEnvironment(NId)](#m-NDefinitionFilter-ofDesktopEnvironment) | Creates a new instance of desktop. |
| `static NDefinitionFilter` | [ofPlatform(String)](#m-NDefinitionFilter-ofPlatform) | accept only dependencies that match any of the given Platform |
| `static NDefinitionFilter` | [ofDefaultVersion(Boolean)](#m-NDefinitionFilter-ofDefaultVersion) | search for default versions status. |
| `static NDefinitionFilter` | [ofName(String)](#m-NDefinitionFilter-ofName) | Creates a new instance of name. |
| `static NDefinitionFilter` | [ofEnv(Map&lt;String,String&gt;)](#m-NDefinitionFilter-ofEnv) | Creates a new instance of env. |
| `static NDefinitionFilter` | [ofPackaging(String)](#m-NDefinitionFilter-ofPackaging) | Creates a new instance of packaging. |
| `static NDefinitionFilter` | [ofOs(NId)](#m-NDefinitionFilter-ofOs) | Creates a new instance of os. |
| `static NDefinitionFilter` | [ofPlatformFamily(NRuntimeDistributionFamily)](#m-NDefinitionFilter-ofPlatformFamily) | Creates a new instance of platform. |
| `static NDefinitionFilter` | [ofPlatform(NId)](#m-NDefinitionFilter-ofPlatform) | Creates a new instance of platform. |
| `static NDefinitionFilter` | [ofOs(String)](#m-NDefinitionFilter-ofOs) | Creates a new instance of os. |
| `static NDefinitionFilter` | [ofPackaging(Collection&lt;String&gt;)](#m-NDefinitionFilter-ofPackaging) | Creates a new instance of packaging. |
| `static NDefinitionFilter` | [ofPlatform(Collection&lt;String&gt;)](#m-NDefinitionFilter-ofPlatform) | Creates a new instance of platform. |
| `static NDefinitionFilter` | [ofDesktopEnvironment(Collection&lt;String&gt;)](#m-NDefinitionFilter-ofDesktopEnvironment) | Creates a new instance of desktop. |
| `static NDefinitionFilter` | [ofFlag(NDescriptorFlag)](#m-NDefinitionFilter-ofFlag) | Creates a new instance of flag. |
| `static NDefinitionFilter` | [ofFlag(Collection&lt;NDescriptorFlag&gt;)](#m-NDefinitionFilter-ofFlag) | Creates a new instance of flag. |
| `static NDefinitionFilter` | [ofEffectiveFlag(NDescriptorFlag)](#m-NDefinitionFilter-ofEffectiveFlag) | Creates a new instance of effective. |
| `static NDefinitionFilter` | [ofEffectiveFlag(Collection&lt;NDescriptorFlag&gt;)](#m-NDefinitionFilter-ofEffectiveFlag) | Creates a new instance of effective. |
| `static NDefinitionFilter` | [ofExtension(NVersion)](#m-NDefinitionFilter-ofExtension) | Creates a new instance of extension. |
| `static NDefinitionFilter` | [ofRuntime(NVersion)](#m-NDefinitionFilter-ofRuntime) | Creates a new instance of runtime. |
| `static NDefinitionFilter` | [ofCompanion(NVersion)](#m-NDefinitionFilter-ofCompanion) | Creates a new instance of companion. |
| `static NDefinitionFilter` | [ofApiVersion(NVersion)](#m-NDefinitionFilter-ofApiVersion) | Creates a new instance of api. |
| `static NDefinitionFilter` | [ofBootVersion(NVersion)](#m-NDefinitionFilter-ofBootVersion) | Creates a new instance of boot. |
| `static NDefinitionFilter` | [ofLockedIds(String)](#m-NDefinitionFilter-ofLockedIds) | Creates a new instance of locked. |
| `static NDefinitionFilter` | [ofLockedIds(NId)](#m-NDefinitionFilter-ofLockedIds) | Creates a new instance of locked. |
| `static NDefinitionFilter` | [ofVersion(String)](#m-NDefinitionFilter-ofVersion) | Creates a new instance of version. |
| `static NDefinitionFilter` | [ofVersion(NVersion)](#m-NDefinitionFilter-ofVersion) | Creates a new instance of version. |
| `static NDefinitionFilter` | [ofVersion(NVersionFilter)](#m-NDefinitionFilter-ofVersion) | Creates a new instance of version. |
| `static NDefinitionFilter` | [ofInstalled(boolean)](#m-NDefinitionFilter-ofInstalled) | Creates a new instance of installed. |
| `static NDefinitionFilter` | [ofInstalledOrRequired(boolean)](#m-NDefinitionFilter-ofInstalledOrRequired) | Creates a new instance of installed. |
| `static NDefinitionFilter` | [ofRequired(boolean)](#m-NDefinitionFilter-ofRequired) | Creates a new instance of required. |
| `static NDefinitionFilter` | [ofDefaultVersion(boolean)](#m-NDefinitionFilter-ofDefaultVersion) | Creates a new instance of default. |
| `static NDefinitionFilter` | [ofObsolete(boolean)](#m-NDefinitionFilter-ofObsolete) | Creates a new instance of obsolete. |
| `static NDefinitionFilter` | [ofDeployed(boolean)](#m-NDefinitionFilter-ofDeployed) | Creates a new instance of deployed. |
| `boolean` | [acceptDefinition(NDefinition)](#m-NDefinitionFilter-acceptDefinition) | return true if definition is accepted |
| `NDefinitionFilter` | [or(NDefinitionFilter)](#m-NDefinitionFilter-or) | Or. |
| `NDefinitionFilter` | [and(NDefinitionFilter)](#m-NDefinitionFilter-and) | And. |
| `NDefinitionFilter` | [neg()](#m-NDefinitionFilter-neg) | Neg. |

### Method Details


---

<a id="m-NDefinitionFilter-ofNonnull"></a>

#### ofNonnull

```java
static NDefinitionFilter ofNonnull(NFilter filter)
```

Creates a new instance of nonnull.

**Parameters:**

- `filter` - filter

**Returns:**

of nonnull result



---

<a id="m-NDefinitionFilter-ofAlways"></a>

#### ofAlways

```java
static NDefinitionFilter ofAlways()
```

Creates a new instance of always.

**Returns:**

of always result



---

<a id="m-NDefinitionFilter-ofNever"></a>

#### ofNever

```java
static NDefinitionFilter ofNever()
```

Creates a new instance of never.

**Returns:**

of never result



---

<a id="m-NDefinitionFilter-ofAll"></a>

#### ofAll

```java
static NDefinitionFilter ofAll(NFilter others)
```

Creates a new instance of all.

**Parameters:**

- `others` - others

**Returns:**

of all result



---

<a id="m-NDefinitionFilter-ofAny"></a>

#### ofAny

```java
static NDefinitionFilter ofAny(NFilter others)
```

Creates a new instance of any.

**Parameters:**

- `others` - others

**Returns:**

of any result



---

<a id="m-NDefinitionFilter-ofNot"></a>

#### ofNot

```java
static NDefinitionFilter ofNot(NFilter other)
```

Creates a new instance of not.

**Parameters:**

- `other` - other

**Returns:**

of not result



---

<a id="m-NDefinitionFilter-ofNone"></a>

#### ofNone

```java
static NDefinitionFilter ofNone(NFilter others)
```

Creates a new instance of none.

**Parameters:**

- `others` - others

**Returns:**

of none result



---

<a id="m-NDefinitionFilter-ofFrom"></a>

#### ofFrom

```java
static NDefinitionFilter ofFrom(NFilter a)
```

Creates a new instance of from.

**Parameters:**

- `a` - a

**Returns:**

of from result



---

<a id="m-NDefinitionFilter-ofAs"></a>

#### ofAs

```java
static NDefinitionFilter ofAs(NFilter a)
```

Creates a new instance of as.

**Parameters:**

- `a` - a

**Returns:**

of as result



---

<a id="m-NDefinitionFilter-of"></a>

#### of

```java
static NDefinitionFilter of(String expression)
```

Creates a new instance.

**Parameters:**

- `expression` - expression

**Returns:**

of result



---

<a id="m-NDefinitionFilter-ofArch"></a>

#### ofArch

```java
static NDefinitionFilter ofArch(Collection<NArchFamily> values)
```

accept only dependencies that match the given archs

**Parameters:**

- `values` - accepted archs list

**Returns:**

a filter that accepts only dependencies that match the given archs



---

<a id="m-NDefinitionFilter-ofArch"></a>

#### ofArch

```java
static NDefinitionFilter ofArch(NArchFamily values)
```

accept only dependencies that match the given archs

**Parameters:**

- `values` - accepted arch list

**Returns:**

a filter that accepts only dependencies that match the given archs



---

<a id="m-NDefinitionFilter-ofArch"></a>

#### ofArch

```java
static NDefinitionFilter ofArch(String values)
```

accept only dependencies that match the given arch

**Parameters:**

- `values` - accepted arch

**Returns:**

a filter that accepts only dependencies that match the given arch



---

<a id="m-NDefinitionFilter-ofOsFamily"></a>

#### ofOsFamily

```java
static NDefinitionFilter ofOsFamily(Collection<NOsFamily> values)
```

accept only dependencies that match the given OSes

**Parameters:**

- `values` - accepted OS list

**Returns:**

a filter that accepts only dependencies that match the given OSes



---

<a id="m-NDefinitionFilter-ofOsFamily"></a>

#### ofOsFamily

```java
static NDefinitionFilter ofOsFamily(NOsFamily values)
```

Creates a new instance of os. family.

**Parameters:**

- `values` - values

**Returns:**

of os family result



---

<a id="m-NDefinitionFilter-ofArchFamily"></a>

#### ofArchFamily

```java
static NDefinitionFilter ofArchFamily(NArchFamily values)
```

Creates a new instance of arch. family.

**Parameters:**

- `values` - values

**Returns:**

of arch family result



---

<a id="m-NDefinitionFilter-ofOsDist"></a>

#### ofOsDist

```java
static NDefinitionFilter ofOsDist(String values)
```

accept only dependencies that match the given OsDist list

**Parameters:**

- `values` - accepted OsDist list

**Returns:**

a filter that accepts only dependencies that match the given OsDist list



---

<a id="m-NDefinitionFilter-ofOsDist"></a>

#### ofOsDist

```java
static NDefinitionFilter ofOsDist(Collection<String> values)
```

accept only dependencies that match the given OsDist list

**Parameters:**

- `values` - accepted OsDist list

**Returns:**

a filter that accepts only dependencies that match the given OsDist list



---

<a id="m-NDefinitionFilter-ofCurrentDesktopEnvironmentFamily"></a>

#### ofCurrentDesktopEnvironmentFamily

```java
static NDefinitionFilter ofCurrentDesktopEnvironmentFamily()
```

accept only dependencies that match the current Desktop Environment

**Returns:**

a filter that accepts only dependencies that match the current Desktop Environment



---

<a id="m-NDefinitionFilter-ofCurrentArch"></a>

#### ofCurrentArch

```java
static NDefinitionFilter ofCurrentArch()
```

accept only dependencies that match the current Architecture

**Returns:**

a filter that accepts only dependencies that match the current Architecture



---

<a id="m-NDefinitionFilter-ofCurrentOsFamily"></a>

#### ofCurrentOsFamily

```java
static NDefinitionFilter ofCurrentOsFamily()
```

accept only dependencies that match the current OS

**Returns:**

a filter that accepts only dependencies that match the current OS



---

<a id="m-NDefinitionFilter-ofCurrentEnv"></a>

#### ofCurrentEnv

```java
static NDefinitionFilter ofCurrentEnv()
```

accept only dependencies that match the current environment (OS, arch, etc...)

**Returns:**

a filter that accept only dependencies that match the current environment (OS, arch, etc...)



---

<a id="m-NDefinitionFilter-ofDesktopEnvironmentFamily"></a>

#### ofDesktopEnvironmentFamily

```java
static NDefinitionFilter ofDesktopEnvironmentFamily(NDesktopEnvironmentFamily values)
```

accept only dependencies that match the given Desktop Environment

**Parameters:**

- `values` - accepted Desktop Environment

**Returns:**

a filter that accepts only dependencies that match the given Desktop Environment



---

<a id="m-NDefinitionFilter-ofDesktopEnvironment"></a>

#### ofDesktopEnvironment

```java
static NDefinitionFilter ofDesktopEnvironment(String values)
```

Creates a new instance of desktop. environment.

**Parameters:**

- `values` - values

**Returns:**

of desktop environment result



---

<a id="m-NDefinitionFilter-ofDesktopEnvironment"></a>

#### ofDesktopEnvironment

```java
static NDefinitionFilter ofDesktopEnvironment(NId values)
```

Creates a new instance of desktop. environment.

**Parameters:**

- `values` - values

**Returns:**

of desktop environment result



---

<a id="m-NDefinitionFilter-ofPlatform"></a>

#### ofPlatform

```java
static NDefinitionFilter ofPlatform(String values)
```

accept only dependencies that match any of the given Platform

**Parameters:**

- `values` - accepted Desktop Environment list

**Returns:**

a filter that accepts only dependencies that match any of the given Platform



---

<a id="m-NDefinitionFilter-ofDefaultVersion"></a>

#### ofDefaultVersion

```java
static NDefinitionFilter ofDefaultVersion(Boolean value)
```

search for default versions status.


- return true of only default values are searched for
- return false of only default values are searched for
- return null of both default values and non default ones are searched
for

**Parameters:**

- `value` - 

**Returns:**

search for default versions status



---

<a id="m-NDefinitionFilter-ofName"></a>

#### ofName

```java
static NDefinitionFilter ofName(String values)
```

Creates a new instance of name.

**Parameters:**

- `values` - values

**Returns:**

of name result



---

<a id="m-NDefinitionFilter-ofEnv"></a>

#### ofEnv

```java
static NDefinitionFilter ofEnv(Map<String,String> values)
```

Creates a new instance of env.

**Parameters:**

- `values` - values

**Returns:**

of env result



---

<a id="m-NDefinitionFilter-ofPackaging"></a>

#### ofPackaging

```java
static NDefinitionFilter ofPackaging(String values)
```

Creates a new instance of packaging.

**Parameters:**

- `values` - values

**Returns:**

of packaging result



---

<a id="m-NDefinitionFilter-ofOs"></a>

#### ofOs

```java
static NDefinitionFilter ofOs(NId values)
```

Creates a new instance of os.

**Parameters:**

- `values` - values

**Returns:**

of os result



---

<a id="m-NDefinitionFilter-ofPlatformFamily"></a>

#### ofPlatformFamily

```java
static NDefinitionFilter ofPlatformFamily(NRuntimeDistributionFamily values)
```

Creates a new instance of platform. family.

**Parameters:**

- `values` - values

**Returns:**

of platform family result



---

<a id="m-NDefinitionFilter-ofPlatform"></a>

#### ofPlatform

```java
static NDefinitionFilter ofPlatform(NId values)
```

Creates a new instance of platform.

**Parameters:**

- `values` - values

**Returns:**

of platform result



---

<a id="m-NDefinitionFilter-ofOs"></a>

#### ofOs

```java
static NDefinitionFilter ofOs(String values)
```

Creates a new instance of os.

**Parameters:**

- `values` - values

**Returns:**

of os result



---

<a id="m-NDefinitionFilter-ofPackaging"></a>

#### ofPackaging

```java
static NDefinitionFilter ofPackaging(Collection<String> values)
```

Creates a new instance of packaging.

**Parameters:**

- `values` - values

**Returns:**

of packaging result



---

<a id="m-NDefinitionFilter-ofPlatform"></a>

#### ofPlatform

```java
static NDefinitionFilter ofPlatform(Collection<String> values)
```

Creates a new instance of platform.

**Parameters:**

- `values` - values

**Returns:**

of platform result



---

<a id="m-NDefinitionFilter-ofDesktopEnvironment"></a>

#### ofDesktopEnvironment

```java
static NDefinitionFilter ofDesktopEnvironment(Collection<String> values)
```

Creates a new instance of desktop. environment.

**Parameters:**

- `values` - values

**Returns:**

of desktop environment result



---

<a id="m-NDefinitionFilter-ofFlag"></a>

#### ofFlag

```java
static NDefinitionFilter ofFlag(NDescriptorFlag values)
```

Creates a new instance of flag.

**Parameters:**

- `values` - values

**Returns:**

of flag result



---

<a id="m-NDefinitionFilter-ofFlag"></a>

#### ofFlag

```java
static NDefinitionFilter ofFlag(Collection<NDescriptorFlag> values)
```

Creates a new instance of flag.

**Parameters:**

- `values` - values

**Returns:**

of flag result



---

<a id="m-NDefinitionFilter-ofEffectiveFlag"></a>

#### ofEffectiveFlag

```java
static NDefinitionFilter ofEffectiveFlag(NDescriptorFlag values)
```

Creates a new instance of effective. flag.

**Parameters:**

- `values` - values

**Returns:**

of effective flag result



---

<a id="m-NDefinitionFilter-ofEffectiveFlag"></a>

#### ofEffectiveFlag

```java
static NDefinitionFilter ofEffectiveFlag(Collection<NDescriptorFlag> values)
```

Creates a new instance of effective. flag.

**Parameters:**

- `values` - values

**Returns:**

of effective flag result



---

<a id="m-NDefinitionFilter-ofExtension"></a>

#### ofExtension

```java
static NDefinitionFilter ofExtension(NVersion value)
```

Creates a new instance of extension.

**Parameters:**

- `value` - value

**Returns:**

of extension result



---

<a id="m-NDefinitionFilter-ofRuntime"></a>

#### ofRuntime

```java
static NDefinitionFilter ofRuntime(NVersion value)
```

Creates a new instance of runtime.

**Parameters:**

- `value` - value

**Returns:**

of runtime result



---

<a id="m-NDefinitionFilter-ofCompanion"></a>

#### ofCompanion

```java
static NDefinitionFilter ofCompanion(NVersion value)
```

Creates a new instance of companion.

**Parameters:**

- `value` - value

**Returns:**

of companion result



---

<a id="m-NDefinitionFilter-ofApiVersion"></a>

#### ofApiVersion

```java
static NDefinitionFilter ofApiVersion(NVersion value)
```

Creates a new instance of api. version.

**Parameters:**

- `value` - value

**Returns:**

of api version result



---

<a id="m-NDefinitionFilter-ofBootVersion"></a>

#### ofBootVersion

```java
static NDefinitionFilter ofBootVersion(NVersion value)
```

Creates a new instance of boot. version.

**Parameters:**

- `value` - value

**Returns:**

of boot version result



---

<a id="m-NDefinitionFilter-ofLockedIds"></a>

#### ofLockedIds

```java
static NDefinitionFilter ofLockedIds(String values)
```

Creates a new instance of locked. ids.

**Parameters:**

- `values` - values

**Returns:**

of locked ids result



---

<a id="m-NDefinitionFilter-ofLockedIds"></a>

#### ofLockedIds

```java
static NDefinitionFilter ofLockedIds(NId values)
```

Creates a new instance of locked. ids.

**Parameters:**

- `values` - values

**Returns:**

of locked ids result



---

<a id="m-NDefinitionFilter-ofVersion"></a>

#### ofVersion

```java
static NDefinitionFilter ofVersion(String value)
```

Creates a new instance of version.

**Parameters:**

- `value` - value

**Returns:**

of version result



---

<a id="m-NDefinitionFilter-ofVersion"></a>

#### ofVersion

```java
static NDefinitionFilter ofVersion(NVersion value)
```

Creates a new instance of version.

**Parameters:**

- `value` - value

**Returns:**

of version result



---

<a id="m-NDefinitionFilter-ofVersion"></a>

#### ofVersion

```java
static NDefinitionFilter ofVersion(NVersionFilter value)
```

Creates a new instance of version.

**Parameters:**

- `value` - value

**Returns:**

of version result



---

<a id="m-NDefinitionFilter-ofInstalled"></a>

#### ofInstalled

```java
static NDefinitionFilter ofInstalled(boolean value)
```

Creates a new instance of installed.

**Parameters:**

- `value` - value

**Returns:**

of installed result



---

<a id="m-NDefinitionFilter-ofInstalledOrRequired"></a>

#### ofInstalledOrRequired

```java
static NDefinitionFilter ofInstalledOrRequired(boolean value)
```

Creates a new instance of installed. or required.

**Parameters:**

- `value` - value

**Returns:**

of installed or required result



---

<a id="m-NDefinitionFilter-ofRequired"></a>

#### ofRequired

```java
static NDefinitionFilter ofRequired(boolean value)
```

Creates a new instance of required.

**Parameters:**

- `value` - value

**Returns:**

of required result



---

<a id="m-NDefinitionFilter-ofDefaultVersion"></a>

#### ofDefaultVersion

```java
static NDefinitionFilter ofDefaultVersion(boolean value)
```

Creates a new instance of default. version.

**Parameters:**

- `value` - value

**Returns:**

of default version result



---

<a id="m-NDefinitionFilter-ofObsolete"></a>

#### ofObsolete

```java
static NDefinitionFilter ofObsolete(boolean value)
```

Creates a new instance of obsolete.

**Parameters:**

- `value` - value

**Returns:**

of obsolete result



---

<a id="m-NDefinitionFilter-ofDeployed"></a>

#### ofDeployed

```java
static NDefinitionFilter ofDeployed(boolean value)
```

Creates a new instance of deployed.

**Parameters:**

- `value` - value

**Returns:**

of deployed result



---

<a id="m-NDefinitionFilter-acceptDefinition"></a>

#### acceptDefinition

```java
boolean acceptDefinition(NDefinition descriptor)
```

return true if definition is accepted

**Parameters:**

- `descriptor` - descriptor

**Returns:**

true if descriptor is accepted



---

<a id="m-NDefinitionFilter-or"></a>

#### or

```java
NDefinitionFilter or(NDefinitionFilter other)
```

Or.

**Parameters:**

- `other` - other

**Returns:**

or result



---

<a id="m-NDefinitionFilter-and"></a>

#### and

```java
NDefinitionFilter and(NDefinitionFilter other)
```

And.

**Parameters:**

- `other` - other

**Returns:**

and result



---

<a id="m-NDefinitionFilter-neg"></a>

#### neg

```java
NDefinitionFilter neg()
```

Neg.

**Returns:**

neg result



---

<a id="cls-net-thevpc-nuts-artifact-NDependencies"></a>

## NDependencies

```java
public interface NDependencies extends NIterable<NDependency>
```

Package dependencies representation

**Author:** thevpc

**Since:** 0.8.2

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NStream&lt;NId&gt;` | [sourceIds()](#m-NDependencies-sourceIds) | sources of this dependencies |
| `String` | [solver()](#m-NDependencies-solver) | solver name used to compute the dependencies |
| `NDependencyFilter` | [filter()](#m-NDependencies-filter) | filter used to compute the dependencies |
| `NStream&lt;NDependency&gt;` | [immediate()](#m-NDependencies-immediate) | return immediate dependencies (not including source ids) |
| `NStream&lt;NDependency&gt;` | [transitive()](#m-NDependencies-transitive) |  |
| `NStream&lt;NDependency&gt;` | [transitiveWithSource()](#m-NDependencies-transitiveWithSource) | transitive dependencies merged with source ids, which may constitute a full classpath |
| `NStream&lt;NDependencyTreeNode&gt;` | [transitiveNodes()](#m-NDependencies-transitiveNodes) | return all or some of the transitive dependencies of the current Artifact as Tree result of the search command |
| `NStream&lt;NDependencyTreeNode&gt;` | [sourceNodes()](#m-NDependencies-sourceNodes) | return source Nodes tree including sources and their immediate dependencies at each Node level |
| `List&lt;NDependency&gt;` | [toList()](#m-NDependencies-toList) | equivalent to transitive().toList() |

### Method Details


---

<a id="m-NDependencies-sourceIds"></a>

#### sourceIds

```java
NStream<NId> sourceIds()
```

sources of this dependencies

**Returns:**

sources of this dependencies



---

<a id="m-NDependencies-solver"></a>

#### solver

```java
String solver()
```

solver name used to compute the dependencies

**Returns:**

solver name used to compute the dependencies



---

<a id="m-NDependencies-filter"></a>

#### filter

```java
NDependencyFilter filter()
```

filter used to compute the dependencies

**Returns:**

filter used to compute the dependencies



---

<a id="m-NDependencies-immediate"></a>

#### immediate

```java
NStream<NDependency> immediate()
```

return immediate dependencies (not including source ids)

**Returns:**

immediate dependencies



---

<a id="m-NDependencies-transitive"></a>

#### transitive

```java
NStream<NDependency> transitive()
```



---

<a id="m-NDependencies-transitiveWithSource"></a>

#### transitiveWithSource

```java
NStream<NDependency> transitiveWithSource()
```

transitive dependencies merged with source ids, which may constitute a full classpath

**Returns:**

transitive dependencies merged with source ids, which may constitute a full classpath



---

<a id="m-NDependencies-transitiveNodes"></a>

#### transitiveNodes

```java
NStream<NDependencyTreeNode> transitiveNodes()
```

return all or some of the transitive dependencies of the current Artifact as Tree result of the search command

**Returns:**

all or some of the transitive dependencies of the current Artifact as Tree result of the search command.



---

<a id="m-NDependencies-sourceNodes"></a>

#### sourceNodes

```java
NStream<NDependencyTreeNode> sourceNodes()
```

return source Nodes tree including sources and their immediate dependencies at each Node level

**Returns:**

source Nodes tree including sources and their immediate dependencies at each Node level



---

<a id="m-NDependencies-toList"></a>

#### toList

```java
List<NDependency> toList()
```

equivalent to transitive().toList()

**Returns:**

transitive (all including source) dependencies



---

<a id="cls-net-thevpc-nuts-artifact-NDependency"></a>

## NDependency

```java
public interface NDependency extends Serializable, NBlankable
```

NutsDependency is an **immutable** object that contains all information about a package's dependency.

**Author:** thevpc

**Since:** 0.5.3

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;NDependency&gt;` | [get(String)](#m-NDependency-get) | Returns the get. |
| `static NOptional&lt;NDependency&gt;` | [get(NId)](#m-NDependency-get) | Returns the get. |
| `static NDependency` | [of(String)](#m-NDependency-of) | Creates a new instance. |
| `static NDependency` | [of(NId)](#m-NDependency-of) | Creates a new instance. |
| `NDependencyBuilder` | [builder()](#m-NDependency-builder) | return mutable id builder instance initialized with this instance. |
| `boolean` | [isOptional()](#m-NDependency-isOptional) | true if this dependency is optional. |
| `boolean` | [isAnyProvided()](#m-NDependency-isAnyProvided) | Checks if this is any. |
| `boolean` | [isAnyRuntime()](#m-NDependency-isAnyRuntime) | Checks if this is any. |
| `boolean` | [isProvided()](#m-NDependency-isProvided) | Checks if this is provided. |
| `boolean` | [isRuntime()](#m-NDependency-isRuntime) | Checks if this is runtime. |
| `boolean` | [isAnyTest()](#m-NDependency-isAnyTest) | Checks if this is any. |
| `String` | [optional()](#m-NDependency-optional) | Indicates the dependency is optional for use of this library. |
| `String` | [scope()](#m-NDependency-scope) | get scope string value (may be $ var). |
| `String` | [classifier()](#m-NDependency-classifier) | get classifier string value (may be $ var) |
| `NId` | [toId()](#m-NDependency-toId) | convert to NutsId |
| `String` | [repository()](#m-NDependency-repository) | return repository |
| `String` | [groupId()](#m-NDependency-groupId) | return artifact group id (aka groupId in maven) |
| `String` | [artifactId()](#m-NDependency-artifactId) | return artifact id (aka artifactId) |
| `String` | [shortName()](#m-NDependency-shortName) | return dependency full name in the form group:name |
| `String` | [longName()](#m-NDependency-longName) | return dependency full name in the form group:name#version |
| `String` | [fullName()](#m-NDependency-fullName) | return dependency full name in the form group:name#version?scope=&lt;scope&gt;&optional=&lt;optional&gt; |
| `NVersion` | [version()](#m-NDependency-version) | return dependency version |
| `NEnvCondition` | [condition()](#m-NDependency-condition) | Condition. |
| `String` | [type()](#m-NDependency-type) | Type. |
| `List&lt;NId&gt;` | [exclusions()](#m-NDependency-exclusions) | dependency exclusions |
| `String` | [propertiesQuery()](#m-NDependency-propertiesQuery) | properties in the URL query form |
| `Map&lt;String,String&gt;` | [properties()](#m-NDependency-properties) | properties in the URL query form |

### Method Details


---

<a id="m-NDependency-get"></a>

#### get

```java
static NOptional<NDependency> get(String value)
```

Returns the get.

**Parameters:**

- `value` - value

**Returns:**

get result



---

<a id="m-NDependency-get"></a>

#### get

```java
static NOptional<NDependency> get(NId value)
```

Returns the get.

**Parameters:**

- `value` - value

**Returns:**

get result



---

<a id="m-NDependency-of"></a>

#### of

```java
static NDependency of(String value)
```

Creates a new instance.

**Parameters:**

- `value` - value

**Returns:**

of result



---

<a id="m-NDependency-of"></a>

#### of

```java
static NDependency of(NId value)
```

Creates a new instance.

**Parameters:**

- `value` - value

**Returns:**

of result



---

<a id="m-NDependency-builder"></a>

#### builder

```java
NDependencyBuilder builder()
```

return mutable id builder instance initialized with `this` instance.

**Returns:**

mutable id builder instance initialized with `this` instance



---

<a id="m-NDependency-isOptional"></a>

#### isOptional

```java
boolean isOptional()
```

true if this dependency is optional.
equivalent to `Boolean.parseBoolean(getOptional())`

**Returns:**

true if this dependency is optional.



---

<a id="m-NDependency-isAnyProvided"></a>

#### isAnyProvided

```java
boolean isAnyProvided()
```

Checks if this is any. provided.

**Returns:**

is any provided result



---

<a id="m-NDependency-isAnyRuntime"></a>

#### isAnyRuntime

```java
boolean isAnyRuntime()
```

Checks if this is any. runtime.

**Returns:**

is any runtime result



---

<a id="m-NDependency-isProvided"></a>

#### isProvided

```java
boolean isProvided()
```

Checks if this is provided.

**Returns:**

is provided result



---

<a id="m-NDependency-isRuntime"></a>

#### isRuntime

```java
boolean isRuntime()
```

Checks if this is runtime.

**Returns:**

is runtime result



---

<a id="m-NDependency-isAnyTest"></a>

#### isAnyTest

```java
boolean isAnyTest()
```

Checks if this is any. test.

**Returns:**

is any test result



---

<a id="m-NDependency-optional"></a>

#### optional

```java
@NGetter
String optional()
```

**Accessor:** Property getter.

Indicates the dependency is optional for use of this library.

**Returns:**

string representation (or $ var) that can be evaluated as 'true'



---

<a id="m-NDependency-scope"></a>

#### scope

```java
@NGetter
String scope()
```

**Accessor:** Property getter.

get scope string value (may be $ var).

**Returns:**

scope string value (may be $ var)



---

<a id="m-NDependency-classifier"></a>

#### classifier

```java
@NGetter
String classifier()
```

**Accessor:** Property getter.

get classifier string value (may be $ var)

**Returns:**

classifier string



---

<a id="m-NDependency-toId"></a>

#### toId

```java
NId toId()
```

convert to NutsId

**Returns:**

converted to NutsId



---

<a id="m-NDependency-repository"></a>

#### repository

```java
@NGetter
String repository()
```

**Accessor:** Property getter.

return repository

**Returns:**

repository



---

<a id="m-NDependency-groupId"></a>

#### groupId

```java
@NGetter
String groupId()
```

**Accessor:** Property getter.

return artifact group id (aka groupId in maven)

**Returns:**

artifact group id (aka groupId in maven)



---

<a id="m-NDependency-artifactId"></a>

#### artifactId

```java
@NGetter
String artifactId()
```

**Accessor:** Property getter.

return artifact id (aka artifactId)

**Returns:**

artifact id (aka artifactId in maven)



---

<a id="m-NDependency-shortName"></a>

#### shortName

```java
@NGetter
String shortName()
```

**Accessor:** Property getter.

return dependency full name in the form
group:name

**Returns:**

return dependency short name



---

<a id="m-NDependency-longName"></a>

#### longName

```java
@NGetter
String longName()
```

**Accessor:** Property getter.

return dependency full name in the form
group:name#version

**Returns:**

return dependency long name



---

<a id="m-NDependency-fullName"></a>

#### fullName

```java
@NGetter
String fullName()
```

**Accessor:** Property getter.

return dependency full name in the form
group:name#version?scope=&lt;scope&gt;`&`optional=&lt;optional&gt;

**Returns:**

return dependency full name



---

<a id="m-NDependency-version"></a>

#### version

```java
@NGetter
NVersion version()
```

**Accessor:** Property getter.

return dependency version

**Returns:**

return dependency version



---

<a id="m-NDependency-condition"></a>

#### condition

```java
@NGetter
NEnvCondition condition()
```

**Accessor:** Property getter.

Condition.

**Returns:**

condition result



---

<a id="m-NDependency-type"></a>

#### type

```java
@NGetter
String type()
```

**Accessor:** Property getter.

Type.

**Returns:**

type result



---

<a id="m-NDependency-exclusions"></a>

#### exclusions

```java
@NGetter
List<NId> exclusions()
```

**Accessor:** Property getter.

dependency exclusions

**Returns:**

dependency exclusions



---

<a id="m-NDependency-propertiesQuery"></a>

#### propertiesQuery

```java
@NGetter
String propertiesQuery()
```

**Accessor:** Property getter.

properties in the URL query form

**Returns:**

properties in the URL query form.



---

<a id="m-NDependency-properties"></a>

#### properties

```java
Map<String,String> properties()
```

properties in the URL query form

**Returns:**

properties in the URL query form.



---

<a id="cls-net-thevpc-nuts-artifact-NDependencyBuilder"></a>

## NDependencyBuilder

```java
public interface NDependencyBuilder extends NComponent, Serializable, NBlankable
```

Dependency Builder (mutable).
User should use available 'set' method and finally call [#build()](#m-build)
to get an instance of immutable NutsDependency

**Author:** thevpc

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NDependencyBuilder` | [of(String, String)](#m-NDependencyBuilder-of) | Creates a new instance. |
| `static NDependencyBuilder` | [of()](#m-NDependencyBuilder-of) | Creates a new instance. |
| `NDependencyBuilder` | [id(NId)](#m-NDependencyBuilder-id) | set id value |
| `NDependencyBuilder` | [dependency(NDependencyBuilder)](#m-NDependencyBuilder-dependency) | reset this instance with value |
| `NDependencyBuilder` | [copyFrom(NDependencyBuilder)](#m-NDependencyBuilder-copyFrom) | reset this instance with value |
| `NDependencyBuilder` | [copyFrom(NDependency)](#m-NDependencyBuilder-copyFrom) | reset this instance with value |
| `NDependencyBuilder` | [dependency(NDependency)](#m-NDependencyBuilder-dependency) | reset this instance with value |
| `NDependencyBuilder` | [clear()](#m-NDependencyBuilder-clear) | reset this instance |
| `NDependencyBuilder` | [type(String)](#m-NDependencyBuilder-type) | set type value |
| `NDependencyBuilder` | [optional(String)](#m-NDependencyBuilder-optional) | set optional value |
| `NDependencyBuilder` | [scope(NDependencyScope)](#m-NDependencyBuilder-scope) | set scope value |
| `NDependencyBuilder` | [scope(String)](#m-NDependencyBuilder-scope) | set scope value |
| `NDependencyBuilder` | [repository(String)](#m-NDependencyBuilder-repository) | set repository value |
| `NDependencyBuilder` | [groupId(String)](#m-NDependencyBuilder-groupId) | set group value |
| `NDependencyBuilder` | [artifactId(String)](#m-NDependencyBuilder-artifactId) | set name value |
| `NDependencyBuilder` | [classifier(String)](#m-NDependencyBuilder-classifier) | set classifier value |
| `NDependencyBuilder` | [version(NVersion)](#m-NDependencyBuilder-version) | set version value |
| `NDependencyBuilder` | [version(String)](#m-NDependencyBuilder-version) | set version value |
| `NDependencyBuilder` | [exclusions(List&lt;NId&gt;)](#m-NDependencyBuilder-exclusions) | set exclusions value |
| `NDependency` | [build()](#m-NDependencyBuilder-build) | build new instance of NutsDependencies |
| `NDependencyBuilder` | [property(String, String)](#m-NDependencyBuilder-property) | Property. |
| `NDependencyBuilder` | [propertiesQuery(String)](#m-NDependencyBuilder-propertiesQuery) | Properties query. |
| `NDependencyBuilder` | [properties(Map&lt;String,String&gt;)](#m-NDependencyBuilder-properties) | Properties. |
| `NDependencyBuilder` | [addPropertiesQuery(String)](#m-NDependencyBuilder-addPropertiesQuery) | Adds the specified properties query. |
| `NDependencyBuilder` | [addProperties(Map&lt;String,String&gt;)](#m-NDependencyBuilder-addProperties) | Adds the specified properties. |
| `NDependencyBuilder` | [condition(NEnvCondition)](#m-NDependencyBuilder-condition) | set condition |
| `NDependencyBuilder` | [condition(NEnvConditionBuilder)](#m-NDependencyBuilder-condition) | Condition. |
| `NDependencyBuilder` | [removeCondition()](#m-NDependencyBuilder-removeCondition) | Removes the specified condition. |
| `NDependencyBuilder` | [copy()](#m-NDependencyBuilder-copy) | Copy. |
| `boolean` | [isOptional()](#m-NDependencyBuilder-isOptional) | true if this dependency is optional. |
| `String` | [optional()](#m-NDependencyBuilder-optional) | Indicates the dependency is optional for use of this library. |
| `String` | [scope()](#m-NDependencyBuilder-scope) | get scope string value (may be $ var). |
| `String` | [classifier()](#m-NDependencyBuilder-classifier) | get classifier string value (may be $ var) |
| `NId` | [toId()](#m-NDependencyBuilder-toId) | convert to NutsId |
| `String` | [repository()](#m-NDependencyBuilder-repository) | return repository |
| `String` | [groupId()](#m-NDependencyBuilder-groupId) | return artifact group id (aka groupId in maven) |
| `String` | [artifactId()](#m-NDependencyBuilder-artifactId) | return artifact id (aka artifactId) |
| `String` | [shortName()](#m-NDependencyBuilder-shortName) | return dependency full name in the form group:name |
| `String` | [longName()](#m-NDependencyBuilder-longName) | return dependency full name in the form group:name#version |
| `String` | [fullName()](#m-NDependencyBuilder-fullName) | return dependency full name in the form group:name#version?scope=&lt;scope&gt;&optional=&lt;optional&gt; |
| `NVersion` | [version()](#m-NDependencyBuilder-version) | return dependency version |
| `NEnvConditionBuilder` | [condition()](#m-NDependencyBuilder-condition) | Condition. |
| `String` | [type()](#m-NDependencyBuilder-type) | Type. |
| `List&lt;NId&gt;` | [exclusions()](#m-NDependencyBuilder-exclusions) | dependency exclusions |
| `String` | [propertiesQuery()](#m-NDependencyBuilder-propertiesQuery) | properties in the URL query form |
| `Map&lt;String,String&gt;` | [properties()](#m-NDependencyBuilder-properties) | properties in the URL query form |

### Method Details


---

<a id="m-NDependencyBuilder-of"></a>

#### of

```java
static NDependencyBuilder of(String groupId, String artifactId)
```

Creates a new instance.

**Parameters:**

- `groupId` - group id
- `artifactId` - artifact id

**Returns:**

of result



---

<a id="m-NDependencyBuilder-of"></a>

#### of

```java
static NDependencyBuilder of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NDependencyBuilder-id"></a>

#### id

```java
@NSetter
NDependencyBuilder id(NId id)
```

**Mutator:** Property setter.

set id value

**Parameters:**

- `id` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-dependency"></a>

#### dependency

```java
NDependencyBuilder dependency(NDependencyBuilder value)
```

reset this instance with value

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-copyFrom"></a>

#### copyFrom

```java
NDependencyBuilder copyFrom(NDependencyBuilder value)
```

reset this instance with value

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-copyFrom"></a>

#### copyFrom

```java
NDependencyBuilder copyFrom(NDependency value)
```

reset this instance with value

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-dependency"></a>

#### dependency

```java
@NSetter
NDependencyBuilder dependency(NDependency value)
```

**Mutator:** Property setter.

reset this instance with value

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-clear"></a>

#### clear

```java
NDependencyBuilder clear()
```

reset this instance

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-type"></a>

#### type

```java
@NSetter
NDependencyBuilder type(String type)
```

**Mutator:** Property setter.

set type value

**Parameters:**

- `type` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-optional"></a>

#### optional

```java
NDependencyBuilder optional(String optional)
```

set optional value

**Parameters:**

- `optional` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-scope"></a>

#### scope

```java
@NSetter
NDependencyBuilder scope(NDependencyScope scope)
```

**Mutator:** Property setter.

set scope value

**Parameters:**

- `scope` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-scope"></a>

#### scope

```java
NDependencyBuilder scope(String scope)
```

set scope value

**Parameters:**

- `scope` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-repository"></a>

#### repository

```java
@NSetter
NDependencyBuilder repository(String repository)
```

**Mutator:** Property setter.

set repository value

**Parameters:**

- `repository` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-groupId"></a>

#### groupId

```java
@NSetter
NDependencyBuilder groupId(String groupId)
```

**Mutator:** Property setter.

set group value

**Parameters:**

- `groupId` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-artifactId"></a>

#### artifactId

```java
@NSetter
NDependencyBuilder artifactId(String artifactId)
```

**Mutator:** Property setter.

set name value

**Parameters:**

- `artifactId` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-classifier"></a>

#### classifier

```java
@NSetter
NDependencyBuilder classifier(String classifier)
```

**Mutator:** Property setter.

set classifier value

**Parameters:**

- `classifier` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-version"></a>

#### version

```java
@NSetter
NDependencyBuilder version(NVersion version)
```

**Mutator:** Property setter.

set version value

**Parameters:**

- `version` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-version"></a>

#### version

```java
NDependencyBuilder version(String version)
```

set version value

**Parameters:**

- `version` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-exclusions"></a>

#### exclusions

```java
NDependencyBuilder exclusions(List<NId> exclusions)
```

set exclusions value

**Parameters:**

- `exclusions` - new value

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-build"></a>

#### build

```java
NDependency build()
```

build new instance of NutsDependencies

**Returns:**

new instance of NutsDependencies



---

<a id="m-NDependencyBuilder-property"></a>

#### property

```java
NDependencyBuilder property(String property, String value)
```

Property.

**Parameters:**

- `property` - property
- `value` - value

**Returns:**

property result



---

<a id="m-NDependencyBuilder-propertiesQuery"></a>

#### propertiesQuery

```java
NDependencyBuilder propertiesQuery(String propertiesQuery)
```

Properties query.

**Parameters:**

- `propertiesQuery` - properties query

**Returns:**

properties query result



---

<a id="m-NDependencyBuilder-properties"></a>

#### properties

```java
NDependencyBuilder properties(Map<String,String> queryMap)
```

Properties.

**Parameters:**

- `queryMap` - query map

**Returns:**

properties result



---

<a id="m-NDependencyBuilder-addPropertiesQuery"></a>

#### addPropertiesQuery

```java
NDependencyBuilder addPropertiesQuery(String propertiesQuery)
```

Adds the specified properties query.

**Parameters:**

- `propertiesQuery` - properties query

**Returns:**

add properties query result



---

<a id="m-NDependencyBuilder-addProperties"></a>

#### addProperties

```java
NDependencyBuilder addProperties(Map<String,String> queryMap)
```

Adds the specified properties.

**Parameters:**

- `queryMap` - query map

**Returns:**

add properties result



---

<a id="m-NDependencyBuilder-condition"></a>

#### condition

```java
NDependencyBuilder condition(NEnvCondition condition)
```

set condition

**Parameters:**

- `condition` - condition

**Returns:**

`this` instance



---

<a id="m-NDependencyBuilder-condition"></a>

#### condition

```java
NDependencyBuilder condition(NEnvConditionBuilder condition)
```

Condition.

**Parameters:**

- `condition` - condition

**Returns:**

condition result



---

<a id="m-NDependencyBuilder-removeCondition"></a>

#### removeCondition

```java
NDependencyBuilder removeCondition()
```

Removes the specified condition.

**Returns:**

remove condition result



---

<a id="m-NDependencyBuilder-copy"></a>

#### copy

```java
NDependencyBuilder copy()
```

Copy.

**Returns:**

copy result



---

<a id="m-NDependencyBuilder-isOptional"></a>

#### isOptional

```java
boolean isOptional()
```

true if this dependency is optional.
equivalent to `Boolean.parseBoolean(getOptional())`

**Returns:**

true if this dependency is optional.



---

<a id="m-NDependencyBuilder-optional"></a>

#### optional

```java
@NGetter
String optional()
```

**Accessor:** Property getter.

Indicates the dependency is optional for use of this library.

**Returns:**

string representation (or $ var) that can be evaluated as 'true'



---

<a id="m-NDependencyBuilder-scope"></a>

#### scope

```java
String scope()
```

get scope string value (may be $ var).

**Returns:**

scope string value (may be $ var)



---

<a id="m-NDependencyBuilder-classifier"></a>

#### classifier

```java
@NGetter
String classifier()
```

**Accessor:** Property getter.

get classifier string value (may be $ var)

**Returns:**

classifier string



---

<a id="m-NDependencyBuilder-toId"></a>

#### toId

```java
NId toId()
```

convert to NutsId

**Returns:**

converted to NutsId



---

<a id="m-NDependencyBuilder-repository"></a>

#### repository

```java
@NGetter
String repository()
```

**Accessor:** Property getter.

return repository

**Returns:**

repository



---

<a id="m-NDependencyBuilder-groupId"></a>

#### groupId

```java
@NGetter
String groupId()
```

**Accessor:** Property getter.

return artifact group id (aka groupId in maven)

**Returns:**

artifact group id (aka groupId in maven)



---

<a id="m-NDependencyBuilder-artifactId"></a>

#### artifactId

```java
@NGetter
String artifactId()
```

**Accessor:** Property getter.

return artifact id (aka artifactId)

**Returns:**

artifact id (aka artifactId in maven)



---

<a id="m-NDependencyBuilder-shortName"></a>

#### shortName

```java
@NGetter
String shortName()
```

**Accessor:** Property getter.

return dependency full name in the form
group:name

**Returns:**

return dependency short name



---

<a id="m-NDependencyBuilder-longName"></a>

#### longName

```java
@NGetter
String longName()
```

**Accessor:** Property getter.

return dependency full name in the form
group:name#version

**Returns:**

return dependency long name



---

<a id="m-NDependencyBuilder-fullName"></a>

#### fullName

```java
@NGetter
String fullName()
```

**Accessor:** Property getter.

return dependency full name in the form
group:name#version?scope=&lt;scope&gt;`&`optional=&lt;optional&gt;

**Returns:**

return dependency full name



---

<a id="m-NDependencyBuilder-version"></a>

#### version

```java
@NGetter
NVersion version()
```

**Accessor:** Property getter.

return dependency version

**Returns:**

return dependency version



---

<a id="m-NDependencyBuilder-condition"></a>

#### condition

```java
@NGetter
NEnvConditionBuilder condition()
```

**Accessor:** Property getter.

Condition.

**Returns:**

condition result



---

<a id="m-NDependencyBuilder-type"></a>

#### type

```java
@NGetter
String type()
```

**Accessor:** Property getter.

Type.

**Returns:**

type result



---

<a id="m-NDependencyBuilder-exclusions"></a>

#### exclusions

```java
@NGetter
List<NId> exclusions()
```

**Accessor:** Property getter.

dependency exclusions

**Returns:**

dependency exclusions



---

<a id="m-NDependencyBuilder-propertiesQuery"></a>

#### propertiesQuery

```java
@NGetter
String propertiesQuery()
```

**Accessor:** Property getter.

properties in the URL query form

**Returns:**

properties in the URL query form.



---

<a id="m-NDependencyBuilder-properties"></a>

#### properties

```java
@NGetter
Map<String,String> properties()
```

**Accessor:** Property getter.

properties in the URL query form

**Returns:**

properties in the URL query form.



---

<a id="cls-net-thevpc-nuts-artifact-NDependencyFilter"></a>

## NDependencyFilter

```java
public interface NDependencyFilter extends NFilter
```

Dependency filter

**Author:** thevpc

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NDependencyFilter` | [ofNonnull(NFilter)](#m-NDependencyFilter-ofNonnull) | Creates a new instance of nonnull. |
| `static NDependencyFilter` | [ofAlways()](#m-NDependencyFilter-ofAlways) | Creates a new instance of always. |
| `static NDependencyFilter` | [ofNever()](#m-NDependencyFilter-ofNever) | Creates a new instance of never. |
| `static NDependencyFilter` | [ofAll(NFilter)](#m-NDependencyFilter-ofAll) | Creates a new instance of all. |
| `static NDependencyFilter` | [ofAny(NFilter)](#m-NDependencyFilter-ofAny) | Creates a new instance of any. |
| `static NDependencyFilter` | [ofNot(NFilter)](#m-NDependencyFilter-ofNot) | Creates a new instance of not. |
| `static NDependencyFilter` | [ofNone(NFilter)](#m-NDependencyFilter-ofNone) | Creates a new instance of none. |
| `static NDependencyFilter` | [ofFrom(NFilter)](#m-NDependencyFilter-ofFrom) | Creates a new instance of from. |
| `static NDependencyFilter` | [ofAs(NFilter)](#m-NDependencyFilter-ofAs) | Creates a new instance of as. |
| `static NDependencyFilter` | [of(String)](#m-NDependencyFilter-of) | Creates a new instance. |
| `static NDependencyFilter` | [ofScope(NDependencyScopePattern)](#m-NDependencyFilter-ofScope) | accept only dependencies that match the given scope |
| `static NDependencyFilter` | [ofScope(NDependencyScope)](#m-NDependencyFilter-ofScope) | accept only dependencies that match the given scope |
| `static NDependencyFilter` | [ofScope(NDependencyScope)](#m-NDependencyFilter-ofScope) | accept only dependencies that match the given scopes |
| `static NDependencyFilter` | [ofScope(Collection&lt;NDependencyScope&gt;)](#m-NDependencyFilter-ofScope) | accept only dependencies that match the given scopes |
| `static NDependencyFilter` | [ofOptional(Boolean)](#m-NDependencyFilter-ofOptional) | accept only dependencies that match the given optional state |
| `static NDependencyFilter` | [ofExclude(NDependencyFilter, String[])](#m-NDependencyFilter-ofExclude) | return a new filter that accepts all of filter but the given exclusions |
| `static NDependencyFilter` | [ofArch(Collection&lt;NArchFamily&gt;)](#m-NDependencyFilter-ofArch) | accept only dependencies that match the given archs |
| `static NDependencyFilter` | [ofArch(NArchFamily)](#m-NDependencyFilter-ofArch) | accept only dependencies that match the given arch |
| `static NDependencyFilter` | [ofArch(NArchFamily)](#m-NDependencyFilter-ofArch) | accept only dependencies that match the given archs |
| `static NDependencyFilter` | [ofArch(String)](#m-NDependencyFilter-ofArch) | accept only dependencies that match the given arch |
| `static NDependencyFilter` | [ofOs(Collection&lt;NOsFamily&gt;)](#m-NDependencyFilter-ofOs) | accept only dependencies that match the given OSes |
| `static NDependencyFilter` | [ofOs(String)](#m-NDependencyFilter-ofOs) | accept only dependencies that match the given OS |
| `static NDependencyFilter` | [ofOsDist(String)](#m-NDependencyFilter-ofOsDist) | accept only dependencies that match the given OsDist |
| `static NDependencyFilter` | [ofOsDist(String)](#m-NDependencyFilter-ofOsDist) | accept only dependencies that match the given OsDist list |
| `static NDependencyFilter` | [ofOsDist(Collection&lt;String&gt;)](#m-NDependencyFilter-ofOsDist) | accept only dependencies that match the given OsDist list |
| `static NDependencyFilter` | [ofOs(NOsFamily)](#m-NDependencyFilter-ofOs) | accept only dependencies that match the given OS |
| `static NDependencyFilter` | [ofOs(NOsFamily)](#m-NDependencyFilter-ofOs) | accept only dependencies that match the given OSes |
| `static NDependencyFilter` | [ofCurrentDesktop()](#m-NDependencyFilter-ofCurrentDesktop) | accept only dependencies that match the current Desktop Environment |
| `static NDependencyFilter` | [ofCurrentArch()](#m-NDependencyFilter-ofCurrentArch) | accept only dependencies that match the current Architecture |
| `static NDependencyFilter` | [ofCurrentOs()](#m-NDependencyFilter-ofCurrentOs) | accept only dependencies that match the current OS |
| `static NDependencyFilter` | [ofRegularType()](#m-NDependencyFilter-ofRegularType) | accept only dependencies that have a regular dependency type (such as "jar" in java) |
| `static NDependencyFilter` | [ofCurrentEnv()](#m-NDependencyFilter-ofCurrentEnv) | accept only dependencies that match the current environment (OS, arch, etc...) |
| `static NDependencyFilter` | [ofRunnable()](#m-NDependencyFilter-ofRunnable) | create filter that accepts only dependencies required for runtime execution. |
| `static NDependencyFilter` | [ofRunnable(boolean)](#m-NDependencyFilter-ofRunnable) | create filter that accepts only dependencies required for runtime execution. |
| `static NDependencyFilter` | [ofRunnable(boolean, boolean)](#m-NDependencyFilter-ofRunnable) | Creates a new instance of runnable. |
| `static NDependencyFilter` | [ofDesktop(NDesktopEnvironmentFamily)](#m-NDependencyFilter-ofDesktop) | accept only dependencies that match the given Desktop Environment |
| `static NDependencyFilter` | [ofDesktop(NDesktopEnvironmentFamily)](#m-NDependencyFilter-ofDesktop) | accept only dependencies that match any of the given Desktop Environment |
| `static NDependencyFilter` | [ofDesktop(Collection&lt;NDesktopEnvironmentFamily&gt;)](#m-NDependencyFilter-ofDesktop) | Creates a new instance of desktop. |
| `static NDependencyFilter` | [ofPlatform(NRuntimeDistributionFamily)](#m-NDependencyFilter-ofPlatform) | accept only dependencies that match any of the given Platform |
| `static NDependencyFilter` | [ofPlatform(String)](#m-NDependencyFilter-ofPlatform) | accept only dependencies that match any of the given Platform |
| `static NDependencyFilter` | [ofType(String)](#m-NDependencyFilter-ofType) | accept only dependencies that match the given type |
| `boolean` | [acceptDependency(NDependency, NId)](#m-NDependencyFilter-acceptDependency) | return true if the dependency is accepted |
| `NDependencyFilter` | [or(NDependencyFilter)](#m-NDependencyFilter-or) | Or. |
| `NDependencyFilter` | [and(NDependencyFilter)](#m-NDependencyFilter-and) | And. |
| `NDependencyFilter` | [neg()](#m-NDependencyFilter-neg) | Neg. |

### Method Details


---

<a id="m-NDependencyFilter-ofNonnull"></a>

#### ofNonnull

```java
static NDependencyFilter ofNonnull(NFilter filter)
```

Creates a new instance of nonnull.

**Parameters:**

- `filter` - filter

**Returns:**

of nonnull result



---

<a id="m-NDependencyFilter-ofAlways"></a>

#### ofAlways

```java
static NDependencyFilter ofAlways()
```

Creates a new instance of always.

**Returns:**

of always result



---

<a id="m-NDependencyFilter-ofNever"></a>

#### ofNever

```java
static NDependencyFilter ofNever()
```

Creates a new instance of never.

**Returns:**

of never result



---

<a id="m-NDependencyFilter-ofAll"></a>

#### ofAll

```java
static NDependencyFilter ofAll(NFilter others)
```

Creates a new instance of all.

**Parameters:**

- `others` - others

**Returns:**

of all result



---

<a id="m-NDependencyFilter-ofAny"></a>

#### ofAny

```java
static NDependencyFilter ofAny(NFilter others)
```

Creates a new instance of any.

**Parameters:**

- `others` - others

**Returns:**

of any result



---

<a id="m-NDependencyFilter-ofNot"></a>

#### ofNot

```java
static NDependencyFilter ofNot(NFilter other)
```

Creates a new instance of not.

**Parameters:**

- `other` - other

**Returns:**

of not result



---

<a id="m-NDependencyFilter-ofNone"></a>

#### ofNone

```java
static NDependencyFilter ofNone(NFilter others)
```

Creates a new instance of none.

**Parameters:**

- `others` - others

**Returns:**

of none result



---

<a id="m-NDependencyFilter-ofFrom"></a>

#### ofFrom

```java
static NDependencyFilter ofFrom(NFilter a)
```

Creates a new instance of from.

**Parameters:**

- `a` - a

**Returns:**

of from result



---

<a id="m-NDependencyFilter-ofAs"></a>

#### ofAs

```java
static NDependencyFilter ofAs(NFilter a)
```

Creates a new instance of as.

**Parameters:**

- `a` - a

**Returns:**

of as result



---

<a id="m-NDependencyFilter-of"></a>

#### of

```java
static NDependencyFilter of(String expression)
```

Creates a new instance.

**Parameters:**

- `expression` - expression

**Returns:**

of result



---

<a id="m-NDependencyFilter-ofScope"></a>

#### ofScope

```java
static NDependencyFilter ofScope(NDependencyScopePattern scopes)
```

accept only dependencies that match the given scope

**Parameters:**

- `scopes` - accepted scope

**Returns:**

a filter that accepts only dependencies that match the given scope



---

<a id="m-NDependencyFilter-ofScope"></a>

#### ofScope

```java
static NDependencyFilter ofScope(NDependencyScope scope)
```

accept only dependencies that match the given scope

**Parameters:**

- `scope` - accepted scope

**Returns:**

a filter that accepts only dependencies that match the given scope



---

<a id="m-NDependencyFilter-ofScope"></a>

#### ofScope

```java
static NDependencyFilter ofScope(NDependencyScope scopes)
```

accept only dependencies that match the given scopes

**Parameters:**

- `scopes` - accepted scopes list

**Returns:**

a filter that accepts only dependencies that match the given scopes



---

<a id="m-NDependencyFilter-ofScope"></a>

#### ofScope

```java
static NDependencyFilter ofScope(Collection<NDependencyScope> scopes)
```

accept only dependencies that match the given scopes

**Parameters:**

- `scopes` - accepted scope list

**Returns:**

a filter that accepts only dependencies that match the given scopes



---

<a id="m-NDependencyFilter-ofOptional"></a>

#### ofOptional

```java
static NDependencyFilter ofOptional(Boolean optional)
```

accept only dependencies that match the given optional state

**Parameters:**

- `optional` - accepted scope state. null matches any optional value

**Returns:**

a filter that accepts only dependencies that match the given scope



---

<a id="m-NDependencyFilter-ofExclude"></a>

#### ofExclude

```java
static NDependencyFilter ofExclude(NDependencyFilter filter, String[] exclusions)
```

return a new filter that accepts all of `filter` but the given exclusions

**Parameters:**

- `filter` - base filter
- `exclusions` - excluded dependencies

**Returns:**

return a new filter that accepts all of `filter` but the given exclusions



---

<a id="m-NDependencyFilter-ofArch"></a>

#### ofArch

```java
static NDependencyFilter ofArch(Collection<NArchFamily> archs)
```

accept only dependencies that match the given archs

**Parameters:**

- `archs` - accepted archs list

**Returns:**

a filter that accepts only dependencies that match the given archs



---

<a id="m-NDependencyFilter-ofArch"></a>

#### ofArch

```java
static NDependencyFilter ofArch(NArchFamily arch)
```

accept only dependencies that match the given arch

**Parameters:**

- `arch` - accepted arch

**Returns:**

a filter that accepts only dependencies that match the given arch



---

<a id="m-NDependencyFilter-ofArch"></a>

#### ofArch

```java
static NDependencyFilter ofArch(NArchFamily archs)
```

accept only dependencies that match the given archs

**Parameters:**

- `archs` - accepted arch list

**Returns:**

a filter that accepts only dependencies that match the given archs



---

<a id="m-NDependencyFilter-ofArch"></a>

#### ofArch

```java
static NDependencyFilter ofArch(String arch)
```

accept only dependencies that match the given arch

**Parameters:**

- `arch` - accepted arch

**Returns:**

a filter that accepts only dependencies that match the given arch



---

<a id="m-NDependencyFilter-ofOs"></a>

#### ofOs

```java
static NDependencyFilter ofOs(Collection<NOsFamily> os)
```

accept only dependencies that match the given OSes

**Parameters:**

- `os` - accepted OS list

**Returns:**

a filter that accepts only dependencies that match the given OSes



---

<a id="m-NDependencyFilter-ofOs"></a>

#### ofOs

```java
static NDependencyFilter ofOs(String os)
```

accept only dependencies that match the given OS

**Parameters:**

- `os` - accepted OS

**Returns:**

a filter that accepts only dependencies that match the given OS



---

<a id="m-NDependencyFilter-ofOsDist"></a>

#### ofOsDist

```java
static NDependencyFilter ofOsDist(String osDist)
```

accept only dependencies that match the given OsDist

**Parameters:**

- `osDist` - accepted OsDist

**Returns:**

a filter that accepts only dependencies that match the given OsDist



---

<a id="m-NDependencyFilter-ofOsDist"></a>

#### ofOsDist

```java
static NDependencyFilter ofOsDist(String osDists)
```

accept only dependencies that match the given OsDist list

**Parameters:**

- `osDists` - accepted OsDist list

**Returns:**

a filter that accepts only dependencies that match the given OsDist list



---

<a id="m-NDependencyFilter-ofOsDist"></a>

#### ofOsDist

```java
static NDependencyFilter ofOsDist(Collection<String> osDists)
```

accept only dependencies that match the given OsDist list

**Parameters:**

- `osDists` - accepted OsDist list

**Returns:**

a filter that accepts only dependencies that match the given OsDist list



---

<a id="m-NDependencyFilter-ofOs"></a>

#### ofOs

```java
static NDependencyFilter ofOs(NOsFamily os)
```

accept only dependencies that match the given OS

**Parameters:**

- `os` - accepted OS

**Returns:**

a filter that accepts only dependencies that match the given OS



---

<a id="m-NDependencyFilter-ofOs"></a>

#### ofOs

```java
static NDependencyFilter ofOs(NOsFamily os)
```

accept only dependencies that match the given OSes

**Parameters:**

- `os` - accepted OS list

**Returns:**

a filter that accepts only dependencies that match the given OSes



---

<a id="m-NDependencyFilter-ofCurrentDesktop"></a>

#### ofCurrentDesktop

```java
static NDependencyFilter ofCurrentDesktop()
```

accept only dependencies that match the current Desktop Environment

**Returns:**

a filter that accepts only dependencies that match the current Desktop Environment



---

<a id="m-NDependencyFilter-ofCurrentArch"></a>

#### ofCurrentArch

```java
static NDependencyFilter ofCurrentArch()
```

accept only dependencies that match the current Architecture

**Returns:**

a filter that accepts only dependencies that match the current Architecture



---

<a id="m-NDependencyFilter-ofCurrentOs"></a>

#### ofCurrentOs

```java
static NDependencyFilter ofCurrentOs()
```

accept only dependencies that match the current OS

**Returns:**

a filter that accepts only dependencies that match the current OS



---

<a id="m-NDependencyFilter-ofRegularType"></a>

#### ofRegularType

```java
static NDependencyFilter ofRegularType()
```

accept only dependencies that have a regular dependency type (such as "jar" in java)

**Returns:**

a filter that accepts only dependencies that have a regular dependency type (such as "jar" in java)



---

<a id="m-NDependencyFilter-ofCurrentEnv"></a>

#### ofCurrentEnv

```java
static NDependencyFilter ofCurrentEnv()
```

accept only dependencies that match the current environment (OS, arch, etc...)

**Returns:**

a filter that accept only dependencies that match the current environment (OS, arch, etc...)



---

<a id="m-NDependencyFilter-ofRunnable"></a>

#### ofRunnable

```java
static NDependencyFilter ofRunnable()
```

create filter that accepts only dependencies required for runtime execution.

equivalent to `ofRunnable(false)`

**Returns:**

filter that accepts only dependencies required for runtime execution



---

<a id="m-NDependencyFilter-ofRunnable"></a>

#### ofRunnable

```java
static NDependencyFilter ofRunnable(boolean optional)
```

create filter that accepts only dependencies required for runtime execution.

equivalent to ```java
ofScope(NutsDependencyScopePattern.RUN)
                .and(byOptional(optional?null:false))
                .and(byRegularType())
                .and(byCurrentEnv())
```

**Parameters:**

- `optional` - optional

**Returns:**

filter that accepts only dependencies required for runtime execution



---

<a id="m-NDependencyFilter-ofRunnable"></a>

#### ofRunnable

```java
static NDependencyFilter ofRunnable(boolean optional, boolean anyEnv)
```

Creates a new instance of runnable.

**Parameters:**

- `optional` - optional
- `anyEnv` - any env

**Returns:**

of runnable result



---

<a id="m-NDependencyFilter-ofDesktop"></a>

#### ofDesktop

```java
static NDependencyFilter ofDesktop(NDesktopEnvironmentFamily de)
```

accept only dependencies that match the given Desktop Environment

**Parameters:**

- `de` - accepted Desktop Environment

**Returns:**

a filter that accepts only dependencies that match the given Desktop Environment



---

<a id="m-NDependencyFilter-ofDesktop"></a>

#### ofDesktop

```java
static NDependencyFilter ofDesktop(NDesktopEnvironmentFamily de)
```

accept only dependencies that match any of the given Desktop Environment

**Parameters:**

- `de` - accepted Desktop Environment list

**Returns:**

a filter that accepts only dependencies that match any of the given Desktop Environment



---

<a id="m-NDependencyFilter-ofDesktop"></a>

#### ofDesktop

```java
static NDependencyFilter ofDesktop(Collection<NDesktopEnvironmentFamily> de)
```

Creates a new instance of desktop.

**Parameters:**

- `de` - de

**Returns:**

of desktop result



---

<a id="m-NDependencyFilter-ofPlatform"></a>

#### ofPlatform

```java
static NDependencyFilter ofPlatform(NRuntimeDistributionFamily values)
```

accept only dependencies that match any of the given Platform

**Parameters:**

- `values` - accepted Desktop Environment list

**Returns:**

a filter that accepts only dependencies that match any of the given Platform



---

<a id="m-NDependencyFilter-ofPlatform"></a>

#### ofPlatform

```java
static NDependencyFilter ofPlatform(String values)
```

accept only dependencies that match any of the given Platform

**Parameters:**

- `values` - accepted Desktop Environment list

**Returns:**

a filter that accepts only dependencies that match any of the given Platform



---

<a id="m-NDependencyFilter-ofType"></a>

#### ofType

```java
static NDependencyFilter ofType(String type)
```

accept only dependencies that match the given type

**Parameters:**

- `type` - accepted type

**Returns:**

a filter that accepts only dependencies that match the given type



---

<a id="m-NDependencyFilter-acceptDependency"></a>

#### acceptDependency

```java
boolean acceptDependency(NDependency dependency, NId from)
```

return true if the `dependency` is accepted

**Parameters:**

- `dependency` - dependency id
- `from` - parent (dependent) id

**Returns:**

true if the `dependency` is accepted



---

<a id="m-NDependencyFilter-or"></a>

#### or

```java
NDependencyFilter or(NDependencyFilter other)
```

Or.

**Parameters:**

- `other` - other

**Returns:**

or result



---

<a id="m-NDependencyFilter-and"></a>

#### and

```java
NDependencyFilter and(NDependencyFilter other)
```

And.

**Parameters:**

- `other` - other

**Returns:**

and result



---

<a id="m-NDependencyFilter-neg"></a>

#### neg

```java
NDependencyFilter neg()
```

Neg.

**Returns:**

neg result



---

<a id="cls-net-thevpc-nuts-artifact-NDependencyScope"></a>

## NDependencyScope

```java
public enum NDependencyScope implements NEnum
```

Supported dependency scope lists

**Author:** thevpc

**Since:** 0.5.4

### Enum Constants

| Constant | Description |
| :--- | :--- |
| `API` | equivalent to maven's compile and to gradle's api |
| `IMPLEMENTATION` | equivalent to gradle's implementation |
| `PROVIDED` | equivalent to maven's provided |
| `IMPORT` | equivalent to maven's import |
| `RUNTIME` | equivalent to maven's runtime |
| `SYSTEM` | equivalent to maven's system |
| `TEST_API` | equivalent to maven's test |
| `TEST_IMPLEMENTATION` | equivalent to maven's test |
| `TEST_PROVIDED` | dependencies needed for test but are provided by container. |
| `TEST_RUNTIME` | dependencies needed for test execution |
| `TEST_SYSTEM` | dependencies needed for test execution |
| `OTHER` | other |
| `TEST_OTHER` | dependencies needed for test execution |

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private final String` | `id` | lower-cased identifier for the enum entry |
| `private final boolean` | `api` |  |
| `private final boolean` | `implementation` |  |
| `private final boolean` | `test` |  |
| `private final boolean` | `system` |  |
| `private final boolean` | `runtime` |  |
| `private final boolean` | `provided` |  |
| `private final boolean` | `other` |  |

### Constructors

#### `NDependencyScope()`

```java
NDependencyScope()
```

default constructor


### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;NDependencyScope&gt;` | [parse(String)](#m-NDependencyScope-parse) | Parse. |
| `boolean` | [isCompile()](#m-NDependencyScope-isCompile) | Checks if this is compile. |
| `boolean` | [isApi()](#m-NDependencyScope-isApi) | Checks if this is api. |
| `boolean` | [isImplementation()](#m-NDependencyScope-isImplementation) | Checks if this is implementation. |
| `boolean` | [isTest()](#m-NDependencyScope-isTest) | Checks if this is test. |
| `boolean` | [isSystem()](#m-NDependencyScope-isSystem) | Checks if this is system. |
| `boolean` | [isRuntime()](#m-NDependencyScope-isRuntime) | Checks if this is runtime. |
| `boolean` | [isProvided()](#m-NDependencyScope-isProvided) | Checks if this is provided. |
| `boolean` | [isOther()](#m-NDependencyScope-isOther) | Checks if this is other. |
| `String` | [id()](#m-NDependencyScope-id) | lower cased identifier. |

### Method Details


---

<a id="m-NDependencyScope-parse"></a>

#### parse

```java
public static NOptional<NDependencyScope> parse(String value)
```

Parse.

**Parameters:**

- `value` - value

**Returns:**

parse result



---

<a id="m-NDependencyScope-isCompile"></a>

#### isCompile

```java
public boolean isCompile()
```

Checks if this is compile.

**Returns:**

is compile result



---

<a id="m-NDependencyScope-isApi"></a>

#### isApi

```java
public boolean isApi()
```

Checks if this is api.

**Returns:**

is api result



---

<a id="m-NDependencyScope-isImplementation"></a>

#### isImplementation

```java
public boolean isImplementation()
```

Checks if this is implementation.

**Returns:**

is implementation result



---

<a id="m-NDependencyScope-isTest"></a>

#### isTest

```java
public boolean isTest()
```

Checks if this is test.

**Returns:**

is test result



---

<a id="m-NDependencyScope-isSystem"></a>

#### isSystem

```java
public boolean isSystem()
```

Checks if this is system.

**Returns:**

is system result



---

<a id="m-NDependencyScope-isRuntime"></a>

#### isRuntime

```java
public boolean isRuntime()
```

Checks if this is runtime.

**Returns:**

is runtime result



---

<a id="m-NDependencyScope-isProvided"></a>

#### isProvided

```java
public boolean isProvided()
```

Checks if this is provided.

**Returns:**

is provided result



---

<a id="m-NDependencyScope-isOther"></a>

#### isOther

```java
public boolean isOther()
```

Checks if this is other.

**Returns:**

is other result



---

<a id="m-NDependencyScope-id"></a>

#### id

```java
public String id()
```

lower cased identifier.

**Returns:**

lower cased identifier



---

<a id="cls-net-thevpc-nuts-artifact-NDependencyScopePattern"></a>

## NDependencyScopePattern

```java
public enum NDependencyScopePattern implements NEnum
```

Supported dependency scope pattern.
A dependency scope pattern

**Author:** thevpc

**Since:** 0.5.6

### Enum Constants

| Constant | Description |
| :--- | :--- |
| `API` | api (gradle) / compile (maven) |
| `IMPLEMENTATION` | implementation (gradle) / compile (maven) |
| `PROVIDED` | provided (gradle) / provided (maven) |
| `IMPORT` | import (maven) |
| `RUNTIME` | testRuntime (gradle) / runtime (maven) |
| `SYSTEM` | system (maven) |
| `TEST_API` | equivalent to maven's test |
| `TEST_IMPLEMENTATION` | equivalent to maven's test |
| `TEST_PROVIDED` | testCompileOnly (gradle) |
| `TEST_RUNTIME` | testRuntime (gradle) |
| `TEST_SYSTEM` | dependencies needed for test execution |
| `OTHER` | other |
| `TEST_OTHER` | dependencies needed for test execution |
| `TEST_COMPILE` | [PATTERN] testCompile (gradle) / test (maven) |
| `TEST` | [PATTERN] dependencies needed for running/executing unit tests the nuts : includes 'test,compile,system,runtime' witch are NOT optional |
| `COMPILE` | [PATTERN] maven compile |
| `RUN` | [PATTERN] dependencies needed for running/executing the nuts : includes 'compile,system,runtime' witch are NOT optional |
| `RUN_TEST` | [PATTERN] run test |
| `ALL` | [PATTERN] all dependencies (no restriction) |

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private final String` | `id` | lower-cased identifier for the enum entry |

### Constructors

#### `NDependencyScopePattern()`

```java
NDependencyScopePattern()
```

N dependency scope pattern.


### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;NDependencyScopePattern&gt;` | [parse(String)](#m-NDependencyScopePattern-parse) | Parse. |
| `String` | [id()](#m-NDependencyScopePattern-id) | lower cased identifier. |
| `EnumSet&lt;NDependencyScope&gt;` | [toScopes()](#m-NDependencyScopePattern-toScopes) | Converts to scopes. |

### Method Details


---

<a id="m-NDependencyScopePattern-parse"></a>

#### parse

```java
public static NOptional<NDependencyScopePattern> parse(String value)
```

Parse.

**Parameters:**

- `value` - value

**Returns:**

parse result



---

<a id="m-NDependencyScopePattern-id"></a>

#### id

```java
public String id()
```

lower cased identifier.

**Returns:**

lower cased identifier



---

<a id="m-NDependencyScopePattern-toScopes"></a>

#### toScopes

```java
public EnumSet<NDependencyScope> toScopes()
```

Converts to scopes.

**Returns:**

to scopes result



---

<a id="cls-net-thevpc-nuts-artifact-NDependencySolver"></a>

## NDependencySolver

```java
public interface NDependencySolver
```

Nuts descriptors solver responsible to building traversable dependency tree

**Since:** 0.8.3

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NDependencySolver` | [of()](#m-NDependencySolver-of) | Creates a new instance. |
| `static NDependencySolver` | [of(String)](#m-NDependencySolver-of) | Creates a new instance. |
| `static List&lt;String&gt;` | [solverNames()](#m-NDependencySolver-solverNames) | Solver names. |
| `NDependencySolver` | [add(NDefinition)](#m-NDependencySolver-add) | Adds the given element. |
| `NDependencySolver` | [add(NDependency)](#m-NDependencySolver-add) | Adds the given element. |
| `NDependencySolver` | [add(NDependency, NDefinition)](#m-NDependencySolver-add) | Adds the given element. |
| `NDependencySolver` | [dependencyFilter(NDependencyFilter)](#m-NDependencySolver-dependencyFilter) | Dependency filter. |
| `NDependencySolver` | [repositoryFilter(NRepositoryFilter)](#m-NDependencySolver-repositoryFilter) | Repository filter. |
| `NDependencies` | [solve()](#m-NDependencySolver-solve) | Solve. |
| `String` | [name()](#m-NDependencySolver-name) | Name. |
| `boolean` | [isIgnoreCurrentEnvironment()](#m-NDependencySolver-isIgnoreCurrentEnvironment) | Checks if this is ignore. |
| `NDependencySolver` | [ignoreCurrentEnvironment(boolean)](#m-NDependencySolver-ignoreCurrentEnvironment) | Ignore current environment. |

### Method Details


---

<a id="m-NDependencySolver-of"></a>

#### of

```java
static NDependencySolver of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NDependencySolver-of"></a>

#### of

```java
static NDependencySolver of(String solverName)
```

Creates a new instance.

**Parameters:**

- `solverName` - solver name

**Returns:**

of result



---

<a id="m-NDependencySolver-solverNames"></a>

#### solverNames

```java
static List<String> solverNames()
```

Solver names.

**Returns:**

solver names result



---

<a id="m-NDependencySolver-add"></a>

#### add

```java
NDependencySolver add(NDefinition def)
```

Adds the given element.

**Parameters:**

- `def` - def

**Returns:**

add result



---

<a id="m-NDependencySolver-add"></a>

#### add

```java
NDependencySolver add(NDependency dependency)
```

Adds the given element.

**Parameters:**

- `dependency` - dependency

**Returns:**

add result



---

<a id="m-NDependencySolver-add"></a>

#### add

```java
NDependencySolver add(NDependency dependency, NDefinition def)
```

Adds the given element.

**Parameters:**

- `dependency` - dependency
- `def` - def

**Returns:**

add result



---

<a id="m-NDependencySolver-dependencyFilter"></a>

#### dependencyFilter

```java
NDependencySolver dependencyFilter(NDependencyFilter dependencyFilter)
```

Dependency filter.

**Parameters:**

- `dependencyFilter` - dependency filter

**Returns:**

dependency filter result



---

<a id="m-NDependencySolver-repositoryFilter"></a>

#### repositoryFilter

```java
NDependencySolver repositoryFilter(NRepositoryFilter repositoryFilter)
```

Repository filter.

**Parameters:**

- `repositoryFilter` - repository filter

**Returns:**

repository filter result



---

<a id="m-NDependencySolver-solve"></a>

#### solve

```java
NDependencies solve()
```

Solve.

**Returns:**

solve result



---

<a id="m-NDependencySolver-name"></a>

#### name

```java
String name()
```

Name.

**Returns:**

name result



---

<a id="m-NDependencySolver-isIgnoreCurrentEnvironment"></a>

#### isIgnoreCurrentEnvironment

```java
boolean isIgnoreCurrentEnvironment()
```

Checks if this is ignore. current environment.

**Returns:**

is ignore current environment result



---

<a id="m-NDependencySolver-ignoreCurrentEnvironment"></a>

#### ignoreCurrentEnvironment

```java
NDependencySolver ignoreCurrentEnvironment(boolean ignoreCurrentEnvironment)
```

Ignore current environment.

**Parameters:**

- `ignoreCurrentEnvironment` - ignore current environment

**Returns:**

ignore current environment result



---

<a id="cls-net-thevpc-nuts-artifact-NDependencyTreeNode"></a>

## NDependencyTreeNode

```java
public interface NDependencyTreeNode extends Serializable
```

Dependency tree node

**Author:** thevpc

**Since:** 0.5.5

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NDependency` | [dependency()](#m-NDependencyTreeNode-dependency) | node dependency |
| `boolean` | [isOptional()](#m-NDependencyTreeNode-isOptional) | Checks if this is optional. |
| `boolean` | [isProvided()](#m-NDependencyTreeNode-isProvided) | Checks if this is provided. |
| `List&lt;NDependencyTreeNode&gt;` | [children()](#m-NDependencyTreeNode-children) | node children |
| `boolean` | [isPartial()](#m-NDependencyTreeNode-isPartial) | true if the node is partial filled (not all children are considered) |

### Method Details


---

<a id="m-NDependencyTreeNode-dependency"></a>

#### dependency

```java
NDependency dependency()
```

node dependency

**Returns:**

node dependency



---

<a id="m-NDependencyTreeNode-isOptional"></a>

#### isOptional

```java
boolean isOptional()
```

Checks if this is optional.

**Returns:**

is optional result



---

<a id="m-NDependencyTreeNode-isProvided"></a>

#### isProvided

```java
boolean isProvided()
```

Checks if this is provided.

**Returns:**

is provided result



---

<a id="m-NDependencyTreeNode-children"></a>

#### children

```java
List<NDependencyTreeNode> children()
```

node children

**Returns:**

node children



---

<a id="m-NDependencyTreeNode-isPartial"></a>

#### isPartial

```java
boolean isPartial()
```

true if the node is partial filled (not all children are considered)

**Returns:**

true if the node is partial filled (not all children are considered)



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptor"></a>

## NDescriptor

```java
public interface NDescriptor extends Serializable, NBlankable
```

Nuts descriptors define an **immutable** image to all information needed to execute an artifact.
It resembles to maven's pom file, but it focuses on execution information
rather than build information. Common features are inheritance
dependencies, standard dependencies, exclusions and properties.
However, nuts descriptor adds new features such as :


- multiple parent inheritance
- executable/nuts-executable flag
- environment (arch, os, dist,platform) filters
- classifiers may be mapped to environment (think of dlls for windows and so for linux)


A versatile way to change descriptor is to use builder ([#builder()](#m-builder)).

**Author:** thevpc

**Since:** 0.1.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NId` | [id()](#m-NDescriptor-id) | artifact full id (groupId+artifactId+version) |
| `List&lt;NId&gt;` | [parents()](#m-NDescriptor-parents) | descriptor parent list (maybe empty) |
| `boolean` | [isExecutable()](#m-NDescriptor-isExecutable) | true if the artifact is executable and is considered an application. |
| `boolean` | [isNutsApplication()](#m-NDescriptor-isNutsApplication) | true if the artifact is a java executable that implements NApplicationHandler interface. |
| `boolean` | [isPlatformApplication()](#m-NDescriptor-isPlatformApplication) | Checks if this is platform. |
| `Set&lt;NDescriptorFlag&gt;` | [flags()](#m-NDescriptor-flags) | return descriptor flags |
| `NIdType` | [idType()](#m-NDescriptor-idType) | return id type |
| `String` | [packaging()](#m-NDescriptor-packaging) | return descriptor packaging (used to resolve file extension) |
| `boolean` | [isNoContent()](#m-NDescriptor-isNoContent) | This is typically the case for pom projects |
| `String` | [solver()](#m-NDescriptor-solver) | dependency resolution solver. |
| `NEnvCondition` | [condition()](#m-NDescriptor-condition) | Descriptor Condition |
| `String` | [name()](#m-NDescriptor-name) | user-friendly name, a short description for the artifact |
| `List&lt;String&gt;` | [icons()](#m-NDescriptor-icons) | url (external or classpath url) to the application Icon |
| `String` | [genericName()](#m-NDescriptor-genericName) | Generic Artifact Name (like 'Text Editor', 'Image Processing Application', etc) |
| `List&lt;String&gt;` | [categories()](#m-NDescriptor-categories) | category path of the artifact (slash separated). |
| `String` | [description()](#m-NDescriptor-description) | long description for the artifact |
| `List&lt;NIdLocation&gt;` | [locations()](#m-NDescriptor-locations) | list of available mirror locations from which nuts can download artifact content. |
| `List&lt;NDependency&gt;` | [standardDependencies()](#m-NDescriptor-standardDependencies) | The dependencies specified here are not used until they are referenced in a POM within the group. |
| `List&lt;NDependency&gt;` | [dependencies()](#m-NDescriptor-dependencies) | list of immediate (non-inherited and non-transitive dependencies |
| `NArtifactCall` | [executor()](#m-NDescriptor-executor) | descriptor of artifact responsible for running this artifact |
| `NArtifactCall` | [installer()](#m-NDescriptor-installer) | descriptor of artifact responsible for installing this artifact |
| `List&lt;NDescriptorProperty&gt;` | [properties()](#m-NDescriptor-properties) | custom properties that can be used as placeholders (int ${name} form) in other fields. |
| `NOptional&lt;NDescriptorProperty&gt;` | [getProperty(String)](#m-NDescriptor-getProperty) | custom property |
| `NOptional&lt;NLiteral&gt;` | [getPropertyValue(String)](#m-NDescriptor-getPropertyValue) | custom property |
| `NDescriptorBuilder` | [builder()](#m-NDescriptor-builder) | create new builder filled with this descriptor fields. |
| `List&lt;NDescriptorContributor&gt;` | [contributors()](#m-NDescriptor-contributors) |  |
| `List&lt;NDescriptorContributor&gt;` | [developers()](#m-NDescriptor-developers) |  |
| `List&lt;NDescriptorLicense&gt;` | [licenses()](#m-NDescriptor-licenses) |  |
| `List&lt;NDescriptorMailingList&gt;` | [mailingLists()](#m-NDescriptor-mailingLists) |  |
| `NDescriptorOrganization` | [organization()](#m-NDescriptor-organization) |  |

### Method Details


---

<a id="m-NDescriptor-id"></a>

#### id

```java
@NGetter
NId id()
```

**Accessor:** Property getter.

artifact full id (groupId+artifactId+version)

**Returns:**

artifact id



---

<a id="m-NDescriptor-parents"></a>

#### parents

```java
@NGetter
List<NId> parents()
```

**Accessor:** Property getter.

descriptor parent list (maybe empty)

**Returns:**

descriptor parent list (maybe empty)



---

<a id="m-NDescriptor-isExecutable"></a>

#### isExecutable

```java
boolean isExecutable()
```

true if the artifact is executable and is considered an application. if not It's a library.

**Returns:**

true if the artifact is executable



---

<a id="m-NDescriptor-isNutsApplication"></a>

#### isNutsApplication

```java
boolean isNutsApplication()
```

true if the artifact is a java executable that implements [NApplicationHandler](#cls-NApplicationHandler) interface.

**Returns:**

true if the artifact is a java executable that implements [NApplicationHandler](#cls-NApplicationHandler) interface.



---

<a id="m-NDescriptor-isPlatformApplication"></a>

#### isPlatformApplication

```java
boolean isPlatformApplication()
```

Checks if this is platform. application.

**Returns:**

is platform application result



---

<a id="m-NDescriptor-flags"></a>

#### flags

```java
@NGetter
Set<NDescriptorFlag> flags()
```

**Accessor:** Property getter.

return descriptor flags

**Returns:**

descriptor flags



---

<a id="m-NDescriptor-idType"></a>

#### idType

```java
@NGetter
NIdType idType()
```

**Accessor:** Property getter.

return id type

**Returns:**

id type



---

<a id="m-NDescriptor-packaging"></a>

#### packaging

```java
@NGetter
String packaging()
```

**Accessor:** Property getter.

return descriptor packaging (used to resolve file extension)

**Returns:**

return descriptor packaging (used to resolve file extension)



---

<a id="m-NDescriptor-isNoContent"></a>

#### isNoContent

```java
boolean isNoContent()
```

This is typically the case for pom projects

**Returns:**

true when the descriptor does not define a content.



---

<a id="m-NDescriptor-solver"></a>

#### solver

```java
String solver()
```

dependency resolution solver. defaults to 'maven'

**Returns:**

dependency resolution solver



---

<a id="m-NDescriptor-condition"></a>

#### condition

```java
@NGetter
NEnvCondition condition()
```

**Accessor:** Property getter.

Descriptor Condition

**Returns:**

Descriptor Condition



---

<a id="m-NDescriptor-name"></a>

#### name

```java
@NGetter
String name()
```

**Accessor:** Property getter.

user-friendly name, a short description for the artifact

**Returns:**

user friendly name



---

<a id="m-NDescriptor-icons"></a>

#### icons

```java
@NGetter
List<String> icons()
```

**Accessor:** Property getter.

url (external or classpath url) to the application Icon

**Returns:**

url (external or classpath url) to the application Icon



---

<a id="m-NDescriptor-genericName"></a>

#### genericName

```java
@NGetter
String genericName()
```

**Accessor:** Property getter.

Generic Artifact Name (like 'Text Editor', 'Image Processing Application', etc)

**Returns:**

Generic Artifact Name



---

<a id="m-NDescriptor-categories"></a>

#### categories

```java
@NGetter
List<String> categories()
```

**Accessor:** Property getter.

category path of the artifact (slash separated).
Standard Category Names should be used.

**Returns:**

category path of the artifact



---

<a id="m-NDescriptor-description"></a>

#### description

```java
String description()
```

long description for the artifact

**Returns:**

long description for the artifact



---

<a id="m-NDescriptor-locations"></a>

#### locations

```java
@NGetter
List<NIdLocation> locations()
```

**Accessor:** Property getter.

list of available mirror locations from which nuts can download artifact content.
location can be mapped to a classifier.

**Returns:**

list of available mirror locations



---

<a id="m-NDescriptor-standardDependencies"></a>

#### standardDependencies

```java
@NGetter
List<NDependency> standardDependencies()
```

**Accessor:** Property getter.

The dependencies specified here are not used until they are referenced in
a POM within the group. This allows the specification of a
&quot;standard&quot; version for a particular. This corresponds to
"dependencyManagement.dependencies" in maven

**Returns:**

"standard" dependencies



---

<a id="m-NDescriptor-dependencies"></a>

#### dependencies

```java
@NGetter
List<NDependency> dependencies()
```

**Accessor:** Property getter.

list of immediate (non-inherited and non-transitive dependencies

**Returns:**

list of immediate (non-inherited and non-transitive dependencies



---

<a id="m-NDescriptor-executor"></a>

#### executor

```java
@NGetter
NArtifactCall executor()
```

**Accessor:** Property getter.

descriptor of artifact responsible for running this artifact

**Returns:**

descriptor of artifact responsible for running this artifact



---

<a id="m-NDescriptor-installer"></a>

#### installer

```java
@NGetter
NArtifactCall installer()
```

**Accessor:** Property getter.

descriptor of artifact responsible for installing this artifact

**Returns:**

descriptor of artifact responsible for installing this artifact



---

<a id="m-NDescriptor-properties"></a>

#### properties

```java
@NGetter
List<NDescriptorProperty> properties()
```

**Accessor:** Property getter.

custom properties that can be used as placeholders (int ${name} form) in other fields.

**Returns:**

custom properties that can be used as placeholders (int ${name} form) in other fields.



---

<a id="m-NDescriptor-getProperty"></a>

#### getProperty

```java
NOptional<NDescriptorProperty> getProperty(String name)
```

custom property

**Parameters:**

- `name` - name

**Returns:**

custom property value by name



---

<a id="m-NDescriptor-getPropertyValue"></a>

#### getPropertyValue

```java
NOptional<NLiteral> getPropertyValue(String name)
```

custom property

**Parameters:**

- `name` - name

**Returns:**

custom property value by name



---

<a id="m-NDescriptor-builder"></a>

#### builder

```java
NDescriptorBuilder builder()
```

create new builder filled with this descriptor fields.

**Returns:**

new builder filled with this descriptor fields.



---

<a id="m-NDescriptor-contributors"></a>

#### contributors

```java
@NGetter
List<NDescriptorContributor> contributors()
```

**Accessor:** Property getter.

**Returns:**

contributors



---

<a id="m-NDescriptor-developers"></a>

#### developers

```java
@NGetter
List<NDescriptorContributor> developers()
```

**Accessor:** Property getter.

**Returns:**

developers



---

<a id="m-NDescriptor-licenses"></a>

#### licenses

```java
@NGetter
List<NDescriptorLicense> licenses()
```

**Accessor:** Property getter.

**Returns:**

licenses



---

<a id="m-NDescriptor-mailingLists"></a>

#### mailingLists

```java
@NGetter
List<NDescriptorMailingList> mailingLists()
```

**Accessor:** Property getter.

**Returns:**

mailing lists



---

<a id="m-NDescriptor-organization"></a>

#### organization

```java
@NGetter
NDescriptorOrganization organization()
```

**Accessor:** Property getter.

**Returns:**

organization



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorBuilder"></a>

## NDescriptorBuilder

```java
public interface NDescriptorBuilder extends Serializable, NBlankable, NComponent
```

Nuts descriptors define a **mutable** image to all information needed to execute an artifact.
It help creating an instance of [NDescriptor](#cls-NDescriptor) by calling [#build()](#m-build)

**Author:** thevpc

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NDescriptorBuilder` | [of()](#m-NDescriptorBuilder-of) | Creates a new instance. |
| `NId` | [id()](#m-NDescriptorBuilder-id) | artifact full id (groupId+artifactId+version) |
| `boolean` | [isExecutable()](#m-NDescriptorBuilder-isExecutable) | true if the artifact is executable and is considered an application. |
| `boolean` | [isNutsApplication()](#m-NDescriptorBuilder-isNutsApplication) | true if the artifact is a java executable that implements NApplicationHandler interface. |
| `boolean` | [isPlatformApplication()](#m-NDescriptorBuilder-isPlatformApplication) | Checks if this is platform. |
| `String` | [packaging()](#m-NDescriptorBuilder-packaging) | return descriptor packaging (used to resolve file extension) |
| `boolean` | [isNoContent()](#m-NDescriptorBuilder-isNoContent) | This is typically the case for pom projects |
| `String` | [solver()](#m-NDescriptorBuilder-solver) | dependency resolution solver. |
| `NEnvConditionBuilder` | [condition()](#m-NDescriptorBuilder-condition) | Descriptor Condition |
| `String` | [name()](#m-NDescriptorBuilder-name) | user-friendly name, a short description for the artifact |
| `List&lt;String&gt;` | [icons()](#m-NDescriptorBuilder-icons) | url (external or classpath url) to the application Icon |
| `List&lt;String&gt;` | [categories()](#m-NDescriptorBuilder-categories) | category path of the artifact (slash separated). |
| `String` | [description()](#m-NDescriptorBuilder-description) | long description for the artifact |
| `List&lt;NIdLocation&gt;` | [locations()](#m-NDescriptorBuilder-locations) | list of available mirror locations from which nuts can download artifact content. |
| `List&lt;NDependency&gt;` | [standardDependencies()](#m-NDescriptorBuilder-standardDependencies) | The dependencies specified here are not used until they are referenced in a POM within the group. |
| `List&lt;NDependency&gt;` | [dependencies()](#m-NDescriptorBuilder-dependencies) | list of immediate (non-inherited and non-transitive dependencies |
| `NArtifactCall` | [executor()](#m-NDescriptorBuilder-executor) | descriptor of artifact responsible for running this artifact |
| `NArtifactCall` | [installer()](#m-NDescriptorBuilder-installer) | descriptor of artifact responsible for installing this artifact |
| `List&lt;NDescriptorProperty&gt;` | [properties()](#m-NDescriptorBuilder-properties) | custom properties that can be used as placeholders (int ${name} form) in other fields. |
| `NDescriptorBuilder` | [builder()](#m-NDescriptorBuilder-builder) | create new builder filled with this descriptor fields. |
| `List&lt;NDescriptorContributor&gt;` | [contributors()](#m-NDescriptorBuilder-contributors) |  |
| `List&lt;NDescriptorContributor&gt;` | [developers()](#m-NDescriptorBuilder-developers) |  |
| `List&lt;NDescriptorLicense&gt;` | [licenses()](#m-NDescriptorBuilder-licenses) |  |
| `List&lt;NDescriptorMailingList&gt;` | [mailingLists()](#m-NDescriptorBuilder-mailingLists) |  |
| `NDescriptorOrganization` | [organization()](#m-NDescriptorBuilder-organization) |  |
| `NDescriptorBuilder` | [id(NId)](#m-NDescriptorBuilder-id) | set id |
| `NDescriptorBuilder` | [id(String)](#m-NDescriptorBuilder-id) | set id |
| `List&lt;NId&gt;` | [parents()](#m-NDescriptorBuilder-parents) | descriptor parent list (may be empty) |
| `NDescriptorBuilder` | [parents(List&lt;NId&gt;)](#m-NDescriptorBuilder-parents) | set parents |
| `NDescriptorBuilder` | [packaging(String)](#m-NDescriptorBuilder-packaging) | set packaging |
| `NDescriptorBuilder` | [name(String)](#m-NDescriptorBuilder-name) | set name |
| `NDescriptorBuilder` | [solver(String)](#m-NDescriptorBuilder-solver) | update dependency resolution solver |
| `String` | [genericName()](#m-NDescriptorBuilder-genericName) | Generic name. |
| `NDescriptorBuilder` | [genericName(String)](#m-NDescriptorBuilder-genericName) | Generic name. |
| `NDescriptorBuilder` | [icons(List&lt;String&gt;)](#m-NDescriptorBuilder-icons) | Icons. |
| `NDescriptorBuilder` | [icons(String)](#m-NDescriptorBuilder-icons) | Icons. |
| `NDescriptorBuilder` | [categories(List&lt;String&gt;)](#m-NDescriptorBuilder-categories) | Categories. |
| `NDescriptorBuilder` | [categories(String)](#m-NDescriptorBuilder-categories) | Categories. |
| `NDescriptorBuilder` | [condition(NEnvCondition)](#m-NDescriptorBuilder-condition) | Condition. |
| `NDescriptorBuilder` | [condition(NEnvConditionBuilder)](#m-NDescriptorBuilder-condition) | Condition. |
| `NDescriptorBuilder` | [description(String)](#m-NDescriptorBuilder-description) | set description |
| `NDescriptorBuilder` | [locations(List&lt;NIdLocation&gt;)](#m-NDescriptorBuilder-locations) | set locations |
| `NDescriptorBuilder` | [locations(NIdLocation)](#m-NDescriptorBuilder-locations) | Locations. |
| `NDescriptorBuilder` | [standardDependencies(List&lt;NDependency&gt;)](#m-NDescriptorBuilder-standardDependencies) | set standard dependencies |
| `NDescriptorBuilder` | [dependencies(List&lt;NDependency&gt;)](#m-NDescriptorBuilder-dependencies) | set dependencies |
| `NDescriptorBuilder` | [executor(NArtifactCall)](#m-NDescriptorBuilder-executor) | set executor flag |
| `NDescriptorBuilder` | [installer(NArtifactCall)](#m-NDescriptorBuilder-installer) | set installer |
| `NDescriptorBuilder` | [setProperties(List&lt;NDescriptorProperty&gt;)](#m-NDescriptorBuilder-setProperties) | set properties |
| `NDescriptorBuilder` | [addLocation(NIdLocation)](#m-NDescriptorBuilder-addLocation) | add location |
| `NDescriptorBuilder` | [setProperty(String, String)](#m-NDescriptorBuilder-setProperty) | set or unset property. |
| `NDescriptorBuilder` | [copyFrom(NDescriptorBuilder)](#m-NDescriptorBuilder-copyFrom) | set all fields from other |
| `NDescriptorBuilder` | [copyFrom(NDescriptor)](#m-NDescriptorBuilder-copyFrom) | set all fields from other |
| `NDescriptorBuilder` | [clear()](#m-NDescriptorBuilder-clear) | clear this instance (set null/default all properties) |
| `NDescriptorBuilder` | [removeDependency(NDependency)](#m-NDescriptorBuilder-removeDependency) | remove dependency |
| `NDescriptorBuilder` | [addDependency(NDependency)](#m-NDescriptorBuilder-addDependency) | add dependency |
| `NDescriptorBuilder` | [addDependencies(List&lt;NDependency&gt;)](#m-NDescriptorBuilder-addDependencies) | add dependencies |
| `NDescriptorBuilder` | [removeStandardDependency(NDependency)](#m-NDescriptorBuilder-removeStandardDependency) | remove standard dependency |
| `NDescriptorBuilder` | [addStandardDependency(NDependency)](#m-NDescriptorBuilder-addStandardDependency) | add standard dependency |
| `NDescriptorBuilder` | [addStandardDependencies(List&lt;NDependency&gt;)](#m-NDescriptorBuilder-addStandardDependencies) | add standard dependencies |
| `NDescriptorBuilder` | [addProperty(NDescriptorProperty)](#m-NDescriptorBuilder-addProperty) | add property |
| `NDescriptorBuilder` | [removeProperties(NDescriptorProperty)](#m-NDescriptorBuilder-removeProperties) | remove property |
| `NDescriptorBuilder` | [addProperties(List&lt;NDescriptorProperty&gt;)](#m-NDescriptorBuilder-addProperties) | merge properties |
| `NDescriptorBuilder` | [replaceProperty(Predicate&lt;NDescriptorProperty&gt;, Function&lt;NDescriptorProperty,NDescriptorProperty&gt;)](#m-NDescriptorBuilder-replaceProperty) | create a new instance of descriptor with added/merged properties |
| `NDescriptorBuilder` | [replaceDependency(NDependencyFilter, UnaryOperator&lt;NDependency&gt;)](#m-NDescriptorBuilder-replaceDependency) | create a new instance of descriptor with added/merged dependencies |
| `NDescriptorBuilder` | [removeDependency(NDependencyFilter)](#m-NDescriptorBuilder-removeDependency) | create a new instance of descriptor with removed dependencies that match the predicate |
| `NDescriptor` | [build()](#m-NDescriptorBuilder-build) | create new Descriptor filled with this builder fields. |
| `NDescriptorBuilder` | [copy()](#m-NDescriptorBuilder-copy) | create a copy |
| `Set&lt;NDescriptorFlag&gt;` | [flags()](#m-NDescriptorBuilder-flags) | return descriptor flags |
| `NDescriptorBuilder` | [flags(Set&lt;NDescriptorFlag&gt;)](#m-NDescriptorBuilder-flags) | set flags |
| `NDescriptorBuilder` | [flags(NDescriptorFlag)](#m-NDescriptorBuilder-flags) | Flags. |
| `NDescriptorBuilder` | [addFlag(NDescriptorFlag)](#m-NDescriptorBuilder-addFlag) | add flag |
| `NDescriptorBuilder` | [addFlags(NDescriptorFlag)](#m-NDescriptorBuilder-addFlags) | add flags |
| `NDescriptorBuilder` | [removeFlag(NDescriptorFlag)](#m-NDescriptorBuilder-removeFlag) | remove flag |
| `NDescriptorBuilder` | [removeFlags(NDescriptorFlag)](#m-NDescriptorBuilder-removeFlags) | remove flags |
| `NOptional&lt;NDescriptorProperty&gt;` | [getProperty(String)](#m-NDescriptorBuilder-getProperty) | return first property |
| `NOptional&lt;NLiteral&gt;` | [getPropertyValue(String)](#m-NDescriptorBuilder-getPropertyValue) | return first property value |
| `NIdType` | [idType()](#m-NDescriptorBuilder-idType) | return id type |
| `NDescriptorBuilder` | [idType(NIdType)](#m-NDescriptorBuilder-idType) | set idType |
| `NDescriptorBuilder` | [contributors(List&lt;NDescriptorContributor&gt;)](#m-NDescriptorBuilder-contributors) | Contributors. |
| `NDescriptorBuilder` | [developers(List&lt;NDescriptorContributor&gt;)](#m-NDescriptorBuilder-developers) | Developers. |
| `NDescriptorBuilder` | [licenses(List&lt;NDescriptorLicense&gt;)](#m-NDescriptorBuilder-licenses) | Licenses. |
| `NDescriptorBuilder` | [mailingLists(List&lt;NDescriptorMailingList&gt;)](#m-NDescriptorBuilder-mailingLists) | Mailing lists. |
| `NDescriptorBuilder` | [organization(NDescriptorOrganization)](#m-NDescriptorBuilder-organization) | Organization. |

### Method Details


---

<a id="m-NDescriptorBuilder-of"></a>

#### of

```java
static NDescriptorBuilder of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NDescriptorBuilder-id"></a>

#### id

```java
@NGetter
NId id()
```

**Accessor:** Property getter.

artifact full id (groupId+artifactId+version)

**Returns:**

artifact id



---

<a id="m-NDescriptorBuilder-isExecutable"></a>

#### isExecutable

```java
@NGetter
boolean isExecutable()
```

**Accessor:** Property getter.

true if the artifact is executable and is considered an application. if not It's a library.

**Returns:**

true if the artifact is executable



---

<a id="m-NDescriptorBuilder-isNutsApplication"></a>

#### isNutsApplication

```java
@NGetter
boolean isNutsApplication()
```

**Accessor:** Property getter.

true if the artifact is a java executable that implements [NApplicationHandler](#cls-NApplicationHandler) interface.

**Returns:**

true if the artifact is a java executable that implements [NApplicationHandler](#cls-NApplicationHandler) interface.



---

<a id="m-NDescriptorBuilder-isPlatformApplication"></a>

#### isPlatformApplication

```java
@NGetter
boolean isPlatformApplication()
```

**Accessor:** Property getter.

Checks if this is platform. application.

**Returns:**

is platform application result



---

<a id="m-NDescriptorBuilder-packaging"></a>

#### packaging

```java
@NGetter
String packaging()
```

**Accessor:** Property getter.

return descriptor packaging (used to resolve file extension)

**Returns:**

return descriptor packaging (used to resolve file extension)



---

<a id="m-NDescriptorBuilder-isNoContent"></a>

#### isNoContent

```java
@NGetter
boolean isNoContent()
```

**Accessor:** Property getter.

This is typically the case for pom projects

**Returns:**

true when the descriptor does not define a content.



---

<a id="m-NDescriptorBuilder-solver"></a>

#### solver

```java
@NGetter
String solver()
```

**Accessor:** Property getter.

dependency resolution solver. defaults to 'maven'

**Returns:**

dependency resolution solver



---

<a id="m-NDescriptorBuilder-condition"></a>

#### condition

```java
@NGetter
NEnvConditionBuilder condition()
```

**Accessor:** Property getter.

Descriptor Condition

**Returns:**

Descriptor Condition



---

<a id="m-NDescriptorBuilder-name"></a>

#### name

```java
@NGetter
String name()
```

**Accessor:** Property getter.

user-friendly name, a short description for the artifact

**Returns:**

user friendly name



---

<a id="m-NDescriptorBuilder-icons"></a>

#### icons

```java
@NGetter
List<String> icons()
```

**Accessor:** Property getter.

url (external or classpath url) to the application Icon

**Returns:**

url (external or classpath url) to the application Icon



---

<a id="m-NDescriptorBuilder-categories"></a>

#### categories

```java
@NGetter
List<String> categories()
```

**Accessor:** Property getter.

category path of the artifact (slash separated).
Standard Category Names should be used.

**Returns:**

category path of the artifact



---

<a id="m-NDescriptorBuilder-description"></a>

#### description

```java
@NGetter
String description()
```

**Accessor:** Property getter.

long description for the artifact

**Returns:**

long description for the artifact



---

<a id="m-NDescriptorBuilder-locations"></a>

#### locations

```java
@NGetter
List<NIdLocation> locations()
```

**Accessor:** Property getter.

list of available mirror locations from which nuts can download artifact content.
location can be mapped to a classifier.

**Returns:**

list of available mirror locations



---

<a id="m-NDescriptorBuilder-standardDependencies"></a>

#### standardDependencies

```java
@NGetter
List<NDependency> standardDependencies()
```

**Accessor:** Property getter.

The dependencies specified here are not used until they are referenced in
a POM within the group. This allows the specification of a
&quot;standard&quot; version for a particular. This corresponds to
"dependencyManagement.dependencies" in maven

**Returns:**

"standard" dependencies



---

<a id="m-NDescriptorBuilder-dependencies"></a>

#### dependencies

```java
@NGetter
List<NDependency> dependencies()
```

**Accessor:** Property getter.

list of immediate (non-inherited and non-transitive dependencies

**Returns:**

list of immediate (non-inherited and non-transitive dependencies



---

<a id="m-NDescriptorBuilder-executor"></a>

#### executor

```java
@NGetter
NArtifactCall executor()
```

**Accessor:** Property getter.

descriptor of artifact responsible for running this artifact

**Returns:**

descriptor of artifact responsible for running this artifact



---

<a id="m-NDescriptorBuilder-installer"></a>

#### installer

```java
@NGetter
NArtifactCall installer()
```

**Accessor:** Property getter.

descriptor of artifact responsible for installing this artifact

**Returns:**

descriptor of artifact responsible for installing this artifact



---

<a id="m-NDescriptorBuilder-properties"></a>

#### properties

```java
@NGetter
List<NDescriptorProperty> properties()
```

**Accessor:** Property getter.

custom properties that can be used as placeholders (int ${name} form) in other fields.

**Returns:**

custom properties that can be used as placeholders (int ${name} form) in other fields.



---

<a id="m-NDescriptorBuilder-builder"></a>

#### builder

```java
NDescriptorBuilder builder()
```

create new builder filled with this descriptor fields.

**Returns:**

new builder filled with this descriptor fields.



---

<a id="m-NDescriptorBuilder-contributors"></a>

#### contributors

```java
@NGetter
List<NDescriptorContributor> contributors()
```

**Accessor:** Property getter.

**Returns:**

contributors



---

<a id="m-NDescriptorBuilder-developers"></a>

#### developers

```java
@NGetter
List<NDescriptorContributor> developers()
```

**Accessor:** Property getter.

**Returns:**

developers



---

<a id="m-NDescriptorBuilder-licenses"></a>

#### licenses

```java
@NGetter
List<NDescriptorLicense> licenses()
```

**Accessor:** Property getter.

**Returns:**

licenses



---

<a id="m-NDescriptorBuilder-mailingLists"></a>

#### mailingLists

```java
@NGetter
List<NDescriptorMailingList> mailingLists()
```

**Accessor:** Property getter.

**Returns:**

mailing lists



---

<a id="m-NDescriptorBuilder-organization"></a>

#### organization

```java
@NGetter
NDescriptorOrganization organization()
```

**Accessor:** Property getter.

**Returns:**

organization



---

<a id="m-NDescriptorBuilder-id"></a>

#### id

```java
@NSetter
NDescriptorBuilder id(NId id)
```

**Mutator:** Property setter.

set id

**Parameters:**

- `id` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-id"></a>

#### id

```java
NDescriptorBuilder id(String id)
```

set id

**Parameters:**

- `id` - new value

**Returns:**

`this instance`



---

<a id="m-NDescriptorBuilder-parents"></a>

#### parents

```java
@NGetter
List<NId> parents()
```

**Accessor:** Property getter.

descriptor parent list (may be empty)

**Returns:**

descriptor parent list (may be empty)



---

<a id="m-NDescriptorBuilder-parents"></a>

#### parents

```java
@NSetter
NDescriptorBuilder parents(List<NId> parents)
```

**Mutator:** Property setter.

set parents

**Parameters:**

- `parents` - value to set

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-packaging"></a>

#### packaging

```java
@NSetter
NDescriptorBuilder packaging(String packaging)
```

**Mutator:** Property setter.

set packaging

**Parameters:**

- `packaging` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-name"></a>

#### name

```java
@NSetter
NDescriptorBuilder name(String name)
```

**Mutator:** Property setter.

set name

**Parameters:**

- `name` - value to set

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-solver"></a>

#### solver

```java
@NSetter
NDescriptorBuilder solver(String solver)
```

**Mutator:** Property setter.

update dependency resolution solver

**Parameters:**

- `solver` - dependency resolution solver

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-genericName"></a>

#### genericName

```java
@NGetter
String genericName()
```

**Accessor:** Property getter.

Generic name.

**Returns:**

generic name result



---

<a id="m-NDescriptorBuilder-genericName"></a>

#### genericName

```java
NDescriptorBuilder genericName(String name)
```

Generic name.

**Parameters:**

- `name` - name

**Returns:**

generic name result



---

<a id="m-NDescriptorBuilder-icons"></a>

#### icons

```java
@NSetter
NDescriptorBuilder icons(List<String> icons)
```

**Mutator:** Property setter.

Icons.

**Parameters:**

- `icons` - icons

**Returns:**

icons result



---

<a id="m-NDescriptorBuilder-icons"></a>

#### icons

```java
NDescriptorBuilder icons(String icons)
```

Icons.

**Parameters:**

- `icons` - icons

**Returns:**

icons result



---

<a id="m-NDescriptorBuilder-categories"></a>

#### categories

```java
@NSetter
NDescriptorBuilder categories(List<String> categories)
```

**Mutator:** Property setter.

Categories.

**Parameters:**

- `categories` - categories

**Returns:**

categories result



---

<a id="m-NDescriptorBuilder-categories"></a>

#### categories

```java
@NSetter
NDescriptorBuilder categories(String categories)
```

**Mutator:** Property setter.

Categories.

**Parameters:**

- `categories` - categories

**Returns:**

categories result



---

<a id="m-NDescriptorBuilder-condition"></a>

#### condition

```java
@NSetter
NDescriptorBuilder condition(NEnvCondition condition)
```

**Mutator:** Property setter.

Condition.

**Parameters:**

- `condition` - condition

**Returns:**

condition result



---

<a id="m-NDescriptorBuilder-condition"></a>

#### condition

```java
@NSetter
NDescriptorBuilder condition(NEnvConditionBuilder condition)
```

**Mutator:** Property setter.

Condition.

**Parameters:**

- `condition` - condition

**Returns:**

condition result



---

<a id="m-NDescriptorBuilder-description"></a>

#### description

```java
@NSetter
NDescriptorBuilder description(String description)
```

**Mutator:** Property setter.

set description

**Parameters:**

- `description` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-locations"></a>

#### locations

```java
@NSetter
NDescriptorBuilder locations(List<NIdLocation> locations)
```

**Mutator:** Property setter.

set locations

**Parameters:**

- `locations` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-locations"></a>

#### locations

```java
NDescriptorBuilder locations(NIdLocation locations)
```

Locations.

**Parameters:**

- `locations` - locations

**Returns:**

locations result



---

<a id="m-NDescriptorBuilder-standardDependencies"></a>

#### standardDependencies

```java
@NSetter
NDescriptorBuilder standardDependencies(List<NDependency> dependencies)
```

**Mutator:** Property setter.

set standard dependencies

**Parameters:**

- `dependencies` - value to set

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-dependencies"></a>

#### dependencies

```java
@NSetter
NDescriptorBuilder dependencies(List<NDependency> dependencies)
```

**Mutator:** Property setter.

set dependencies

**Parameters:**

- `dependencies` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-executor"></a>

#### executor

```java
@NSetter
NDescriptorBuilder executor(NArtifactCall executor)
```

**Mutator:** Property setter.

set executor flag

**Parameters:**

- `executor` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-installer"></a>

#### installer

```java
@NSetter
NDescriptorBuilder installer(NArtifactCall installer)
```

**Mutator:** Property setter.

set installer

**Parameters:**

- `installer` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-setProperties"></a>

#### setProperties

```java
@NSetter
NDescriptorBuilder setProperties(List<NDescriptorProperty> properties)
```

**Mutator:** Property setter.

set properties

**Parameters:**

- `properties` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-addLocation"></a>

#### addLocation

```java
NDescriptorBuilder addLocation(NIdLocation location)
```

add location

**Parameters:**

- `location` - location to add

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-setProperty"></a>

#### setProperty

```java
NDescriptorBuilder setProperty(String name, String value)
```

set or unset property.
if the value is null, the property is removed.

**Parameters:**

- `name` - property name
- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-copyFrom"></a>

#### copyFrom

```java
NDescriptorBuilder copyFrom(NDescriptorBuilder other)
```

set all fields from `other`

**Parameters:**

- `other` - builder to copy from

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-copyFrom"></a>

#### copyFrom

```java
NDescriptorBuilder copyFrom(NDescriptor other)
```

set all fields from `other`

**Parameters:**

- `other` - descriptor to copy from

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-clear"></a>

#### clear

```java
NDescriptorBuilder clear()
```

clear this instance (set null/default all properties)

**Returns:**

`this instance`



---

<a id="m-NDescriptorBuilder-removeDependency"></a>

#### removeDependency

```java
NDescriptorBuilder removeDependency(NDependency dependency)
```

remove dependency

**Parameters:**

- `dependency` - value to remove

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-addDependency"></a>

#### addDependency

```java
NDescriptorBuilder addDependency(NDependency dependency)
```

add dependency

**Parameters:**

- `dependency` - new value to add

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-addDependencies"></a>

#### addDependencies

```java
NDescriptorBuilder addDependencies(List<NDependency> dependencies)
```

add dependencies

**Parameters:**

- `dependencies` - new value to add

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-removeStandardDependency"></a>

#### removeStandardDependency

```java
NDescriptorBuilder removeStandardDependency(NDependency dependency)
```

remove standard dependency

**Parameters:**

- `dependency` - value to remove

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-addStandardDependency"></a>

#### addStandardDependency

```java
NDescriptorBuilder addStandardDependency(NDependency dependency)
```

add standard dependency

**Parameters:**

- `dependency` - value to add

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-addStandardDependencies"></a>

#### addStandardDependencies

```java
NDescriptorBuilder addStandardDependencies(List<NDependency> dependencies)
```

add standard dependencies

**Parameters:**

- `dependencies` - value to add

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-addProperty"></a>

#### addProperty

```java
NDescriptorBuilder addProperty(NDescriptorProperty property)
```

add property

**Parameters:**

- `property` - property

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-removeProperties"></a>

#### removeProperties

```java
NDescriptorBuilder removeProperties(NDescriptorProperty property)
```

remove property

**Parameters:**

- `property` - property

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-addProperties"></a>

#### addProperties

```java
NDescriptorBuilder addProperties(List<NDescriptorProperty> properties)
```

merge properties

**Parameters:**

- `properties` - new value

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-replaceProperty"></a>

#### replaceProperty

```java
NDescriptorBuilder replaceProperty(Predicate<NDescriptorProperty> filter, Function<NDescriptorProperty,NDescriptorProperty> converter)
```

create a new instance of descriptor with added/merged properties

**Parameters:**

- `filter` - properties entry that match the update
- `converter` - function to provide new value to replace with

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-replaceDependency"></a>

#### replaceDependency

```java
NDescriptorBuilder replaceDependency(NDependencyFilter filter, UnaryOperator<NDependency> converter)
```

create a new instance of descriptor with added/merged dependencies

**Parameters:**

- `filter` - properties entry that match the update
- `converter` - function to provide new value to replace with

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-removeDependency"></a>

#### removeDependency

```java
NDescriptorBuilder removeDependency(NDependencyFilter dependency)
```

create a new instance of descriptor with removed dependencies that match the predicate

**Parameters:**

- `dependency` - predicate to test against

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-build"></a>

#### build

```java
NDescriptor build()
```

create new Descriptor filled with this builder fields.

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-copy"></a>

#### copy

```java
NDescriptorBuilder copy()
```

create a copy

**Returns:**

a copy



---

<a id="m-NDescriptorBuilder-flags"></a>

#### flags

```java
@NGetter
Set<NDescriptorFlag> flags()
```

**Accessor:** Property getter.

return descriptor flags

**Returns:**

return descriptor flags



---

<a id="m-NDescriptorBuilder-flags"></a>

#### flags

```java
@NSetter
NDescriptorBuilder flags(Set<NDescriptorFlag> flags)
```

**Mutator:** Property setter.

set flags

**Parameters:**

- `flags` - flags

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-flags"></a>

#### flags

```java
NDescriptorBuilder flags(NDescriptorFlag flags)
```

Flags.

**Parameters:**

- `flags` - flags

**Returns:**

flags result



---

<a id="m-NDescriptorBuilder-addFlag"></a>

#### addFlag

```java
NDescriptorBuilder addFlag(NDescriptorFlag flag)
```

add flag

**Parameters:**

- `flag` - flag

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-addFlags"></a>

#### addFlags

```java
NDescriptorBuilder addFlags(NDescriptorFlag flags)
```

add flags

**Parameters:**

- `flags` - flags

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-removeFlag"></a>

#### removeFlag

```java
NDescriptorBuilder removeFlag(NDescriptorFlag flag)
```

remove flag

**Parameters:**

- `flag` - flags

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-removeFlags"></a>

#### removeFlags

```java
NDescriptorBuilder removeFlags(NDescriptorFlag flags)
```

remove flags

**Parameters:**

- `flags` - flags

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-getProperty"></a>

#### getProperty

```java
NOptional<NDescriptorProperty> getProperty(String name)
```

return first property

**Parameters:**

- `name` - property name

**Returns:**

first property



---

<a id="m-NDescriptorBuilder-getPropertyValue"></a>

#### getPropertyValue

```java
NOptional<NLiteral> getPropertyValue(String name)
```

return first property value

**Parameters:**

- `name` - property name

**Returns:**

first property value



---

<a id="m-NDescriptorBuilder-idType"></a>

#### idType

```java
@NGetter
NIdType idType()
```

**Accessor:** Property getter.

return id type

**Returns:**

id type



---

<a id="m-NDescriptorBuilder-idType"></a>

#### idType

```java
@NSetter
NDescriptorBuilder idType(NIdType idType)
```

**Mutator:** Property setter.

set idType

**Parameters:**

- `idType` - idType

**Returns:**

`this` instance



---

<a id="m-NDescriptorBuilder-contributors"></a>

#### contributors

```java
@NSetter
NDescriptorBuilder contributors(List<NDescriptorContributor> contributors)
```

**Mutator:** Property setter.

Contributors.

**Parameters:**

- `contributors` - contributors

**Returns:**

contributors result



---

<a id="m-NDescriptorBuilder-developers"></a>

#### developers

```java
@NSetter
NDescriptorBuilder developers(List<NDescriptorContributor> developers)
```

**Mutator:** Property setter.

Developers.

**Parameters:**

- `developers` - developers

**Returns:**

developers result



---

<a id="m-NDescriptorBuilder-licenses"></a>

#### licenses

```java
@NSetter
NDescriptorBuilder licenses(List<NDescriptorLicense> licenses)
```

**Mutator:** Property setter.

Licenses.

**Parameters:**

- `licenses` - licenses

**Returns:**

licenses result



---

<a id="m-NDescriptorBuilder-mailingLists"></a>

#### mailingLists

```java
@NSetter
NDescriptorBuilder mailingLists(List<NDescriptorMailingList> mailingLists)
```

**Mutator:** Property setter.

Mailing lists.

**Parameters:**

- `mailingLists` - mailing lists

**Returns:**

mailing lists result



---

<a id="m-NDescriptorBuilder-organization"></a>

#### organization

```java
@NSetter
NDescriptorBuilder organization(NDescriptorOrganization organization)
```

**Mutator:** Property setter.

Organization.

**Parameters:**

- `organization` - organization

**Returns:**

organization result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorContributor"></a>

## NDescriptorContributor

```java
public interface NDescriptorContributor
```

NDescriptorContributor interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `String` | [id()](#m-NDescriptorContributor-id) | Id. |
| `String` | [name()](#m-NDescriptorContributor-name) | Name. |
| `String` | [url()](#m-NDescriptorContributor-url) | Url. |
| `String` | [email()](#m-NDescriptorContributor-email) | Email. |
| `List&lt;String&gt;` | [roles()](#m-NDescriptorContributor-roles) | Roles. |
| `String` | [timezone()](#m-NDescriptorContributor-timezone) | Timezone. |
| `List&lt;String&gt;` | [icons()](#m-NDescriptorContributor-icons) | Icons. |
| `Map&lt;String,String&gt;` | [properties()](#m-NDescriptorContributor-properties) | Properties. |
| `String` | [comments()](#m-NDescriptorContributor-comments) | Comments. |
| `NDescriptorOrganization` | [organization()](#m-NDescriptorContributor-organization) | Organization. |
| `NDescriptorContributor` | [readOnly()](#m-NDescriptorContributor-readOnly) | Read only. |
| `NDescriptorContributorBuilder` | [builder()](#m-NDescriptorContributor-builder) | Builder. |

### Method Details


---

<a id="m-NDescriptorContributor-id"></a>

#### id

```java
@NGetter
String id()
```

**Accessor:** Property getter.

Id.

**Returns:**

id result



---

<a id="m-NDescriptorContributor-name"></a>

#### name

```java
@NGetter
String name()
```

**Accessor:** Property getter.

Name.

**Returns:**

name result



---

<a id="m-NDescriptorContributor-url"></a>

#### url

```java
@NGetter
String url()
```

**Accessor:** Property getter.

Url.

**Returns:**

url result



---

<a id="m-NDescriptorContributor-email"></a>

#### email

```java
@NGetter
String email()
```

**Accessor:** Property getter.

Email.

**Returns:**

email result



---

<a id="m-NDescriptorContributor-roles"></a>

#### roles

```java
@NGetter
List<String> roles()
```

**Accessor:** Property getter.

Roles.

**Returns:**

roles result



---

<a id="m-NDescriptorContributor-timezone"></a>

#### timezone

```java
@NGetter
String timezone()
```

**Accessor:** Property getter.

Timezone.

**Returns:**

timezone result



---

<a id="m-NDescriptorContributor-icons"></a>

#### icons

```java
@NGetter
List<String> icons()
```

**Accessor:** Property getter.

Icons.

**Returns:**

icons result



---

<a id="m-NDescriptorContributor-properties"></a>

#### properties

```java
@NGetter
Map<String,String> properties()
```

**Accessor:** Property getter.

Properties.

**Returns:**

properties result



---

<a id="m-NDescriptorContributor-comments"></a>

#### comments

```java
@NGetter
String comments()
```

**Accessor:** Property getter.

Comments.

**Returns:**

comments result



---

<a id="m-NDescriptorContributor-organization"></a>

#### organization

```java
@NGetter
NDescriptorOrganization organization()
```

**Accessor:** Property getter.

Organization.

**Returns:**

organization result



---

<a id="m-NDescriptorContributor-readOnly"></a>

#### readOnly

```java
NDescriptorContributor readOnly()
```

Read only.

**Returns:**

read only result



---

<a id="m-NDescriptorContributor-builder"></a>

#### builder

```java
NDescriptorContributorBuilder builder()
```

Builder.

**Returns:**

builder result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorContributorBuilder"></a>

## NDescriptorContributorBuilder

```java
public interface NDescriptorContributorBuilder extends NDescriptorContributor
```

NDescriptorContributorBuilder interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NDescriptorContributorBuilder` | [id(String)](#m-NDescriptorContributorBuilder-id) | Id. |
| `NDescriptorContributorBuilder` | [name(String)](#m-NDescriptorContributorBuilder-name) | Name. |
| `NDescriptorContributorBuilder` | [url(String)](#m-NDescriptorContributorBuilder-url) | Url. |
| `NDescriptorContributorBuilder` | [email(String)](#m-NDescriptorContributorBuilder-email) | Email. |
| `NDescriptorContributorBuilder` | [roles(List&lt;String&gt;)](#m-NDescriptorContributorBuilder-roles) | Roles. |
| `NDescriptorContributorBuilder` | [timezone(String)](#m-NDescriptorContributorBuilder-timezone) | Timezone. |
| `NDescriptorContributorBuilder` | [icons(List&lt;String&gt;)](#m-NDescriptorContributorBuilder-icons) | Icons. |
| `NDescriptorContributorBuilder` | [properties(Map&lt;String,String&gt;)](#m-NDescriptorContributorBuilder-properties) | Properties. |
| `NDescriptorContributorBuilder` | [comments(String)](#m-NDescriptorContributorBuilder-comments) | Comments. |
| `NDescriptorContributorBuilder` | [organization(NDescriptorOrganization)](#m-NDescriptorContributorBuilder-organization) | Organization. |
| `NDescriptorContributor` | [build()](#m-NDescriptorContributorBuilder-build) | Build. |
| `NDescriptorContributorBuilder` | [copy()](#m-NDescriptorContributorBuilder-copy) | Copy. |

### Method Details


---

<a id="m-NDescriptorContributorBuilder-id"></a>

#### id

```java
@NSetter
NDescriptorContributorBuilder id(String id)
```

**Mutator:** Property setter.

Id.

**Parameters:**

- `id` - id

**Returns:**

id result



---

<a id="m-NDescriptorContributorBuilder-name"></a>

#### name

```java
@NSetter
NDescriptorContributorBuilder name(String name)
```

**Mutator:** Property setter.

Name.

**Parameters:**

- `name` - name

**Returns:**

name result



---

<a id="m-NDescriptorContributorBuilder-url"></a>

#### url

```java
@NSetter
NDescriptorContributorBuilder url(String url)
```

**Mutator:** Property setter.

Url.

**Parameters:**

- `url` - url

**Returns:**

url result



---

<a id="m-NDescriptorContributorBuilder-email"></a>

#### email

```java
@NSetter
NDescriptorContributorBuilder email(String email)
```

**Mutator:** Property setter.

Email.

**Parameters:**

- `email` - email

**Returns:**

email result



---

<a id="m-NDescriptorContributorBuilder-roles"></a>

#### roles

```java
@NSetter
NDescriptorContributorBuilder roles(List<String> roles)
```

**Mutator:** Property setter.

Roles.

**Parameters:**

- `roles` - roles

**Returns:**

roles result



---

<a id="m-NDescriptorContributorBuilder-timezone"></a>

#### timezone

```java
@NSetter
NDescriptorContributorBuilder timezone(String timezone)
```

**Mutator:** Property setter.

Timezone.

**Parameters:**

- `timezone` - timezone

**Returns:**

timezone result



---

<a id="m-NDescriptorContributorBuilder-icons"></a>

#### icons

```java
@NSetter
NDescriptorContributorBuilder icons(List<String> icons)
```

**Mutator:** Property setter.

Icons.

**Parameters:**

- `icons` - icons

**Returns:**

icons result



---

<a id="m-NDescriptorContributorBuilder-properties"></a>

#### properties

```java
@NSetter
NDescriptorContributorBuilder properties(Map<String,String> properties)
```

**Mutator:** Property setter.

Properties.

**Parameters:**

- `properties` - properties

**Returns:**

properties result



---

<a id="m-NDescriptorContributorBuilder-comments"></a>

#### comments

```java
@NSetter
NDescriptorContributorBuilder comments(String comments)
```

**Mutator:** Property setter.

Comments.

**Parameters:**

- `comments` - comments

**Returns:**

comments result



---

<a id="m-NDescriptorContributorBuilder-organization"></a>

#### organization

```java
@NSetter
NDescriptorContributorBuilder organization(NDescriptorOrganization organization)
```

**Mutator:** Property setter.

Organization.

**Parameters:**

- `organization` - organization

**Returns:**

organization result



---

<a id="m-NDescriptorContributorBuilder-build"></a>

#### build

```java
NDescriptorContributor build()
```

Build.

**Returns:**

build result



---

<a id="m-NDescriptorContributorBuilder-copy"></a>

#### copy

```java
NDescriptorContributorBuilder copy()
```

Copy.

**Returns:**

copy result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorEffectiveConfig"></a>

## NDescriptorEffectiveConfig

```java
public class NDescriptorEffectiveConfig implements Cloneable
```

NDescriptorEffectiveConfig class.

**Author:** thevpc

**Since:** 0.8.0

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private boolean` | `ignoreCurrentEnvironment` |  |

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `boolean` | [isIgnoreCurrentEnvironment()](#m-NDescriptorEffectiveConfig-isIgnoreCurrentEnvironment) | Checks if this is ignore. |
| `NDescriptorEffectiveConfig` | [setIgnoreCurrentEnvironment(boolean)](#m-NDescriptorEffectiveConfig-setIgnoreCurrentEnvironment) | Sets the ignore current environment. |
| `boolean` | [equals(Object)](#m-NDescriptorEffectiveConfig-equals) |  |
| `int` | [hashCode()](#m-NDescriptorEffectiveConfig-hashCode) |  |
| `String` | [toString()](#m-NDescriptorEffectiveConfig-toString) |  |
| `NDescriptorEffectiveConfig` | [clone()](#m-NDescriptorEffectiveConfig-clone) |  |

### Method Details


---

<a id="m-NDescriptorEffectiveConfig-isIgnoreCurrentEnvironment"></a>

#### isIgnoreCurrentEnvironment

```java
public boolean isIgnoreCurrentEnvironment()
```

Checks if this is ignore. current environment.

**Returns:**

is ignore current environment result



---

<a id="m-NDescriptorEffectiveConfig-setIgnoreCurrentEnvironment"></a>

#### setIgnoreCurrentEnvironment

```java
public NDescriptorEffectiveConfig setIgnoreCurrentEnvironment(boolean ignoreCurrentEnvironment)
```

Sets the ignore current environment.

**Parameters:**

- `ignoreCurrentEnvironment` - ignore current environment

**Returns:**

set ignore current environment result



---

<a id="m-NDescriptorEffectiveConfig-equals"></a>

#### equals

```java
@Override
public boolean equals(Object o)
```



---

<a id="m-NDescriptorEffectiveConfig-hashCode"></a>

#### hashCode

```java
@Override
public int hashCode()
```



---

<a id="m-NDescriptorEffectiveConfig-toString"></a>

#### toString

```java
@Override
public String toString()
```



---

<a id="m-NDescriptorEffectiveConfig-clone"></a>

#### clone

```java
@Override
public NDescriptorEffectiveConfig clone()
```



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorFlag"></a>

## NDescriptorFlag

```java
public enum NDescriptorFlag implements NEnum
```

Formats supported by Nuts

**Author:** thevpc

**Since:** 0.8.3

### Enum Constants

| Constant | Description |
| :--- | :--- |
| `EXEC` | executable application |
| `NUTS_APP` | nuts application |
| `PLATFORM_APP` | executable as app (has main executable) |
| `TERM` | term application |
| `GUI` | gui application |

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private final String` | `id` | lower-cased identifier for the enum entry |

### Constructors

#### `NDescriptorFlag()`

```java
NDescriptorFlag()
```

private constructor


### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;NDescriptorFlag&gt;` | [parse(String)](#m-NDescriptorFlag-parse) | Parse. |
| `String` | [id()](#m-NDescriptorFlag-id) | lower cased identifier. |

### Method Details


---

<a id="m-NDescriptorFlag-parse"></a>

#### parse

```java
public static NOptional<NDescriptorFlag> parse(String value)
```

Parse.

**Parameters:**

- `value` - value

**Returns:**

parse result



---

<a id="m-NDescriptorFlag-id"></a>

#### id

```java
public String id()
```

lower cased identifier.

**Returns:**

lower cased identifier



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorLicense"></a>

## NDescriptorLicense

```java
public interface NDescriptorLicense
```

NDescriptorLicense interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `String` | [name()](#m-NDescriptorLicense-name) | Name. |
| `String` | [date()](#m-NDescriptorLicense-date) | Date. |
| `String` | [url()](#m-NDescriptorLicense-url) | Url. |
| `String` | [distribution()](#m-NDescriptorLicense-distribution) | Distribution. |
| `String` | [comments()](#m-NDescriptorLicense-comments) | Comments. |
| `String` | [id()](#m-NDescriptorLicense-id) | Id. |
| `Map&lt;String,String&gt;` | [properties()](#m-NDescriptorLicense-properties) | Properties. |
| `NDescriptorLicense` | [readOnly()](#m-NDescriptorLicense-readOnly) | Read only. |
| `NDescriptorLicenseBuilder` | [builder()](#m-NDescriptorLicense-builder) | Builder. |

### Method Details


---

<a id="m-NDescriptorLicense-name"></a>

#### name

```java
@NGetter
String name()
```

**Accessor:** Property getter.

Name.

**Returns:**

name result



---

<a id="m-NDescriptorLicense-date"></a>

#### date

```java
@NGetter
String date()
```

**Accessor:** Property getter.

Date.

**Returns:**

date result



---

<a id="m-NDescriptorLicense-url"></a>

#### url

```java
@NGetter
String url()
```

**Accessor:** Property getter.

Url.

**Returns:**

url result



---

<a id="m-NDescriptorLicense-distribution"></a>

#### distribution

```java
@NGetter
String distribution()
```

**Accessor:** Property getter.

Distribution.

**Returns:**

distribution result



---

<a id="m-NDescriptorLicense-comments"></a>

#### comments

```java
@NGetter
String comments()
```

**Accessor:** Property getter.

Comments.

**Returns:**

comments result



---

<a id="m-NDescriptorLicense-id"></a>

#### id

```java
@NGetter
String id()
```

**Accessor:** Property getter.

Id.

**Returns:**

id result



---

<a id="m-NDescriptorLicense-properties"></a>

#### properties

```java
@NGetter
Map<String,String> properties()
```

**Accessor:** Property getter.

Properties.

**Returns:**

properties result



---

<a id="m-NDescriptorLicense-readOnly"></a>

#### readOnly

```java
NDescriptorLicense readOnly()
```

Read only.

**Returns:**

read only result



---

<a id="m-NDescriptorLicense-builder"></a>

#### builder

```java
NDescriptorLicenseBuilder builder()
```

Builder.

**Returns:**

builder result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorLicenseBuilder"></a>

## NDescriptorLicenseBuilder

```java
public interface NDescriptorLicenseBuilder extends NDescriptorLicense
```

NDescriptorLicenseBuilder interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NDescriptorLicenseBuilder` | [name(String)](#m-NDescriptorLicenseBuilder-name) | Name. |
| `NDescriptorLicenseBuilder` | [url(String)](#m-NDescriptorLicenseBuilder-url) | Url. |
| `NDescriptorLicenseBuilder` | [distribution(String)](#m-NDescriptorLicenseBuilder-distribution) | Distribution. |
| `NDescriptorLicenseBuilder` | [comments(String)](#m-NDescriptorLicenseBuilder-comments) | Comments. |
| `NDescriptorLicenseBuilder` | [id(String)](#m-NDescriptorLicenseBuilder-id) | Id. |
| `NDescriptorLicenseBuilder` | [properties(Map&lt;String,String&gt;)](#m-NDescriptorLicenseBuilder-properties) | Properties. |
| `NDescriptorLicenseBuilder` | [copy()](#m-NDescriptorLicenseBuilder-copy) | Copy. |
| `NDescriptorLicense` | [build()](#m-NDescriptorLicenseBuilder-build) | Build. |

### Method Details


---

<a id="m-NDescriptorLicenseBuilder-name"></a>

#### name

```java
@NSetter
NDescriptorLicenseBuilder name(String name)
```

**Mutator:** Property setter.

Name.

**Parameters:**

- `name` - name

**Returns:**

name result



---

<a id="m-NDescriptorLicenseBuilder-url"></a>

#### url

```java
@NSetter
NDescriptorLicenseBuilder url(String url)
```

**Mutator:** Property setter.

Url.

**Parameters:**

- `url` - url

**Returns:**

url result



---

<a id="m-NDescriptorLicenseBuilder-distribution"></a>

#### distribution

```java
@NSetter
NDescriptorLicenseBuilder distribution(String distribution)
```

**Mutator:** Property setter.

Distribution.

**Parameters:**

- `distribution` - distribution

**Returns:**

distribution result



---

<a id="m-NDescriptorLicenseBuilder-comments"></a>

#### comments

```java
@NSetter
NDescriptorLicenseBuilder comments(String comments)
```

**Mutator:** Property setter.

Comments.

**Parameters:**

- `comments` - comments

**Returns:**

comments result



---

<a id="m-NDescriptorLicenseBuilder-id"></a>

#### id

```java
@NSetter
NDescriptorLicenseBuilder id(String id)
```

**Mutator:** Property setter.

Id.

**Parameters:**

- `id` - id

**Returns:**

id result



---

<a id="m-NDescriptorLicenseBuilder-properties"></a>

#### properties

```java
@NSetter
NDescriptorLicenseBuilder properties(Map<String,String> properties)
```

**Mutator:** Property setter.

Properties.

**Parameters:**

- `properties` - properties

**Returns:**

properties result



---

<a id="m-NDescriptorLicenseBuilder-copy"></a>

#### copy

```java
NDescriptorLicenseBuilder copy()
```

Copy.

**Returns:**

copy result



---

<a id="m-NDescriptorLicenseBuilder-build"></a>

#### build

```java
NDescriptorLicense build()
```

Build.

**Returns:**

build result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorMailingList"></a>

## NDescriptorMailingList

```java
public interface NDescriptorMailingList
```

NDescriptorMailingList interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `String` | [id()](#m-NDescriptorMailingList-id) | Id. |
| `String` | [name()](#m-NDescriptorMailingList-name) | Name. |
| `String` | [subscribe()](#m-NDescriptorMailingList-subscribe) | Subscribe. |
| `String` | [unsubscribe()](#m-NDescriptorMailingList-unsubscribe) | Unsubscribe. |
| `String` | [post()](#m-NDescriptorMailingList-post) | Post. |
| `String` | [archive()](#m-NDescriptorMailingList-archive) | Archive. |
| `List&lt;String&gt;` | [otherArchives()](#m-NDescriptorMailingList-otherArchives) | Other archives. |
| `Map&lt;String,String&gt;` | [properties()](#m-NDescriptorMailingList-properties) | Properties. |
| `String` | [comments()](#m-NDescriptorMailingList-comments) | Comments. |
| `NDescriptorMailingList` | [readOnly()](#m-NDescriptorMailingList-readOnly) | Read only. |
| `NDescriptorMailingListBuilder` | [builder()](#m-NDescriptorMailingList-builder) | Builder. |

### Method Details


---

<a id="m-NDescriptorMailingList-id"></a>

#### id

```java
@NGetter
String id()
```

**Accessor:** Property getter.

Id.

**Returns:**

id result



---

<a id="m-NDescriptorMailingList-name"></a>

#### name

```java
@NGetter
String name()
```

**Accessor:** Property getter.

Name.

**Returns:**

name result



---

<a id="m-NDescriptorMailingList-subscribe"></a>

#### subscribe

```java
@NGetter
String subscribe()
```

**Accessor:** Property getter.

Subscribe.

**Returns:**

subscribe result



---

<a id="m-NDescriptorMailingList-unsubscribe"></a>

#### unsubscribe

```java
@NGetter
String unsubscribe()
```

**Accessor:** Property getter.

Unsubscribe.

**Returns:**

unsubscribe result



---

<a id="m-NDescriptorMailingList-post"></a>

#### post

```java
@NGetter
String post()
```

**Accessor:** Property getter.

Post.

**Returns:**

post result



---

<a id="m-NDescriptorMailingList-archive"></a>

#### archive

```java
@NGetter
String archive()
```

**Accessor:** Property getter.

Archive.

**Returns:**

archive result



---

<a id="m-NDescriptorMailingList-otherArchives"></a>

#### otherArchives

```java
@NGetter
List<String> otherArchives()
```

**Accessor:** Property getter.

Other archives.

**Returns:**

other archives result



---

<a id="m-NDescriptorMailingList-properties"></a>

#### properties

```java
@NGetter
Map<String,String> properties()
```

**Accessor:** Property getter.

Properties.

**Returns:**

properties result



---

<a id="m-NDescriptorMailingList-comments"></a>

#### comments

```java
@NGetter
String comments()
```

**Accessor:** Property getter.

Comments.

**Returns:**

comments result



---

<a id="m-NDescriptorMailingList-readOnly"></a>

#### readOnly

```java
NDescriptorMailingList readOnly()
```

Read only.

**Returns:**

read only result



---

<a id="m-NDescriptorMailingList-builder"></a>

#### builder

```java
NDescriptorMailingListBuilder builder()
```

Builder.

**Returns:**

builder result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorMailingListBuilder"></a>

## NDescriptorMailingListBuilder

```java
public interface NDescriptorMailingListBuilder extends NDescriptorMailingList
```

NDescriptorMailingListBuilder interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NDescriptorMailingListBuilder` | [id(String)](#m-NDescriptorMailingListBuilder-id) | Id. |
| `NDescriptorMailingListBuilder` | [name(String)](#m-NDescriptorMailingListBuilder-name) | Name. |
| `NDescriptorMailingListBuilder` | [subscribe(String)](#m-NDescriptorMailingListBuilder-subscribe) | Subscribe. |
| `NDescriptorMailingListBuilder` | [unsubscribe(String)](#m-NDescriptorMailingListBuilder-unsubscribe) | Unsubscribe. |
| `NDescriptorMailingListBuilder` | [post(String)](#m-NDescriptorMailingListBuilder-post) | Post. |
| `NDescriptorMailingListBuilder` | [archive(String)](#m-NDescriptorMailingListBuilder-archive) | Archive. |
| `NDescriptorMailingListBuilder` | [otherArchives(List&lt;String&gt;)](#m-NDescriptorMailingListBuilder-otherArchives) | Other archives. |
| `NDescriptorMailingListBuilder` | [properties(Map&lt;String,String&gt;)](#m-NDescriptorMailingListBuilder-properties) | Properties. |
| `NDescriptorMailingListBuilder` | [comments(String)](#m-NDescriptorMailingListBuilder-comments) | Comments. |
| `NDescriptorMailingList` | [build()](#m-NDescriptorMailingListBuilder-build) | Build. |
| `NDescriptorMailingListBuilder` | [copy()](#m-NDescriptorMailingListBuilder-copy) | Copy. |

### Method Details


---

<a id="m-NDescriptorMailingListBuilder-id"></a>

#### id

```java
@NSetter
NDescriptorMailingListBuilder id(String id)
```

**Mutator:** Property setter.

Id.

**Parameters:**

- `id` - id

**Returns:**

id result



---

<a id="m-NDescriptorMailingListBuilder-name"></a>

#### name

```java
@NSetter
NDescriptorMailingListBuilder name(String name)
```

**Mutator:** Property setter.

Name.

**Parameters:**

- `name` - name

**Returns:**

name result



---

<a id="m-NDescriptorMailingListBuilder-subscribe"></a>

#### subscribe

```java
@NSetter
NDescriptorMailingListBuilder subscribe(String subscribe)
```

**Mutator:** Property setter.

Subscribe.

**Parameters:**

- `subscribe` - subscribe

**Returns:**

subscribe result



---

<a id="m-NDescriptorMailingListBuilder-unsubscribe"></a>

#### unsubscribe

```java
@NSetter
NDescriptorMailingListBuilder unsubscribe(String unsubscribe)
```

**Mutator:** Property setter.

Unsubscribe.

**Parameters:**

- `unsubscribe` - unsubscribe

**Returns:**

unsubscribe result



---

<a id="m-NDescriptorMailingListBuilder-post"></a>

#### post

```java
@NSetter
NDescriptorMailingListBuilder post(String post)
```

**Mutator:** Property setter.

Post.

**Parameters:**

- `post` - post

**Returns:**

post result



---

<a id="m-NDescriptorMailingListBuilder-archive"></a>

#### archive

```java
@NSetter
NDescriptorMailingListBuilder archive(String archive)
```

**Mutator:** Property setter.

Archive.

**Parameters:**

- `archive` - archive

**Returns:**

archive result



---

<a id="m-NDescriptorMailingListBuilder-otherArchives"></a>

#### otherArchives

```java
@NSetter
NDescriptorMailingListBuilder otherArchives(List<String> otherArchives)
```

**Mutator:** Property setter.

Other archives.

**Parameters:**

- `otherArchives` - other archives

**Returns:**

other archives result



---

<a id="m-NDescriptorMailingListBuilder-properties"></a>

#### properties

```java
@NSetter
NDescriptorMailingListBuilder properties(Map<String,String> properties)
```

**Mutator:** Property setter.

Properties.

**Parameters:**

- `properties` - properties

**Returns:**

properties result



---

<a id="m-NDescriptorMailingListBuilder-comments"></a>

#### comments

```java
@NSetter
NDescriptorMailingListBuilder comments(String comments)
```

**Mutator:** Property setter.

Comments.

**Parameters:**

- `comments` - comments

**Returns:**

comments result



---

<a id="m-NDescriptorMailingListBuilder-build"></a>

#### build

```java
NDescriptorMailingList build()
```

Build.

**Returns:**

build result



---

<a id="m-NDescriptorMailingListBuilder-copy"></a>

#### copy

```java
NDescriptorMailingListBuilder copy()
```

Copy.

**Returns:**

copy result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorOrganization"></a>

## NDescriptorOrganization

```java
public interface NDescriptorOrganization
```

NDescriptorOrganization interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `String` | [name()](#m-NDescriptorOrganization-name) | Name. |
| `String` | [url()](#m-NDescriptorOrganization-url) | Url. |
| `String` | [comments()](#m-NDescriptorOrganization-comments) | Comments. |
| `String` | [id()](#m-NDescriptorOrganization-id) | Id. |
| `Map&lt;String,String&gt;` | [properties()](#m-NDescriptorOrganization-properties) | Properties. |
| `NDescriptorOrganization` | [readOnly()](#m-NDescriptorOrganization-readOnly) | Read only. |
| `NDescriptorOrganizationBuilder` | [builder()](#m-NDescriptorOrganization-builder) | Builder. |

### Method Details


---

<a id="m-NDescriptorOrganization-name"></a>

#### name

```java
@NGetter
String name()
```

**Accessor:** Property getter.

Name.

**Returns:**

name result



---

<a id="m-NDescriptorOrganization-url"></a>

#### url

```java
@NGetter
String url()
```

**Accessor:** Property getter.

Url.

**Returns:**

url result



---

<a id="m-NDescriptorOrganization-comments"></a>

#### comments

```java
@NGetter
String comments()
```

**Accessor:** Property getter.

Comments.

**Returns:**

comments result



---

<a id="m-NDescriptorOrganization-id"></a>

#### id

```java
@NGetter
String id()
```

**Accessor:** Property getter.

Id.

**Returns:**

id result



---

<a id="m-NDescriptorOrganization-properties"></a>

#### properties

```java
@NGetter
Map<String,String> properties()
```

**Accessor:** Property getter.

Properties.

**Returns:**

properties result



---

<a id="m-NDescriptorOrganization-readOnly"></a>

#### readOnly

```java
NDescriptorOrganization readOnly()
```

Read only.

**Returns:**

read only result



---

<a id="m-NDescriptorOrganization-builder"></a>

#### builder

```java
NDescriptorOrganizationBuilder builder()
```

Builder.

**Returns:**

builder result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorOrganizationBuilder"></a>

## NDescriptorOrganizationBuilder

```java
public interface NDescriptorOrganizationBuilder extends NDescriptorOrganization
```

NDescriptorOrganizationBuilder interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NDescriptorOrganizationBuilder` | [name(String)](#m-NDescriptorOrganizationBuilder-name) | Name. |
| `NDescriptorOrganizationBuilder` | [url(String)](#m-NDescriptorOrganizationBuilder-url) | Url. |
| `NDescriptorOrganizationBuilder` | [comments(String)](#m-NDescriptorOrganizationBuilder-comments) | Comments. |
| `NDescriptorOrganizationBuilder` | [id(String)](#m-NDescriptorOrganizationBuilder-id) | Id. |
| `NDescriptorOrganizationBuilder` | [properties(Map&lt;String,String&gt;)](#m-NDescriptorOrganizationBuilder-properties) | Properties. |
| `NDescriptorOrganization` | [build()](#m-NDescriptorOrganizationBuilder-build) | Build. |
| `NDescriptorOrganizationBuilder` | [copy()](#m-NDescriptorOrganizationBuilder-copy) | Copy. |

### Method Details


---

<a id="m-NDescriptorOrganizationBuilder-name"></a>

#### name

```java
@NSetter
NDescriptorOrganizationBuilder name(String name)
```

**Mutator:** Property setter.

Name.

**Parameters:**

- `name` - name

**Returns:**

name result



---

<a id="m-NDescriptorOrganizationBuilder-url"></a>

#### url

```java
@NSetter
NDescriptorOrganizationBuilder url(String url)
```

**Mutator:** Property setter.

Url.

**Parameters:**

- `url` - url

**Returns:**

url result



---

<a id="m-NDescriptorOrganizationBuilder-comments"></a>

#### comments

```java
@NSetter
NDescriptorOrganizationBuilder comments(String comments)
```

**Mutator:** Property setter.

Comments.

**Parameters:**

- `comments` - comments

**Returns:**

comments result



---

<a id="m-NDescriptorOrganizationBuilder-id"></a>

#### id

```java
@NSetter
NDescriptorOrganizationBuilder id(String id)
```

**Mutator:** Property setter.

Id.

**Parameters:**

- `id` - id

**Returns:**

id result



---

<a id="m-NDescriptorOrganizationBuilder-properties"></a>

#### properties

```java
@NSetter
NDescriptorOrganizationBuilder properties(Map<String,String> properties)
```

**Mutator:** Property setter.

Properties.

**Parameters:**

- `properties` - properties

**Returns:**

properties result



---

<a id="m-NDescriptorOrganizationBuilder-build"></a>

#### build

```java
NDescriptorOrganization build()
```

Build.

**Returns:**

build result



---

<a id="m-NDescriptorOrganizationBuilder-copy"></a>

#### copy

```java
NDescriptorOrganizationBuilder copy()
```

Copy.

**Returns:**

copy result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorParser"></a>

## NDescriptorParser

```java
public interface NDescriptorParser extends NComponent
```

Descriptor Parser

**Author:** thevpc

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NDescriptorParser` | [of()](#m-NDescriptorParser-of) | Creates a new instance. |
| `static NDescriptorParser` | [ofMaven()](#m-NDescriptorParser-ofMaven) | Creates a new instance of maven. |
| `static NDescriptorParser` | [ofManifest()](#m-NDescriptorParser-ofManifest) | Creates a new instance of manifest. |
| `static NDescriptorParser` | [ofNuts()](#m-NDescriptorParser-ofNuts) | Creates a new instance of nuts. |
| `NOptional&lt;NDescriptor&gt;` | [parse(URL)](#m-NDescriptorParser-parse) | parse descriptor. |
| `NOptional&lt;NDescriptor&gt;` | [parse(byte[])](#m-NDescriptorParser-parse) | parse descriptor. |
| `NOptional&lt;NDescriptor&gt;` | [parse(Path)](#m-NDescriptorParser-parse) | parse descriptor. |
| `NOptional&lt;NDescriptor&gt;` | [parse(File)](#m-NDescriptorParser-parse) | parse descriptor. |
| `NOptional&lt;NDescriptor&gt;` | [parse(InputStream)](#m-NDescriptorParser-parse) | parse descriptor. |
| `NOptional&lt;NDescriptor&gt;` | [parse(NPath)](#m-NDescriptorParser-parse) | Parse. |
| `NOptional&lt;NDescriptor&gt;` | [parse(String)](#m-NDescriptorParser-parse) | parse descriptor. |
| `NDescriptorStyle` | [descriptorStyle()](#m-NDescriptorParser-descriptorStyle) | Descriptor style. |
| `NDescriptorParser` | [descriptorStyle(NDescriptorStyle)](#m-NDescriptorParser-descriptorStyle) | Descriptor style. |

### Method Details


---

<a id="m-NDescriptorParser-of"></a>

#### of

```java
static NDescriptorParser of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NDescriptorParser-ofMaven"></a>

#### ofMaven

```java
static NDescriptorParser ofMaven()
```

Creates a new instance of maven.

**Returns:**

of maven result



---

<a id="m-NDescriptorParser-ofManifest"></a>

#### ofManifest

```java
static NDescriptorParser ofManifest()
```

Creates a new instance of manifest.

**Returns:**

of manifest result



---

<a id="m-NDescriptorParser-ofNuts"></a>

#### ofNuts

```java
static NDescriptorParser ofNuts()
```

Creates a new instance of nuts.

**Returns:**

of nuts result



---

<a id="m-NDescriptorParser-parse"></a>

#### parse

```java
NOptional<NDescriptor> parse(URL url)
```

parse descriptor.

**Parameters:**

- `url` - URL to parse

**Returns:**

parsed Descriptor



---

<a id="m-NDescriptorParser-parse"></a>

#### parse

```java
NOptional<NDescriptor> parse(byte[] bytes)
```

parse descriptor.

**Parameters:**

- `bytes` - value to parse

**Returns:**

parsed Descriptor



---

<a id="m-NDescriptorParser-parse"></a>

#### parse

```java
NOptional<NDescriptor> parse(Path path)
```

parse descriptor.

**Parameters:**

- `path` - path to parse

**Returns:**

parsed Descriptor



---

<a id="m-NDescriptorParser-parse"></a>

#### parse

```java
NOptional<NDescriptor> parse(File file)
```

parse descriptor.

**Parameters:**

- `file` - file to parse

**Returns:**

parsed Descriptor



---

<a id="m-NDescriptorParser-parse"></a>

#### parse

```java
NOptional<NDescriptor> parse(InputStream stream)
```

parse descriptor.

**Parameters:**

- `stream` - stream to parse

**Returns:**

parsed Descriptor



---

<a id="m-NDescriptorParser-parse"></a>

#### parse

```java
NOptional<NDescriptor> parse(NPath path)
```

Parse.

**Parameters:**

- `path` - path

**Returns:**

parse result



---

<a id="m-NDescriptorParser-parse"></a>

#### parse

```java
NOptional<NDescriptor> parse(String descriptorString)
```

parse descriptor.

**Parameters:**

- `descriptorString` - string to parse

**Returns:**

parsed Descriptor



---

<a id="m-NDescriptorParser-descriptorStyle"></a>

#### descriptorStyle

```java
@NGetter
NDescriptorStyle descriptorStyle()
```

**Accessor:** Property getter.

Descriptor style.

**Returns:**

descriptor style result



---

<a id="m-NDescriptorParser-descriptorStyle"></a>

#### descriptorStyle

```java
@NSetter
NDescriptorParser descriptorStyle(NDescriptorStyle descriptorStyle)
```

**Mutator:** Property setter.

Descriptor style.

**Parameters:**

- `descriptorStyle` - descriptor style

**Returns:**

descriptor style result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorProperty"></a>

## NDescriptorProperty

```java
public interface NDescriptorProperty extends NBlankable
```

Descriptor Property

**Author:** thevpc

**Since:** 0.8.3

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `String` | [name()](#m-NDescriptorProperty-name) | property name |
| `NLiteral` | [value()](#m-NDescriptorProperty-value) | property value |
| `NEnvCondition` | [condition()](#m-NDescriptorProperty-condition) | property condition |
| `NDescriptorPropertyBuilder` | [builder()](#m-NDescriptorProperty-builder) | creates a populated builder from this instance |

### Method Details


---

<a id="m-NDescriptorProperty-name"></a>

#### name

```java
@NGetter
String name()
```

**Accessor:** Property getter.

property name

**Returns:**

property name



---

<a id="m-NDescriptorProperty-value"></a>

#### value

```java
@NGetter
NLiteral value()
```

**Accessor:** Property getter.

property value

**Returns:**

property value



---

<a id="m-NDescriptorProperty-condition"></a>

#### condition

```java
@NGetter
NEnvCondition condition()
```

**Accessor:** Property getter.

property condition

**Returns:**

property condition



---

<a id="m-NDescriptorProperty-builder"></a>

#### builder

```java
NDescriptorPropertyBuilder builder()
```

creates a populated builder from this instance

**Returns:**

a populated builder from this instance



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorPropertyBuilder"></a>

## NDescriptorPropertyBuilder

```java
public interface NDescriptorPropertyBuilder extends NBlankable
```

NDescriptorPropertyBuilder interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `String` | [name()](#m-NDescriptorPropertyBuilder-name) | property name |
| `NLiteral` | [value()](#m-NDescriptorPropertyBuilder-value) | property value |
| `NEnvConditionBuilder` | [condition()](#m-NDescriptorPropertyBuilder-condition) | property condition |
| `NDescriptorPropertyBuilder` | [condition(NEnvCondition)](#m-NDescriptorPropertyBuilder-condition) | Condition. |
| `NDescriptorPropertyBuilder` | [condition(NEnvConditionBuilder)](#m-NDescriptorPropertyBuilder-condition) | Condition. |
| `NDescriptorPropertyBuilder` | [name(String)](#m-NDescriptorPropertyBuilder-name) | Name. |
| `NDescriptorPropertyBuilder` | [value(String)](#m-NDescriptorPropertyBuilder-value) | Value. |
| `NDescriptorPropertyBuilder` | [copyFrom(NDescriptorProperty)](#m-NDescriptorPropertyBuilder-copyFrom) | Copy from. |
| `NDescriptorPropertyBuilder` | [copyFrom(NDescriptorPropertyBuilder)](#m-NDescriptorPropertyBuilder-copyFrom) | Copy from. |
| `NDescriptorProperty` | [build()](#m-NDescriptorPropertyBuilder-build) | Build. |
| `NDescriptorPropertyBuilder` | [copy()](#m-NDescriptorPropertyBuilder-copy) | Copy. |

### Method Details


---

<a id="m-NDescriptorPropertyBuilder-name"></a>

#### name

```java
@NGetter
String name()
```

**Accessor:** Property getter.

property name

**Returns:**

property name



---

<a id="m-NDescriptorPropertyBuilder-value"></a>

#### value

```java
@NGetter
NLiteral value()
```

**Accessor:** Property getter.

property value

**Returns:**

property value



---

<a id="m-NDescriptorPropertyBuilder-condition"></a>

#### condition

```java
@NGetter
NEnvConditionBuilder condition()
```

**Accessor:** Property getter.

property condition

**Returns:**

property condition



---

<a id="m-NDescriptorPropertyBuilder-condition"></a>

#### condition

```java
@NSetter
NDescriptorPropertyBuilder condition(NEnvCondition condition)
```

**Mutator:** Property setter.

Condition.

**Parameters:**

- `condition` - condition

**Returns:**

condition result



---

<a id="m-NDescriptorPropertyBuilder-condition"></a>

#### condition

```java
NDescriptorPropertyBuilder condition(NEnvConditionBuilder condition)
```

Condition.

**Parameters:**

- `condition` - condition

**Returns:**

condition result



---

<a id="m-NDescriptorPropertyBuilder-name"></a>

#### name

```java
@NSetter
NDescriptorPropertyBuilder name(String name)
```

**Mutator:** Property setter.

Name.

**Parameters:**

- `name` - name

**Returns:**

name result



---

<a id="m-NDescriptorPropertyBuilder-value"></a>

#### value

```java
@NSetter
NDescriptorPropertyBuilder value(String value)
```

**Mutator:** Property setter.

Value.

**Parameters:**

- `value` - value

**Returns:**

value result



---

<a id="m-NDescriptorPropertyBuilder-copyFrom"></a>

#### copyFrom

```java
NDescriptorPropertyBuilder copyFrom(NDescriptorProperty value)
```

Copy from.

**Parameters:**

- `value` - value

**Returns:**

copy from result



---

<a id="m-NDescriptorPropertyBuilder-copyFrom"></a>

#### copyFrom

```java
NDescriptorPropertyBuilder copyFrom(NDescriptorPropertyBuilder value)
```

Copy from.

**Parameters:**

- `value` - value

**Returns:**

copy from result



---

<a id="m-NDescriptorPropertyBuilder-build"></a>

#### build

```java
NDescriptorProperty build()
```

Build.

**Returns:**

build result



---

<a id="m-NDescriptorPropertyBuilder-copy"></a>

#### copy

```java
NDescriptorPropertyBuilder copy()
```

Copy.

**Returns:**

copy result



---

<a id="cls-net-thevpc-nuts-artifact-NDescriptorStyle"></a>

## NDescriptorStyle

```java
public enum NDescriptorStyle implements NEnum
```

Descriptor Style

**Author:** thevpc

**Since:** 0.8.2

### Enum Constants

| Constant | Description |
| :--- | :--- |
| `MAVEN` | pom.xml format |
| `NUTS` | nuts json format |
| `MANIFEST` | MANIFEST.MF format |

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private final String` | `id` | lower-cased identifier for the enum entry |

### Constructors

#### `NDescriptorStyle()`

```java
NDescriptorStyle()
```

N descriptor style.


### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;NDescriptorStyle&gt;` | [parse(String)](#m-NDescriptorStyle-parse) | Parse. |
| `String` | [id()](#m-NDescriptorStyle-id) | lower cased identifier. |

### Method Details


---

<a id="m-NDescriptorStyle-parse"></a>

#### parse

```java
public static NOptional<NDescriptorStyle> parse(String value)
```

Parse.

**Parameters:**

- `value` - value

**Returns:**

parse result



---

<a id="m-NDescriptorStyle-id"></a>

#### id

```java
public String id()
```

lower cased identifier.

**Returns:**

lower cased identifier



---

<a id="cls-net-thevpc-nuts-artifact-NEnvCondition"></a>

## NEnvCondition

```java
public interface NEnvCondition extends NBlankable
```

Nuts environment condition, used to check against the current environment

**Since:** 0.8.3

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `NEnvCondition` | `BLANK` |  |

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `List&lt;String&gt;` | [profiles()](#m-NEnvCondition-profiles) | supported profiles (such as maven profiles) |
| `List&lt;String&gt;` | [arch()](#m-NEnvCondition-arch) | supported arch list. |
| `List&lt;String&gt;` | [os()](#m-NEnvCondition-os) | supported operating systems. |
| `List&lt;String&gt;` | [osDist()](#m-NEnvCondition-osDist) | supported operating system distributions (mostly for linux systems). |
| `List&lt;String&gt;` | [platform()](#m-NEnvCondition-platform) | supported platforms (java, dotnet, ...). |
| `List&lt;String&gt;` | [desktopEnvironment()](#m-NEnvCondition-desktopEnvironment) | supported desktop environments (gnome, kde, none, ...). |
| `NEnvConditionBuilder` | [builder()](#m-NEnvCondition-builder) | create builder from this instance |
| `Map&lt;String,String&gt;` | [properties()](#m-NEnvCondition-properties) | return env properties |
| `Map&lt;String,String&gt;` | [toMap()](#m-NEnvCondition-toMap) | Converts to map. |

### Method Details


---

<a id="m-NEnvCondition-profiles"></a>

#### profiles

```java
@NGetter
List<String> profiles()
```

**Accessor:** Property getter.

supported profiles (such as maven profiles)

**Returns:**

supported supported profiles



---

<a id="m-NEnvCondition-arch"></a>

#### arch

```java
@NGetter
List<String> arch()
```

**Accessor:** Property getter.

supported arch list. if empty, all arch are supported (for example for java, all arch are supported).

**Returns:**

supported arch list



---

<a id="m-NEnvCondition-os"></a>

#### os

```java
@NGetter
List<String> os()
```

**Accessor:** Property getter.

supported operating systems. if empty, all oses are supported (for example for java, all arch are supported).

**Returns:**

supported oses



---

<a id="m-NEnvCondition-osDist"></a>

#### osDist

```java
@NGetter
List<String> osDist()
```

**Accessor:** Property getter.

supported operating system distributions (mostly for linux systems). if empty, all distributions are supported.

**Returns:**

supported operating system distributions



---

<a id="m-NEnvCondition-platform"></a>

#### platform

```java
@NGetter
List<String> platform()
```

**Accessor:** Property getter.

supported platforms (java, dotnet, ...). if empty platform is not relevant.
This is helpful to bind application to a jdk version for instance (in that case platform may be in the form java#8 for instance)

**Returns:**

supported platforms



---

<a id="m-NEnvCondition-desktopEnvironment"></a>

#### desktopEnvironment

```java
@NGetter
List<String> desktopEnvironment()
```

**Accessor:** Property getter.

supported desktop environments (gnome, kde, none, ...). if empty desktop environment is not relevant.
This is helpful to bind application to a specific environment

**Returns:**

supported platforms



---

<a id="m-NEnvCondition-builder"></a>

#### builder

```java
NEnvConditionBuilder builder()
```

create builder from this instance

**Returns:**

builder copy of this instance



---

<a id="m-NEnvCondition-properties"></a>

#### properties

```java
@NGetter
Map<String,String> properties()
```

**Accessor:** Property getter.

return env properties

**Returns:**

env properties



---

<a id="m-NEnvCondition-toMap"></a>

#### toMap

```java
Map<String,String> toMap()
```

Converts to map.

**Returns:**

to map result



---

<a id="cls-net-thevpc-nuts-artifact-NEnvConditionBuilder"></a>

## NEnvConditionBuilder

```java
public interface NEnvConditionBuilder extends NComponent, NBlankable
```

NEnvConditionBuilder interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NEnvConditionBuilder` | [of()](#m-NEnvConditionBuilder-of) | Creates a new instance. |
| `NEnvConditionBuilder` | [arch(List&lt;String&gt;)](#m-NEnvConditionBuilder-arch) | Arch. |
| `NEnvConditionBuilder` | [os(List&lt;String&gt;)](#m-NEnvConditionBuilder-os) | Os. |
| `NEnvConditionBuilder` | [osDist(List&lt;String&gt;)](#m-NEnvConditionBuilder-osDist) | Os dist. |
| `NEnvConditionBuilder` | [platform(List&lt;String&gt;)](#m-NEnvConditionBuilder-platform) | Platform. |
| `NEnvConditionBuilder` | [desktopEnvironment(List&lt;String&gt;)](#m-NEnvConditionBuilder-desktopEnvironment) | Desktop environment. |
| `NEnvConditionBuilder` | [profile(List&lt;String&gt;)](#m-NEnvConditionBuilder-profile) | Profile. |
| `NEnvConditionBuilder` | [copyFrom(NEnvCondition)](#m-NEnvConditionBuilder-copyFrom) | Copy from. |
| `NEnvConditionBuilder` | [copyFrom(NEnvConditionBuilder)](#m-NEnvConditionBuilder-copyFrom) | Copy from. |
| `NEnvConditionBuilder` | [clear()](#m-NEnvConditionBuilder-clear) | Clear. |
| `NEnvCondition` | [build()](#m-NEnvConditionBuilder-build) | Build. |
| `NEnvConditionBuilder` | [copy()](#m-NEnvConditionBuilder-copy) | Copy. |
| `NEnvConditionBuilder` | [setProperties(Map&lt;String,String&gt;)](#m-NEnvConditionBuilder-setProperties) | Sets the properties. |
| `NEnvConditionBuilder` | [addProperties(Map&lt;String,String&gt;)](#m-NEnvConditionBuilder-addProperties) | Adds the specified properties. |
| `NEnvConditionBuilder` | [addProperty(String, String)](#m-NEnvConditionBuilder-addProperty) | Adds the specified property. |
| `NEnvConditionBuilder` | [addDesktopEnvironment(String)](#m-NEnvConditionBuilder-addDesktopEnvironment) | Adds the specified desktop environment. |
| `NEnvConditionBuilder` | [addDesktopEnvironments(String)](#m-NEnvConditionBuilder-addDesktopEnvironments) | Adds the specified desktop environments. |
| `NEnvConditionBuilder` | [addArchs(String)](#m-NEnvConditionBuilder-addArchs) | Adds the specified archs. |
| `NEnvConditionBuilder` | [addArchs(String)](#m-NEnvConditionBuilder-addArchs) | Adds the specified archs. |
| `NEnvConditionBuilder` | [addOs(String)](#m-NEnvConditionBuilder-addOs) | Adds the specified os. |
| `NEnvConditionBuilder` | [addOses(String)](#m-NEnvConditionBuilder-addOses) | Adds the specified oses. |
| `NEnvConditionBuilder` | [addOsDist(String)](#m-NEnvConditionBuilder-addOsDist) | Adds the specified os dist. |
| `NEnvConditionBuilder` | [addOsDists(String)](#m-NEnvConditionBuilder-addOsDists) | Adds the specified os dists. |
| `NEnvConditionBuilder` | [addPlatform(String)](#m-NEnvConditionBuilder-addPlatform) | Adds the specified platform. |
| `NEnvConditionBuilder` | [addPlatforms(String)](#m-NEnvConditionBuilder-addPlatforms) | Adds the specified platforms. |
| `NEnvConditionBuilder` | [addProfile(String)](#m-NEnvConditionBuilder-addProfile) | Adds the specified profile. |
| `NEnvConditionBuilder` | [addProfiles(String)](#m-NEnvConditionBuilder-addProfiles) | Adds the specified profiles. |
| `NEnvConditionBuilder` | [and(NEnvCondition)](#m-NEnvConditionBuilder-and) | And. |
| `NEnvConditionBuilder` | [or(NEnvCondition)](#m-NEnvConditionBuilder-or) | Or. |
| `List&lt;String&gt;` | [profiles()](#m-NEnvConditionBuilder-profiles) |  |
| `List&lt;String&gt;` | [arch()](#m-NEnvConditionBuilder-arch) | supported arch list. |
| `List&lt;String&gt;` | [os()](#m-NEnvConditionBuilder-os) | supported operating systems. |
| `List&lt;String&gt;` | [osDist()](#m-NEnvConditionBuilder-osDist) | supported operating system distributions (mostly for linux systems). |
| `List&lt;String&gt;` | [platform()](#m-NEnvConditionBuilder-platform) | supported platforms (java, dotnet, ...). |
| `List&lt;String&gt;` | [desktopEnvironment()](#m-NEnvConditionBuilder-desktopEnvironment) | supported desktop environments (gnome, kde, none, ...). |
| `Map&lt;String,String&gt;` | [properties()](#m-NEnvConditionBuilder-properties) | return env properties |
| `Map&lt;String,String&gt;` | [toMap()](#m-NEnvConditionBuilder-toMap) | Converts to map. |

### Method Details


---

<a id="m-NEnvConditionBuilder-of"></a>

#### of

```java
static NEnvConditionBuilder of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NEnvConditionBuilder-arch"></a>

#### arch

```java
@NSetter
NEnvConditionBuilder arch(List<String> arch)
```

**Mutator:** Property setter.

Arch.

**Parameters:**

- `arch` - arch

**Returns:**

arch result



---

<a id="m-NEnvConditionBuilder-os"></a>

#### os

```java
@NSetter
NEnvConditionBuilder os(List<String> os)
```

**Mutator:** Property setter.

Os.

**Parameters:**

- `os` - os

**Returns:**

os result



---

<a id="m-NEnvConditionBuilder-osDist"></a>

#### osDist

```java
@NSetter
NEnvConditionBuilder osDist(List<String> osDist)
```

**Mutator:** Property setter.

Os dist.

**Parameters:**

- `osDist` - os dist

**Returns:**

os dist result



---

<a id="m-NEnvConditionBuilder-platform"></a>

#### platform

```java
@NSetter
NEnvConditionBuilder platform(List<String> platform)
```

**Mutator:** Property setter.

Platform.

**Parameters:**

- `platform` - platform

**Returns:**

platform result



---

<a id="m-NEnvConditionBuilder-desktopEnvironment"></a>

#### desktopEnvironment

```java
@NSetter
NEnvConditionBuilder desktopEnvironment(List<String> desktopEnvironment)
```

**Mutator:** Property setter.

Desktop environment.

**Parameters:**

- `desktopEnvironment` - desktop environment

**Returns:**

desktop environment result



---

<a id="m-NEnvConditionBuilder-profile"></a>

#### profile

```java
@NSetter
NEnvConditionBuilder profile(List<String> profiles)
```

**Mutator:** Property setter.

Profile.

**Parameters:**

- `profiles` - profiles

**Returns:**

profile result



---

<a id="m-NEnvConditionBuilder-copyFrom"></a>

#### copyFrom

```java
NEnvConditionBuilder copyFrom(NEnvCondition other)
```

Copy from.

**Parameters:**

- `other` - other

**Returns:**

copy from result



---

<a id="m-NEnvConditionBuilder-copyFrom"></a>

#### copyFrom

```java
NEnvConditionBuilder copyFrom(NEnvConditionBuilder other)
```

Copy from.

**Parameters:**

- `other` - other

**Returns:**

copy from result



---

<a id="m-NEnvConditionBuilder-clear"></a>

#### clear

```java
NEnvConditionBuilder clear()
```

Clear.

**Returns:**

clear result



---

<a id="m-NEnvConditionBuilder-build"></a>

#### build

```java
NEnvCondition build()
```

Build.

**Returns:**

build result



---

<a id="m-NEnvConditionBuilder-copy"></a>

#### copy

```java
NEnvConditionBuilder copy()
```

Copy.

**Returns:**

copy result



---

<a id="m-NEnvConditionBuilder-setProperties"></a>

#### setProperties

```java
NEnvConditionBuilder setProperties(Map<String,String> properties)
```

Sets the properties.

**Parameters:**

- `properties` - properties

**Returns:**

set properties result



---

<a id="m-NEnvConditionBuilder-addProperties"></a>

#### addProperties

```java
NEnvConditionBuilder addProperties(Map<String,String> properties)
```

Adds the specified properties.

**Parameters:**

- `properties` - properties

**Returns:**

add properties result



---

<a id="m-NEnvConditionBuilder-addProperty"></a>

#### addProperty

```java
NEnvConditionBuilder addProperty(String key, String value)
```

Adds the specified property.

**Parameters:**

- `key` - key
- `value` - value

**Returns:**

add property result



---

<a id="m-NEnvConditionBuilder-addDesktopEnvironment"></a>

#### addDesktopEnvironment

```java
NEnvConditionBuilder addDesktopEnvironment(String value)
```

Adds the specified desktop environment.

**Parameters:**

- `value` - value

**Returns:**

add desktop environment result



---

<a id="m-NEnvConditionBuilder-addDesktopEnvironments"></a>

#### addDesktopEnvironments

```java
NEnvConditionBuilder addDesktopEnvironments(String values)
```

Adds the specified desktop environments.

**Parameters:**

- `values` - values

**Returns:**

add desktop environments result



---

<a id="m-NEnvConditionBuilder-addArchs"></a>

#### addArchs

```java
NEnvConditionBuilder addArchs(String value)
```

Adds the specified archs.

**Parameters:**

- `value` - value

**Returns:**

add archs result



---

<a id="m-NEnvConditionBuilder-addArchs"></a>

#### addArchs

```java
NEnvConditionBuilder addArchs(String values)
```

Adds the specified archs.

**Parameters:**

- `values` - values

**Returns:**

add archs result



---

<a id="m-NEnvConditionBuilder-addOs"></a>

#### addOs

```java
NEnvConditionBuilder addOs(String value)
```

Adds the specified os.

**Parameters:**

- `value` - value

**Returns:**

add os result



---

<a id="m-NEnvConditionBuilder-addOses"></a>

#### addOses

```java
NEnvConditionBuilder addOses(String values)
```

Adds the specified oses.

**Parameters:**

- `values` - values

**Returns:**

add oses result



---

<a id="m-NEnvConditionBuilder-addOsDist"></a>

#### addOsDist

```java
NEnvConditionBuilder addOsDist(String value)
```

Adds the specified os dist.

**Parameters:**

- `value` - value

**Returns:**

add os dist result



---

<a id="m-NEnvConditionBuilder-addOsDists"></a>

#### addOsDists

```java
NEnvConditionBuilder addOsDists(String values)
```

Adds the specified os dists.

**Parameters:**

- `values` - values

**Returns:**

add os dists result



---

<a id="m-NEnvConditionBuilder-addPlatform"></a>

#### addPlatform

```java
NEnvConditionBuilder addPlatform(String value)
```

Adds the specified platform.

**Parameters:**

- `value` - value

**Returns:**

add platform result



---

<a id="m-NEnvConditionBuilder-addPlatforms"></a>

#### addPlatforms

```java
NEnvConditionBuilder addPlatforms(String values)
```

Adds the specified platforms.

**Parameters:**

- `values` - values

**Returns:**

add platforms result



---

<a id="m-NEnvConditionBuilder-addProfile"></a>

#### addProfile

```java
NEnvConditionBuilder addProfile(String value)
```

Adds the specified profile.

**Parameters:**

- `value` - value

**Returns:**

add profile result



---

<a id="m-NEnvConditionBuilder-addProfiles"></a>

#### addProfiles

```java
NEnvConditionBuilder addProfiles(String values)
```

Adds the specified profiles.

**Parameters:**

- `values` - values

**Returns:**

add profiles result



---

<a id="m-NEnvConditionBuilder-and"></a>

#### and

```java
NEnvConditionBuilder and(NEnvCondition other)
```

And.

**Parameters:**

- `other` - other

**Returns:**

and result



---

<a id="m-NEnvConditionBuilder-or"></a>

#### or

```java
NEnvConditionBuilder or(NEnvCondition other)
```

Or.

**Parameters:**

- `other` - other

**Returns:**

or result



---

<a id="m-NEnvConditionBuilder-profiles"></a>

#### profiles

```java
@NGetter
List<String> profiles()
```

**Accessor:** Property getter.



---

<a id="m-NEnvConditionBuilder-arch"></a>

#### arch

```java
@NGetter
List<String> arch()
```

**Accessor:** Property getter.

supported arch list. if empty, all arch are supported (for example for java, all arch are supported).

**Returns:**

supported arch list



---

<a id="m-NEnvConditionBuilder-os"></a>

#### os

```java
@NGetter
List<String> os()
```

**Accessor:** Property getter.

supported operating systems. if empty, all oses are supported (for example for java, all arch are supported).

**Returns:**

supported oses



---

<a id="m-NEnvConditionBuilder-osDist"></a>

#### osDist

```java
@NGetter
List<String> osDist()
```

**Accessor:** Property getter.

supported operating system distributions (mostly for linux systems). if empty, all distributions are supported.

**Returns:**

supported operating system distributions



---

<a id="m-NEnvConditionBuilder-platform"></a>

#### platform

```java
@NGetter
List<String> platform()
```

**Accessor:** Property getter.

supported platforms (java, dotnet, ...). if empty platform is not relevant.
This is helpful to bind application to a jdk version for instance (in that case platform may be in the form java#8 for instance)

**Returns:**

supported platforms



---

<a id="m-NEnvConditionBuilder-desktopEnvironment"></a>

#### desktopEnvironment

```java
@NGetter
List<String> desktopEnvironment()
```

**Accessor:** Property getter.

supported desktop environments (gnome, kde, none, ...). if empty desktop environment is not relevant.
This is helpful to bind application to a specific environment

**Returns:**

supported platforms



---

<a id="m-NEnvConditionBuilder-properties"></a>

#### properties

```java
@NGetter
Map<String,String> properties()
```

**Accessor:** Property getter.

return env properties

**Returns:**

env properties



---

<a id="m-NEnvConditionBuilder-toMap"></a>

#### toMap

```java
Map<String,String> toMap()
```

Converts to map.

**Returns:**

to map result



---

<a id="cls-net-thevpc-nuts-artifact-NFilters"></a>

## NFilters

```java
public interface NFilters extends NComponent
```

Filters helper

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NFilters` | [of()](#m-NFilters-of) | Creates a new instance. |
| `T` | [nonnull(Class&lt;T&gt;, NFilter)](#m-NFilters-nonnull) | Nonnull. |
| `T` | [always(Class&lt;T&gt;)](#m-NFilters-always) | Always. |
| `T` | [never(Class&lt;T&gt;)](#m-NFilters-never) | Never. |
| `T` | [all(Class&lt;T&gt;, NFilter)](#m-NFilters-all) | All. |
| `T` | [all(NFilter)](#m-NFilters-all) | All. |
| `T` | [any(Class&lt;T&gt;, NFilter)](#m-NFilters-any) | Any. |
| `T` | [not(NFilter)](#m-NFilters-not) | Not. |
| `T` | [not(Class&lt;T&gt;, NFilter)](#m-NFilters-not) | Not. |
| `T` | [any(NFilter)](#m-NFilters-any) | Any. |
| `T` | [none(Class&lt;T&gt;, NFilter)](#m-NFilters-none) | None. |
| `T` | [none(NFilter)](#m-NFilters-none) | None. |
| `T` | [to(Class&lt;T&gt;, NFilter)](#m-NFilters-to) | convert filter to toFilterInterface or throw error |
| `T` | [as(Class&lt;T&gt;, NFilter)](#m-NFilters-as) | convert filter to toFilterInterface or throw error |
| `Class&lt;? extends NFilter&gt;` | [detectType(NFilter)](#m-NFilters-detectType) | Detect type. |

### Method Details


---

<a id="m-NFilters-of"></a>

#### of

```java
static NFilters of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NFilters-nonnull"></a>

#### nonnull

```java
<T extends NFilter> T nonnull(Class<T> type, NFilter filter)
```

Nonnull.

**Parameters:**

- `type` - type
- `filter` - filter

**Returns:**

nonnull result



---

<a id="m-NFilters-always"></a>

#### always

```java
<T extends NFilter> T always(Class<T> type)
```

Always.

**Parameters:**

- `type` - type

**Returns:**

always result



---

<a id="m-NFilters-never"></a>

#### never

```java
<T extends NFilter> T never(Class<T> type)
```

Never.

**Parameters:**

- `type` - type

**Returns:**

never result



---

<a id="m-NFilters-all"></a>

#### all

```java
<T extends NFilter> T all(Class<T> type, NFilter others)
```

All.

**Parameters:**

- `type` - type
- `others` - others

**Returns:**

all result



---

<a id="m-NFilters-all"></a>

#### all

```java
<T extends NFilter> T all(NFilter others)
```

All.

**Parameters:**

- `others` - others

**Returns:**

all result



---

<a id="m-NFilters-any"></a>

#### any

```java
<T extends NFilter> T any(Class<T> type, NFilter others)
```

Any.

**Parameters:**

- `type` - type
- `others` - others

**Returns:**

any result



---

<a id="m-NFilters-not"></a>

#### not

```java
<T extends NFilter> T not(NFilter other)
```

Not.

**Parameters:**

- `other` - other

**Returns:**

not result



---

<a id="m-NFilters-not"></a>

#### not

```java
<T extends NFilter> T not(Class<T> type, NFilter other)
```

Not.

**Parameters:**

- `type` - type
- `other` - other

**Returns:**

not result



---

<a id="m-NFilters-any"></a>

#### any

```java
<T extends NFilter> T any(NFilter others)
```

Any.

**Parameters:**

- `others` - others

**Returns:**

any result



---

<a id="m-NFilters-none"></a>

#### none

```java
<T extends NFilter> T none(Class<T> type, NFilter others)
```

None.

**Parameters:**

- `type` - type
- `others` - others

**Returns:**

none result



---

<a id="m-NFilters-none"></a>

#### none

```java
<T extends NFilter> T none(NFilter others)
```

None.

**Parameters:**

- `others` - others

**Returns:**

none result



---

<a id="m-NFilters-to"></a>

#### to

```java
<T extends NFilter> T to(Class<T> toFilterInterface, NFilter filter)
```

convert `filter` to `toFilterInterface` or throw error

**Parameters:**

- `toFilterInterface` - one of the valid interfaces of
                         `NutsFilter`
- `filter` - filter instance

**Returns:**

instance of `T` converted from `filter` or error



---

<a id="m-NFilters-as"></a>

#### as

```java
<T extends NFilter> T as(Class<T> toFilterInterface, NFilter filter)
```

convert `filter` to `toFilterInterface` or throw error

**Parameters:**

- `toFilterInterface` - one of the valid interfaces of
                         `NutsFilter`
- `filter` - filter instance

**Returns:**

instance of `T` converted from `filter` or null



---

<a id="m-NFilters-detectType"></a>

#### detectType

```java
Class<? extends NFilter> detectType(NFilter nFilter)
```

Detect type.

**Parameters:**

- `nFilter` - n filter

**Returns:**

detect type result



---

<a id="cls-net-thevpc-nuts-artifact-NId"></a>

## NId

```java
public interface NId extends Comparable<NId>, NBlankable
```

Immutable Artifact id information.

**Author:** thevpc

**Since:** 0.1.0

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `NId` | `API_ID` |  |
| `NId` | `RUNTIME_ID` |  |
| `Pattern` | `PATTERN` |  |
| `Pattern` | `GROUP_ID_PATTERN` |  |
| `Pattern` | `ARTIFACT_ID_PATTERN` |  |
| `NId` | `BLANK` |  |

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;List&lt;NId&gt;&gt;` | [getList(String)](#m-NId-getList) | Returns the list. |
| `static NOptional&lt;Set&lt;NId&gt;&gt;` | [getSet(String)](#m-NId-getSet) | Returns the set. |
| `static NOptional&lt;NId&gt;` | [get(String, String)](#m-NId-get) | Returns the get. |
| `static NOptional&lt;NId&gt;` | [get(String, String, NVersion)](#m-NId-get) | Returns the get. |
| `static NOptional&lt;NId&gt;` | [get(String, String, String)](#m-NId-get) | Returns the get. |
| `static NOptional&lt;NId&gt;` | [getApi(NVersion)](#m-NId-getApi) | Returns the api. |
| `static NOptional&lt;NId&gt;` | [getRuntime(NVersion)](#m-NId-getRuntime) | Returns the runtime. |
| `static NOptional&lt;NId&gt;` | [getApi(String)](#m-NId-getApi) | Returns the api. |
| `static NOptional&lt;NId&gt;` | [getRuntime(String)](#m-NId-getRuntime) | Returns the runtime. |
| `static NOptional&lt;NId&gt;` | [get(String)](#m-NId-get) | Returns the get. |
| `static NOptional&lt;NId&gt;` | [getForClass(Class&lt;?&gt;)](#m-NId-getForClass) | Returns the for class. |
| `static NOptional&lt;NId&gt;` | [getForPath(NPath)](#m-NId-getForPath) | Returns the for path. |
| `static List&lt;NId&gt;` | [ofList(String)](#m-NId-ofList) | Creates a new instance of list. |
| `static Set&lt;NId&gt;` | [ofSet(String)](#m-NId-ofSet) | Creates a new instance of set. |
| `static NId` | [of(String, String)](#m-NId-of) | Creates a new instance. |
| `static NId` | [of(String, String, NVersion)](#m-NId-of) | Creates a new instance. |
| `static NId` | [of(String, String, String)](#m-NId-of) | Creates a new instance. |
| `static NId` | [ofApi(NVersion)](#m-NId-ofApi) | Creates a new instance of api. |
| `static NId` | [ofRuntime(NVersion)](#m-NId-ofRuntime) | Creates a new instance of runtime. |
| `static NId` | [ofApi(String)](#m-NId-ofApi) | Creates a new instance of api. |
| `static NId` | [ofRuntime(String)](#m-NId-ofRuntime) | Creates a new instance of runtime. |
| `static NId` | [of(String)](#m-NId-of) | Creates a new instance. |
| `static NId` | [ofClass(Class&lt;?&gt;)](#m-NId-ofClass) | Creates a new instance of class. |
| `static NId` | [ofPath(NPath)](#m-NId-ofPath) | Creates a new path instance. |
| `static List&lt;NId&gt;` | [findByClass(Class&lt;?&gt;)](#m-NId-findByClass) | Finds the find by class. |
| `static List&lt;NId&gt;` | [findByPath(NPath)](#m-NId-findByPath) | Finds the find by path. |
| `boolean` | [equalsShortId(NId)](#m-NId-equalsShortId) | true if other has exact short name than this |
| `boolean` | [equalsLongId(NId)](#m-NId-equalsLongId) | true if other has exact long name than this |
| `boolean` | [isLongId()](#m-NId-isLongId) | true if this id is a long name |
| `boolean` | [isShortId()](#m-NId-isShortId) | Checks if this is short. |
| `String` | [face()](#m-NId-face) | id face define is a release file type selector of the id. |
| `NEnvCondition` | [condition()](#m-NId-condition) | os supported by the artifact |
| `String` | [propertiesQuery()](#m-NId-propertiesQuery) | properties in the url query form |
| `Map&lt;String,String&gt;` | [properties()](#m-NId-properties) | properties as map. |
| `String` | [repository()](#m-NId-repository) | artifact repository (usually repository name or id) |
| `String` | [groupId()](#m-NId-groupId) | artifact group which identifies uniquely projects and group of projects. |
| `String` | [fullName()](#m-NId-fullName) | return a string representation of this id. |
| `String` | [longName()](#m-NId-longName) | return a string concatenation of group, name and version, ignoring repository, and queryMap values. |
| `String` | [shortName()](#m-NId-shortName) | returns a string concatenation of group and name (':' separated) ignoring version,repository, and queryMap values. |
| `NId` | [shortId()](#m-NId-shortId) | return a new instance of NutsId defining only group and name ignoring version,repository, and queryMap values. |
| `NId` | [sharedId()](#m-NId-sharedId) | Shared id. |
| `NId` | [longId()](#m-NId-longId) | return a new instance of NutsId defining only group, name, version and classifier if available, ignoring repository, and queryMap values. |
| `String` | [artifactId()](#m-NId-artifactId) | return name part of this id |
| `String` | [classifier()](#m-NId-classifier) | tag used to distinguish between different artifacts that were built from the same source code |
| `String` | [packaging()](#m-NId-packaging) | package packaging type |
| `NVersion` | [version()](#m-NId-version) | artifact version (never null) |
| `NIdBuilder` | [builder()](#m-NId-builder) | create a builder (mutable id) based on this id |
| `NDependency` | [toDependency()](#m-NId-toDependency) | Converts to dependency. |
| `NIdFilter` | [toFilter()](#m-NId-toFilter) | filter accepted any id with the defined version or greater |
| `NId` | [toAtLeast()](#m-NId-toAtLeast) | when the current version is a single value version X , returns ],X] version that guarantees backward compatibility in all other cases returns the... |
| `NId` | [toAtMost()](#m-NId-toAtMost) | when the current version is a single value version X , returns [X,[ version that guarantees forward compatibility in all other cases returns the current version |
| `boolean` | [isNull()](#m-NId-isNull) | Checks if this is null. |
| `boolean` | [isBlank()](#m-NId-isBlank) | Checks if this is blank. |
| `String` | [mavenFolder()](#m-NId-mavenFolder) | maven path as [groupId]/[artifactId]/[version] |
| `String` | [getMavenFileName(String)](#m-NId-getMavenFileName) | Returns the maven file name. |
| `String` | [getMavenPath(String)](#m-NId-getMavenPath) | Returns the maven path. |

### Method Details


---

<a id="m-NId-getList"></a>

#### getList

```java
static NOptional<List<NId>> getList(String value)
```

Returns the list.

**Parameters:**

- `value` - value

**Returns:**

get list result



---

<a id="m-NId-getSet"></a>

#### getSet

```java
static NOptional<Set<NId>> getSet(String value)
```

Returns the set.

**Parameters:**

- `value` - value

**Returns:**

get set result



---

<a id="m-NId-get"></a>

#### get

```java
static NOptional<NId> get(String groupId, String artifactId)
```

Returns the get.

**Parameters:**

- `groupId` - group id
- `artifactId` - artifact id

**Returns:**

get result



---

<a id="m-NId-get"></a>

#### get

```java
static NOptional<NId> get(String groupId, String artifactId, NVersion version)
```

Returns the get.

**Parameters:**

- `groupId` - group id
- `artifactId` - artifact id
- `version` - version

**Returns:**

get result



---

<a id="m-NId-get"></a>

#### get

```java
static NOptional<NId> get(String groupId, String artifactId, String version)
```

Returns the get.

**Parameters:**

- `groupId` - group id
- `artifactId` - artifact id
- `version` - version

**Returns:**

get result



---

<a id="m-NId-getApi"></a>

#### getApi

```java
static NOptional<NId> getApi(NVersion version)
```

Returns the api.

**Parameters:**

- `version` - version

**Returns:**

get api result



---

<a id="m-NId-getRuntime"></a>

#### getRuntime

```java
static NOptional<NId> getRuntime(NVersion version)
```

Returns the runtime.

**Parameters:**

- `version` - version

**Returns:**

get runtime result



---

<a id="m-NId-getApi"></a>

#### getApi

```java
static NOptional<NId> getApi(String version)
```

Returns the api.

**Parameters:**

- `version` - version

**Returns:**

get api result



---

<a id="m-NId-getRuntime"></a>

#### getRuntime

```java
static NOptional<NId> getRuntime(String version)
```

Returns the runtime.

**Parameters:**

- `version` - version

**Returns:**

get runtime result



---

<a id="m-NId-get"></a>

#### get

```java
static NOptional<NId> get(String value)
```

Returns the get.

**Parameters:**

- `value` - value

**Returns:**

get result



---

<a id="m-NId-getForClass"></a>

#### getForClass

```java
static NOptional<NId> getForClass(Class<?> value)
```

Returns the for class.

**Parameters:**

- `value` - value

**Returns:**

get for class result



---

<a id="m-NId-getForPath"></a>

#### getForPath

```java
static NOptional<NId> getForPath(NPath value)
```

Returns the for path.

**Parameters:**

- `value` - value

**Returns:**

get for path result



---

<a id="m-NId-ofList"></a>

#### ofList

```java
static List<NId> ofList(String value)
```

Creates a new instance of list.

**Parameters:**

- `value` - value

**Returns:**

of list result



---

<a id="m-NId-ofSet"></a>

#### ofSet

```java
static Set<NId> ofSet(String value)
```

Creates a new instance of set.

**Parameters:**

- `value` - value

**Returns:**

of set result



---

<a id="m-NId-of"></a>

#### of

```java
static NId of(String groupId, String artifactId)
```

Creates a new instance.

**Parameters:**

- `groupId` - group id
- `artifactId` - artifact id

**Returns:**

of result



---

<a id="m-NId-of"></a>

#### of

```java
static NId of(String groupId, String artifactId, NVersion version)
```

Creates a new instance.

**Parameters:**

- `groupId` - group id
- `artifactId` - artifact id
- `version` - version

**Returns:**

of result



---

<a id="m-NId-of"></a>

#### of

```java
static NId of(String groupId, String artifactId, String version)
```

Creates a new instance.

**Parameters:**

- `groupId` - group id
- `artifactId` - artifact id
- `version` - version

**Returns:**

of result



---

<a id="m-NId-ofApi"></a>

#### ofApi

```java
static NId ofApi(NVersion version)
```

Creates a new instance of api.

**Parameters:**

- `version` - version

**Returns:**

of api result



---

<a id="m-NId-ofRuntime"></a>

#### ofRuntime

```java
static NId ofRuntime(NVersion version)
```

Creates a new instance of runtime.

**Parameters:**

- `version` - version

**Returns:**

of runtime result



---

<a id="m-NId-ofApi"></a>

#### ofApi

```java
static NId ofApi(String version)
```

Creates a new instance of api.

**Parameters:**

- `version` - version

**Returns:**

of api result



---

<a id="m-NId-ofRuntime"></a>

#### ofRuntime

```java
static NId ofRuntime(String version)
```

Creates a new instance of runtime.

**Parameters:**

- `version` - version

**Returns:**

of runtime result



---

<a id="m-NId-of"></a>

#### of

```java
static NId of(String value)
```

Creates a new instance.

**Parameters:**

- `value` - value

**Returns:**

of result



---

<a id="m-NId-ofClass"></a>

#### ofClass

```java
static NId ofClass(Class<?> value)
```

Creates a new instance of class.

**Parameters:**

- `value` - value

**Returns:**

of class result



---

<a id="m-NId-ofPath"></a>

#### ofPath

```java
static NId ofPath(NPath value)
```

Creates a new path instance.

**Parameters:**

- `value` - value

**Returns:**

of path result



---

<a id="m-NId-findByClass"></a>

#### findByClass

```java
static List<NId> findByClass(Class<?> value)
```

Finds the find by class.

**Parameters:**

- `value` - value

**Returns:**

find by class result



---

<a id="m-NId-findByPath"></a>

#### findByPath

```java
static List<NId> findByPath(NPath value)
```

Finds the find by path.

**Parameters:**

- `value` - value

**Returns:**

find by path result



---

<a id="m-NId-equalsShortId"></a>

#### equalsShortId

```java
boolean equalsShortId(NId other)
```

true if other has exact short name than `this`

**Parameters:**

- `other` - other id

**Returns:**

true if other has exact short name than `this`



---

<a id="m-NId-equalsLongId"></a>

#### equalsLongId

```java
boolean equalsLongId(NId other)
```

true if other has exact long name than `this`

**Parameters:**

- `other` - other id

**Returns:**

true if other has exact long name than `this`



---

<a id="m-NId-isLongId"></a>

#### isLongId

```java
@NGetter
boolean isLongId()
```

**Accessor:** Property getter.

true if this id is a long name

**Returns:**

true if this id is a long name



---

<a id="m-NId-isShortId"></a>

#### isShortId

```java
@NGetter
boolean isShortId()
```

**Accessor:** Property getter.

Checks if this is short. id.

**Returns:**

is short id result



---

<a id="m-NId-face"></a>

#### face

```java
@NGetter
String face()
```

**Accessor:** Property getter.

id face define is a release file type selector of the id.
It helps discriminating content (jar) from descriptor, from other (hash,...)
files released for the very same  artifact.

**Returns:**

id face selector



---

<a id="m-NId-condition"></a>

#### condition

```java
@NGetter
NEnvCondition condition()
```

**Accessor:** Property getter.

os supported by the artifact

**Returns:**

os supported by the artifact



---

<a id="m-NId-propertiesQuery"></a>

#### propertiesQuery

```java
@NGetter
String propertiesQuery()
```

**Accessor:** Property getter.

properties in the url query form

**Returns:**

properties in the url query form.



---

<a id="m-NId-properties"></a>

#### properties

```java
@NGetter
Map<String,String> properties()
```

**Accessor:** Property getter.

properties as map.

**Returns:**

properties as map.



---

<a id="m-NId-repository"></a>

#### repository

```java
@NGetter
String repository()
```

**Accessor:** Property getter.

artifact repository (usually repository name or id)

**Returns:**

artifact repository (usually repository name or id)



---

<a id="m-NId-groupId"></a>

#### groupId

```java
@NGetter
String groupId()
```

**Accessor:** Property getter.

artifact group which identifies uniquely projects and group of projects.

**Returns:**

artifact group which identifies uniquely projects and group of projects.



---

<a id="m-NId-fullName"></a>

#### fullName

```java
@NGetter
String fullName()
```

**Accessor:** Property getter.

return a string representation of this id. All of group, name, version,
repository, queryMap values are printed. This method is equivalent to
`Object#toString()`

**Returns:**

string representation of this id



---

<a id="m-NId-longName"></a>

#### longName

```java
@NGetter
String longName()
```

**Accessor:** Property getter.

return a string concatenation of group, name and version,
ignoring repository, and queryMap values. An example of long name is
`my-group:my-artifact:classifier#my-version`

**Returns:**

group id, artifact id and version only id instance



---

<a id="m-NId-shortName"></a>

#### shortName

```java
@NGetter
String shortName()
```

**Accessor:** Property getter.

returns a string concatenation of group and name (':' separated) ignoring
version,repository, and queryMap values. In group is empty or null, name
is returned. Ann null values are stripped to "" An example of simple name
is `my-group:my-artifact`

**Returns:**

group id and artifact id



---

<a id="m-NId-shortId"></a>

#### shortId

```java
@NGetter
NId shortId()
```

**Accessor:** Property getter.

return a new instance of NutsId defining only group and name ignoring
version,repository, and queryMap values.

**Returns:**

group and name only Id instance



---

<a id="m-NId-sharedId"></a>

#### sharedId

```java
@NGetter
NId sharedId()
```

**Accessor:** Property getter.

Shared id.

**Returns:**

shared id result



---

<a id="m-NId-longId"></a>

#### longId

```java
@NGetter
NId longId()
```

**Accessor:** Property getter.

return a new instance of NutsId defining only group, name, version and classifier if available,
ignoring repository, and queryMap values.

**Returns:**

group, name and version only Id instance



---

<a id="m-NId-artifactId"></a>

#### artifactId

```java
@NGetter
String artifactId()
```

**Accessor:** Property getter.

return name part of this id

**Returns:**

return name part of this id



---

<a id="m-NId-classifier"></a>

#### classifier

```java
@NGetter
String classifier()
```

**Accessor:** Property getter.

tag used to distinguish between different artifacts that were built from the same source code

**Returns:**

tag used to distinguish between different artifacts that were built from the same source code



---

<a id="m-NId-packaging"></a>

#### packaging

```java
@NGetter
String packaging()
```

**Accessor:** Property getter.

package packaging type

**Returns:**

packaging



---

<a id="m-NId-version"></a>

#### version

```java
@NGetter
NVersion version()
```

**Accessor:** Property getter.

artifact version (never null)

**Returns:**

artifact version (never null)



---

<a id="m-NId-builder"></a>

#### builder

```java
NIdBuilder builder()
```

create a builder (mutable id) based on this id

**Returns:**

a new instance of builder (mutable id) based on this id



---

<a id="m-NId-toDependency"></a>

#### toDependency

```java
NDependency toDependency()
```

Converts to dependency.

**Returns:**

to dependency result



---

<a id="m-NId-toFilter"></a>

#### toFilter

```java
NIdFilter toFilter()
```

filter accepted any id with the defined version or greater

**Returns:**

filter accepted any id with the defined version or greater



---

<a id="m-NId-toAtLeast"></a>

#### toAtLeast

```java
NId toAtLeast()
```

when the current version is a single value version X , returns ],X] version that guarantees backward compatibility
in all other cases returns the current version

**Returns:**

when the current version is a single value version X , returns ],X] version that guarantees backward compatibility in all other cases returns the current version



---

<a id="m-NId-toAtMost"></a>

#### toAtMost

```java
NId toAtMost()
```

when the current version is a single value version X , returns [X,[ version that guarantees forward compatibility
in all other cases returns the current version

**Returns:**

when the current version is a single value version X , returns [X,[ version that guarantees forward compatibility in all other cases returns the current version



---

<a id="m-NId-isNull"></a>

#### isNull

```java
boolean isNull()
```

Checks if this is null.

**Returns:**

is null result



---

<a id="m-NId-isBlank"></a>

#### isBlank

```java
boolean isBlank()
```

Checks if this is blank.

**Returns:**

is blank result



---

<a id="m-NId-mavenFolder"></a>

#### mavenFolder

```java
@NGetter
String mavenFolder()
```

**Accessor:** Property getter.

maven path as [groupId]/[artifactId]/[version]

**Returns:**

maven path



---

<a id="m-NId-getMavenFileName"></a>

#### getMavenFileName

```java
String getMavenFileName(String extension)
```

Returns the maven file name.

**Parameters:**

- `extension` - extension

**Returns:**

get maven file name result



---

<a id="m-NId-getMavenPath"></a>

#### getMavenPath

```java
String getMavenPath(String extension)
```

Returns the maven path.

**Parameters:**

- `extension` - extension

**Returns:**

get maven path result



---

<a id="cls-net-thevpc-nuts-artifact-NIdBuilder"></a>

## NIdBuilder

```java
public interface NIdBuilder extends NBlankable, NComponent, Serializable
```

Mutable Artifact id information used to create instance of [NId](#cls-NId)

**Author:** thevpc

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NIdBuilder` | [of(NId)](#m-NIdBuilder-of) | Creates a new instance. |
| `static NIdBuilder` | [of(String, String)](#m-NIdBuilder-of) | Creates a new instance. |
| `static NIdBuilder` | [of()](#m-NIdBuilder-of) | Creates a new instance. |
| `NIdBuilder` | [face(String)](#m-NIdBuilder-face) | update id face which defines is a release file type selector |
| `NIdBuilder` | [condition(NEnvCondition)](#m-NIdBuilder-condition) | Condition. |
| `NIdBuilder` | [condition(NEnvConditionBuilder)](#m-NIdBuilder-condition) | Condition. |
| `NIdBuilder` | [classifier(String)](#m-NIdBuilder-classifier) | update classifier |
| `NIdBuilder` | [packaging(String)](#m-NIdBuilder-packaging) | update packaging |
| `NIdBuilder` | [faceContent()](#m-NIdBuilder-faceContent) | equivalent to setFace(NutsConstants.QueryFaces.CONTENT) |
| `NIdBuilder` | [faceDescriptor()](#m-NIdBuilder-faceDescriptor) | equivalent to setFace(NutsConstants.QueryFaces.DESCRIPTOR) |
| `NIdBuilder` | [setProperty(String, String)](#m-NIdBuilder-setProperty) | update property. |
| `NIdBuilder` | [properties(Map&lt;String,String&gt;)](#m-NIdBuilder-properties) | update all properties property. |
| `NIdBuilder` | [propertiesQuery(String)](#m-NIdBuilder-propertiesQuery) | update all properties property. |
| `NIdBuilder` | [clearProperties()](#m-NIdBuilder-clearProperties) | clear all properties |
| `NIdBuilder` | [repository(String)](#m-NIdBuilder-repository) | update repository |
| `NIdBuilder` | [groupId(String)](#m-NIdBuilder-groupId) | update groupId |
| `NIdBuilder` | [artifactId(String)](#m-NIdBuilder-artifactId) | update artifactId |
| `NIdBuilder` | [version(String)](#m-NIdBuilder-version) | update version |
| `NIdBuilder` | [version(NVersion)](#m-NIdBuilder-version) | update setVersion |
| `NIdBuilder` | [copyFrom(NId)](#m-NIdBuilder-copyFrom) | update all arguments |
| `NIdBuilder` | [copyFrom(NIdBuilder)](#m-NIdBuilder-copyFrom) | update all arguments |
| `NIdBuilder` | [clear()](#m-NIdBuilder-clear) | clear this instance (set null/default all properties) |
| `NId` | [build()](#m-NIdBuilder-build) | create new instance of NId initialized with this builder values. |
| `boolean` | [equalsShortId(NId)](#m-NIdBuilder-equalsShortId) | true if other has exact short name than this |
| `boolean` | [equalsLongId(NId)](#m-NIdBuilder-equalsLongId) | true if other has exact long name than this |
| `boolean` | [isLongId()](#m-NIdBuilder-isLongId) | true if this id is a long name |
| `boolean` | [isShortId()](#m-NIdBuilder-isShortId) | Checks if this is short. |
| `String` | [face()](#m-NIdBuilder-face) | id face define is a release file type selector of the id. |
| `NEnvConditionBuilder` | [condition()](#m-NIdBuilder-condition) | os supported by the artifact |
| `String` | [propertiesQuery()](#m-NIdBuilder-propertiesQuery) | properties in the url query form |
| `Map&lt;String,String&gt;` | [properties()](#m-NIdBuilder-properties) | properties as map. |
| `String` | [repository()](#m-NIdBuilder-repository) | artifact repository (usually repository name or id) |
| `String` | [groupId()](#m-NIdBuilder-groupId) | artifact group which identifies uniquely projects and group of projects. |
| `String` | [fullName()](#m-NIdBuilder-fullName) | return a string representation of this id. |
| `String` | [longName()](#m-NIdBuilder-longName) | return a string concatenation of group, name and version, ignoring repository, and queryMap values. |
| `String` | [shortName()](#m-NIdBuilder-shortName) | returns a string concatenation of group and name (':' separated) ignoring version,repository, and queryMap values. |
| `NId` | [shortId()](#m-NIdBuilder-shortId) | return a new instance of NutsId defining only group and name ignoring version,repository, and queryMap values. |
| `NId` | [longId()](#m-NIdBuilder-longId) | return a new instance of NutsId defining only group, name, version and classifier if available, ignoring repository, and queryMap values. |
| `String` | [artifactId()](#m-NIdBuilder-artifactId) | return name part of this id |
| `String` | [classifier()](#m-NIdBuilder-classifier) | tag used to distinguish between different artifacts that were built from the same source code |
| `String` | [packaging()](#m-NIdBuilder-packaging) | package packaging type |
| `NVersion` | [version()](#m-NIdBuilder-version) | artifact version (never null) |
| `NIdBuilder` | [builder()](#m-NIdBuilder-builder) | create a builder (mutable id) based on this id |
| `NDependency` | [toDependency()](#m-NIdBuilder-toDependency) | Converts to dependency. |
| `NIdFilter` | [toFilter()](#m-NIdBuilder-toFilter) | Converts to filter. |
| `NId` | [compatNewer()](#m-NIdBuilder-compatNewer) | when the current version is a single value version X , returns ],X] version that guarantees backward compatibility in all other cases returns the... |
| `NId` | [compatOlder()](#m-NIdBuilder-compatOlder) | when the current version is a single value version X , returns [X,[ version that guarantees forward compatibility in all other cases returns the current version |
| `boolean` | [isNull()](#m-NIdBuilder-isNull) | Checks if this is null. |
| `boolean` | [isBlank()](#m-NIdBuilder-isBlank) | Checks if this is blank. |

### Method Details


---

<a id="m-NIdBuilder-of"></a>

#### of

```java
static NIdBuilder of(NId id)
```

Creates a new instance.

**Parameters:**

- `id` - id

**Returns:**

of result



---

<a id="m-NIdBuilder-of"></a>

#### of

```java
static NIdBuilder of(String groupId, String artifactId)
```

Creates a new instance.

**Parameters:**

- `groupId` - group id
- `artifactId` - artifact id

**Returns:**

of result



---

<a id="m-NIdBuilder-of"></a>

#### of

```java
static NIdBuilder of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NIdBuilder-face"></a>

#### face

```java
@NSetter
NIdBuilder face(String value)
```

**Mutator:** Property setter.

update id face which defines is a release file type selector

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-condition"></a>

#### condition

```java
@NSetter
NIdBuilder condition(NEnvCondition c)
```

**Mutator:** Property setter.

Condition.

**Parameters:**

- `c` - c

**Returns:**

condition result



---

<a id="m-NIdBuilder-condition"></a>

#### condition

```java
@NSetter
NIdBuilder condition(NEnvConditionBuilder c)
```

**Mutator:** Property setter.

Condition.

**Parameters:**

- `c` - c

**Returns:**

condition result



---

<a id="m-NIdBuilder-classifier"></a>

#### classifier

```java
@NSetter
NIdBuilder classifier(String value)
```

**Mutator:** Property setter.

update classifier

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-packaging"></a>

#### packaging

```java
@NSetter
NIdBuilder packaging(String packaging)
```

**Mutator:** Property setter.

update packaging

**Parameters:**

- `packaging` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-faceContent"></a>

#### faceContent

```java
@NSetter
NIdBuilder faceContent()
```

**Mutator:** Property setter.

equivalent to `setFace(NutsConstants.QueryFaces.CONTENT)`

**Returns:**

this instance



---

<a id="m-NIdBuilder-faceDescriptor"></a>

#### faceDescriptor

```java
NIdBuilder faceDescriptor()
```

equivalent to `setFace(NutsConstants.QueryFaces.DESCRIPTOR)`

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-setProperty"></a>

#### setProperty

```java
NIdBuilder setProperty(String property, String value)
```

update property.
When `value` is null, property will be removed.

**Parameters:**

- `property` - name
- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-properties"></a>

#### properties

```java
@NSetter
NIdBuilder properties(Map<String,String> queryMap)
```

**Mutator:** Property setter.

update all properties property.

**Parameters:**

- `queryMap` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-propertiesQuery"></a>

#### propertiesQuery

```java
@NSetter
NIdBuilder propertiesQuery(String query)
```

**Mutator:** Property setter.

update all properties property.

**Parameters:**

- `query` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-clearProperties"></a>

#### clearProperties

```java
NIdBuilder clearProperties()
```

clear all properties

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-repository"></a>

#### repository

```java
@NSetter
NIdBuilder repository(String value)
```

**Mutator:** Property setter.

update repository

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-groupId"></a>

#### groupId

```java
@NSetter
NIdBuilder groupId(String value)
```

**Mutator:** Property setter.

update groupId

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-artifactId"></a>

#### artifactId

```java
@NSetter
NIdBuilder artifactId(String value)
```

**Mutator:** Property setter.

update artifactId

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-version"></a>

#### version

```java
NIdBuilder version(String value)
```

update version

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-version"></a>

#### version

```java
@NSetter
NIdBuilder version(NVersion value)
```

**Mutator:** Property setter.

update setVersion

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-copyFrom"></a>

#### copyFrom

```java
NIdBuilder copyFrom(NId id)
```

update all arguments

**Parameters:**

- `id` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-copyFrom"></a>

#### copyFrom

```java
NIdBuilder copyFrom(NIdBuilder id)
```

update all arguments

**Parameters:**

- `id` - new value

**Returns:**

`this` instance



---

<a id="m-NIdBuilder-clear"></a>

#### clear

```java
NIdBuilder clear()
```

clear this instance (set null/default all properties)

**Returns:**

`this instance`



---

<a id="m-NIdBuilder-build"></a>

#### build

```java
NId build()
```

create new instance of [NId](#cls-NId) initialized with this builder values.

**Returns:**

new instance of [NId](#cls-NId) initialized with this builder values.



---

<a id="m-NIdBuilder-equalsShortId"></a>

#### equalsShortId

```java
boolean equalsShortId(NId other)
```

true if other has exact short name than `this`

**Parameters:**

- `other` - other id

**Returns:**

true if other has exact short name than `this`



---

<a id="m-NIdBuilder-equalsLongId"></a>

#### equalsLongId

```java
boolean equalsLongId(NId other)
```

true if other has exact long name than `this`

**Parameters:**

- `other` - other id

**Returns:**

true if other has exact long name than `this`



---

<a id="m-NIdBuilder-isLongId"></a>

#### isLongId

```java
@NGetter
boolean isLongId()
```

**Accessor:** Property getter.

true if this id is a long name

**Returns:**

true if this id is a long name



---

<a id="m-NIdBuilder-isShortId"></a>

#### isShortId

```java
@NGetter
boolean isShortId()
```

**Accessor:** Property getter.

Checks if this is short. id.

**Returns:**

is short id result



---

<a id="m-NIdBuilder-face"></a>

#### face

```java
@NGetter
String face()
```

**Accessor:** Property getter.

id face define is a release file type selector of the id.
It helps discriminating content (jar) from descriptor, from other (hash,...)
files released for the very same  artifact.

**Returns:**

id face selector



---

<a id="m-NIdBuilder-condition"></a>

#### condition

```java
@NGetter
NEnvConditionBuilder condition()
```

**Accessor:** Property getter.

os supported by the artifact

**Returns:**

os supported by the artifact



---

<a id="m-NIdBuilder-propertiesQuery"></a>

#### propertiesQuery

```java
@NGetter
String propertiesQuery()
```

**Accessor:** Property getter.

properties in the url query form

**Returns:**

properties in the url query form.



---

<a id="m-NIdBuilder-properties"></a>

#### properties

```java
@NGetter
Map<String,String> properties()
```

**Accessor:** Property getter.

properties as map.

**Returns:**

properties as map.



---

<a id="m-NIdBuilder-repository"></a>

#### repository

```java
@NGetter
String repository()
```

**Accessor:** Property getter.

artifact repository (usually repository name or id)

**Returns:**

artifact repository (usually repository name or id)



---

<a id="m-NIdBuilder-groupId"></a>

#### groupId

```java
@NGetter
String groupId()
```

**Accessor:** Property getter.

artifact group which identifies uniquely projects and group of projects.

**Returns:**

artifact group which identifies uniquely projects and group of projects.



---

<a id="m-NIdBuilder-fullName"></a>

#### fullName

```java
@NGetter
String fullName()
```

**Accessor:** Property getter.

return a string representation of this id. All of group, name, version,
repository, queryMap values are printed. This method is equivalent to
`Object#toString()`

**Returns:**

string representation of this id



---

<a id="m-NIdBuilder-longName"></a>

#### longName

```java
@NGetter
String longName()
```

**Accessor:** Property getter.

return a string concatenation of group, name and version,
ignoring repository, and queryMap values. An example of long name is
`my-group:my-artifact#my-version?alt`

**Returns:**

group id, artifact id and version only Id instance



---

<a id="m-NIdBuilder-shortName"></a>

#### shortName

```java
@NGetter
String shortName()
```

**Accessor:** Property getter.

returns a string concatenation of group and name (':' separated) ignoring
version,repository, and queryMap values. In group is empty or null, name
is returned. Ann null values are stripped to "" An example of simple name
is `my-group:my-artifact`

**Returns:**

group id and artifact id



---

<a id="m-NIdBuilder-shortId"></a>

#### shortId

```java
@NGetter
NId shortId()
```

**Accessor:** Property getter.

return a new instance of NutsId defining only group and name ignoring
version,repository, and queryMap values.

**Returns:**

group and name only Id instance



---

<a id="m-NIdBuilder-longId"></a>

#### longId

```java
@NGetter
NId longId()
```

**Accessor:** Property getter.

return a new instance of NutsId defining only group, name, version and classifier if available,
ignoring repository, and queryMap values.

**Returns:**

group, name and version only Id instance



---

<a id="m-NIdBuilder-artifactId"></a>

#### artifactId

```java
@NGetter
String artifactId()
```

**Accessor:** Property getter.

return name part of this id

**Returns:**

return name part of this id



---

<a id="m-NIdBuilder-classifier"></a>

#### classifier

```java
@NGetter
String classifier()
```

**Accessor:** Property getter.

tag used to distinguish between different artifacts that were built from the same source code

**Returns:**

tag used to distinguish between different artifacts that were built from the same source code



---

<a id="m-NIdBuilder-packaging"></a>

#### packaging

```java
@NGetter
String packaging()
```

**Accessor:** Property getter.

package packaging type

**Returns:**

packaging



---

<a id="m-NIdBuilder-version"></a>

#### version

```java
@NGetter
NVersion version()
```

**Accessor:** Property getter.

artifact version (never null)

**Returns:**

artifact version (never null)



---

<a id="m-NIdBuilder-builder"></a>

#### builder

```java
NIdBuilder builder()
```

create a builder (mutable id) based on this id

**Returns:**

a new instance of builder (mutable id) based on this id



---

<a id="m-NIdBuilder-toDependency"></a>

#### toDependency

```java
NDependency toDependency()
```

Converts to dependency.

**Returns:**

to dependency result



---

<a id="m-NIdBuilder-toFilter"></a>

#### toFilter

```java
NIdFilter toFilter()
```

Converts to filter.

**Returns:**

to filter result



---

<a id="m-NIdBuilder-compatNewer"></a>

#### compatNewer

```java
NId compatNewer()
```

when the current version is a single value version X , returns ],X] version that guarantees backward compatibility
in all other cases returns the current version

**Returns:**

when the current version is a single value version X , returns ],X] version that guarantees backward compatibility in all other cases returns the current version



---

<a id="m-NIdBuilder-compatOlder"></a>

#### compatOlder

```java
NId compatOlder()
```

when the current version is a single value version X , returns [X,[ version that guarantees forward compatibility
in all other cases returns the current version

**Returns:**

when the current version is a single value version X , returns [X,[ version that guarantees forward compatibility in all other cases returns the current version



---

<a id="m-NIdBuilder-isNull"></a>

#### isNull

```java
boolean isNull()
```

Checks if this is null.

**Returns:**

is null result



---

<a id="m-NIdBuilder-isBlank"></a>

#### isBlank

```java
boolean isBlank()
```

Checks if this is blank.

**Returns:**

is blank result



---

<a id="cls-net-thevpc-nuts-artifact-NIdFilter"></a>

## NIdFilter

```java
public interface NIdFilter extends NFilter
```

Class for filtering Artifact Ids

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NIdFilter` | [ofNonnull(NFilter)](#m-NIdFilter-ofNonnull) | Creates a new instance of nonnull. |
| `static NIdFilter` | [ofAlways()](#m-NIdFilter-ofAlways) | Creates a new instance of always. |
| `static NIdFilter` | [ofNever()](#m-NIdFilter-ofNever) | Creates a new instance of never. |
| `static NIdFilter` | [ofAll(NFilter)](#m-NIdFilter-ofAll) | Creates a new instance of all. |
| `static NIdFilter` | [ofAny(NFilter)](#m-NIdFilter-ofAny) | Creates a new instance of any. |
| `static NIdFilter` | [ofNot(NFilter)](#m-NIdFilter-ofNot) | Creates a new instance of not. |
| `static NIdFilter` | [ofNone(NFilter)](#m-NIdFilter-ofNone) | Creates a new instance of none. |
| `static NIdFilter` | [ofFrom(NFilter)](#m-NIdFilter-ofFrom) | Creates a new instance of from. |
| `static NIdFilter` | [ofAs(NFilter)](#m-NIdFilter-ofAs) | Creates a new instance of as. |
| `static NIdFilter` | [of(String)](#m-NIdFilter-of) | Creates a new instance. |
| `static NIdFilter` | [ofValue(NId)](#m-NIdFilter-ofValue) | Creates a new instance of value. |
| `static NIdFilter` | [ofDefaultVersion(Boolean)](#m-NIdFilter-ofDefaultVersion) | Creates a new instance of default. |
| `static NIdFilter` | [ofName(String)](#m-NIdFilter-ofName) | Creates a new instance of name. |
| `boolean` | [acceptId(NId)](#m-NIdFilter-acceptId) | return true when the id is to be accepted |
| `NIdFilter` | [or(NIdFilter)](#m-NIdFilter-or) | Or. |
| `NIdFilter` | [and(NIdFilter)](#m-NIdFilter-and) | And. |
| `NIdFilter` | [neg()](#m-NIdFilter-neg) | Neg. |

### Method Details


---

<a id="m-NIdFilter-ofNonnull"></a>

#### ofNonnull

```java
static NIdFilter ofNonnull(NFilter filter)
```

Creates a new instance of nonnull.

**Parameters:**

- `filter` - filter

**Returns:**

of nonnull result



---

<a id="m-NIdFilter-ofAlways"></a>

#### ofAlways

```java
static NIdFilter ofAlways()
```

Creates a new instance of always.

**Returns:**

of always result



---

<a id="m-NIdFilter-ofNever"></a>

#### ofNever

```java
static NIdFilter ofNever()
```

Creates a new instance of never.

**Returns:**

of never result



---

<a id="m-NIdFilter-ofAll"></a>

#### ofAll

```java
static NIdFilter ofAll(NFilter others)
```

Creates a new instance of all.

**Parameters:**

- `others` - others

**Returns:**

of all result



---

<a id="m-NIdFilter-ofAny"></a>

#### ofAny

```java
static NIdFilter ofAny(NFilter others)
```

Creates a new instance of any.

**Parameters:**

- `others` - others

**Returns:**

of any result



---

<a id="m-NIdFilter-ofNot"></a>

#### ofNot

```java
static NIdFilter ofNot(NFilter other)
```

Creates a new instance of not.

**Parameters:**

- `other` - other

**Returns:**

of not result



---

<a id="m-NIdFilter-ofNone"></a>

#### ofNone

```java
static NIdFilter ofNone(NFilter others)
```

Creates a new instance of none.

**Parameters:**

- `others` - others

**Returns:**

of none result



---

<a id="m-NIdFilter-ofFrom"></a>

#### ofFrom

```java
static NIdFilter ofFrom(NFilter a)
```

Creates a new instance of from.

**Parameters:**

- `a` - a

**Returns:**

of from result



---

<a id="m-NIdFilter-ofAs"></a>

#### ofAs

```java
static NIdFilter ofAs(NFilter a)
```

Creates a new instance of as.

**Parameters:**

- `a` - a

**Returns:**

of as result



---

<a id="m-NIdFilter-of"></a>

#### of

```java
static NIdFilter of(String expression)
```

Creates a new instance.

**Parameters:**

- `expression` - expression

**Returns:**

of result



---

<a id="m-NIdFilter-ofValue"></a>

#### ofValue

```java
static NIdFilter ofValue(NId id)
```

Creates a new instance of value.

**Parameters:**

- `id` - id

**Returns:**

of value result



---

<a id="m-NIdFilter-ofDefaultVersion"></a>

#### ofDefaultVersion

```java
static NIdFilter ofDefaultVersion(Boolean defaultVersion)
```

Creates a new instance of default. version.

**Parameters:**

- `defaultVersion` - default version

**Returns:**

of default version result



---

<a id="m-NIdFilter-ofName"></a>

#### ofName

```java
static NIdFilter ofName(String names)
```

Creates a new instance of name.

**Parameters:**

- `names` - names

**Returns:**

of name result



---

<a id="m-NIdFilter-acceptId"></a>

#### acceptId

```java
boolean acceptId(NId id)
```

return true when the id is to be accepted

**Parameters:**

- `id` - id to check

**Returns:**

true when the id is to be accepted



---

<a id="m-NIdFilter-or"></a>

#### or

```java
NIdFilter or(NIdFilter other)
```

Or.

**Parameters:**

- `other` - other

**Returns:**

or result



---

<a id="m-NIdFilter-and"></a>

#### and

```java
NIdFilter and(NIdFilter other)
```

And.

**Parameters:**

- `other` - other

**Returns:**

and result



---

<a id="m-NIdFilter-neg"></a>

#### neg

```java
@Override
NIdFilter neg()
```

Neg.

**Returns:**

neg result



---

<a id="cls-net-thevpc-nuts-artifact-NIdLocation"></a>

## NIdLocation

```java
public class NIdLocation implements NBlankable
```

This class is used in [NDescriptor](#cls-NDescriptor) to describe
locations/mirrors to download artifact content instead of the
regular location.

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private final String` | `url` |  |
| `private final String` | `region` |  |
| `private final String` | `classifier` |  |

### Constructors

#### `NIdLocation(String url, String region, String classifier)`

```java
public NIdLocation(String url, String region, String classifier)
```

N id location.

**Parameters:**

- `url` - url
- `region` - region
- `classifier` - classifier

**Returns:**

n id location result


### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `String` | [url()](#m-NIdLocation-url) | location url of the artifact content |
| `String` | [region()](#m-NIdLocation-region) | location (geographic) region that may be used to select the most effective mirror |
| `String` | [classifier()](#m-NIdLocation-classifier) | classifier for the artifact |
| `int` | [hashCode()](#m-NIdLocation-hashCode) |  |
| `boolean` | [equals(Object)](#m-NIdLocation-equals) |  |
| `String` | [toString()](#m-NIdLocation-toString) |  |
| `boolean` | [isBlank()](#m-NIdLocation-isBlank) | true if blank |

### Method Details


---

<a id="m-NIdLocation-url"></a>

#### url

```java
public String url()
```

location url of the artifact content

**Returns:**

location url of the artifact content



---

<a id="m-NIdLocation-region"></a>

#### region

```java
public String region()
```

location (geographic) region that may be used to select
the most effective mirror

**Returns:**

location (geographic) region that may be used to select the most effective mirror



---

<a id="m-NIdLocation-classifier"></a>

#### classifier

```java
public String classifier()
```

classifier for the artifact

**Returns:**

classifier for the artifact



---

<a id="m-NIdLocation-hashCode"></a>

#### hashCode

```java
@Override
public int hashCode()
```



---

<a id="m-NIdLocation-equals"></a>

#### equals

```java
@Override
public boolean equals(Object o)
```



---

<a id="m-NIdLocation-toString"></a>

#### toString

```java
@Override
public String toString()
```



---

<a id="m-NIdLocation-isBlank"></a>

#### isBlank

```java
@Override
public boolean isBlank()
```

true if blank

**Returns:**

argument value



---

<a id="cls-net-thevpc-nuts-artifact-NIdType"></a>

## NIdType

```java
public enum NIdType implements NEnum
```

Artifacts are organized according to `NutsIdType` to reflect how the artifact
should be managed by the workspace.
This information is available in [NDefinition](#cls-NDefinition)

### Enum Constants

| Constant | Description |
| :--- | :--- |
| `API` | the id denotes a nuts api id artifact. |
| `RUNTIME` | the id denotes a nuts runtime id artifact. |
| `EXTENSION` | the id denotes a nuts known extension artifact. |
| `COMPANION` | the id denotes a nuts companion id artifact. |
| `REGULAR` | the id is none of the previous types |

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private final String` | `id` | lower-cased identifier for the enum entry |

### Constructors

#### `NIdType()`

```java
NIdType()
```

N id type.


### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;NIdType&gt;` | [parse(String)](#m-NIdType-parse) | Parse. |
| `String` | [id()](#m-NIdType-id) | lower cased identifier. |

### Method Details


---

<a id="m-NIdType-parse"></a>

#### parse

```java
public static NOptional<NIdType> parse(String value)
```

Parse.

**Parameters:**

- `value` - value

**Returns:**

parse result



---

<a id="m-NIdType-id"></a>

#### id

```java
public String id()
```

lower cased identifier.

**Returns:**

lower cased identifier



---

<a id="cls-net-thevpc-nuts-artifact-NIdWriter"></a>

## NIdWriter

```java
public interface NIdWriter extends NObjectWriter, NComponent
```

Class responsible of manipulating  [NId](#cls-NId) instances:


- formatting (in Nuts Stream Format)
- parsing



**Author:** thevpc

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NIdWriter` | [of()](#m-NIdWriter-of) | Creates a new instance. |
| `boolean` | [isOmitRepository()](#m-NIdWriter-isOmitRepository) | return true when the repository should not be included in formatted instance |
| `NIdWriter` | [omitRepository(boolean)](#m-NIdWriter-omitRepository) | update omitRepository |
| `boolean` | [isOmitGroupId()](#m-NIdWriter-isOmitGroupId) | return true when the groupId should not be included in formatted instance |
| `NIdWriter` | [omitGroupId(boolean)](#m-NIdWriter-omitGroupId) | update omitGroup |
| `boolean` | [isOmitImportedGroupId()](#m-NIdWriter-isOmitImportedGroupId) | return true when the imported groupId should not be included in formatted instance |
| `NIdWriter` | [omitImportedGroupId(boolean)](#m-NIdWriter-omitImportedGroupId) | update omitImportedGroupId |
| `boolean` | [isOmitOtherProperties()](#m-NIdWriter-isOmitOtherProperties) | return true if omit other properties |
| `NIdWriter` | [omitOtherProperties(boolean)](#m-NIdWriter-omitOtherProperties) | update omitOtherProperties |
| `boolean` | [isOmitFace()](#m-NIdWriter-isOmitFace) | return true when the face should not be included in formatted instance |
| `NIdWriter` | [omitFace(boolean)](#m-NIdWriter-omitFace) | update omitFace |
| `boolean` | [isHighlightImportedGroupId()](#m-NIdWriter-isHighlightImportedGroupId) | return true when the imported group should be highlighted in formatted instance |
| `NIdWriter` | [highlightImportedGroupId(boolean)](#m-NIdWriter-highlightImportedGroupId) | update highlightImportedGroupId |
| `List&lt;String&gt;` | [omitProperties()](#m-NIdWriter-omitProperties) | query properties omitted |
| `boolean` | [isOmitProperty(String)](#m-NIdWriter-isOmitProperty) | return true if omit query property named name |
| `NIdWriter` | [setOmitProperty(String, boolean)](#m-NIdWriter-setOmitProperty) | if true omit (do not include) query property named name when formatting the value |
| `NIdWriter` | [configure(boolean, String)](#m-NIdWriter-configure) | configure the current command with the given arguments. |
| `NIdWriter` | [ntf(boolean)](#m-NIdWriter-ntf) | Ntf. |
| `boolean` | [isOmitCondition()](#m-NIdWriter-isOmitCondition) | Checks if this is omit. |
| `NIdWriter` | [omitCondition(boolean)](#m-NIdWriter-omitCondition) | Omit condition. |
| `boolean` | [isOmitExclusion()](#m-NIdWriter-isOmitExclusion) | Checks if this is omit. |
| `NIdWriter` | [omitExclusion(boolean)](#m-NIdWriter-omitExclusion) | Omit exclusion. |

### Method Details


---

<a id="m-NIdWriter-of"></a>

#### of

```java
static NIdWriter of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NIdWriter-isOmitRepository"></a>

#### isOmitRepository

```java
boolean isOmitRepository()
```

return true when the repository should not be included in formatted instance

**Returns:**

return true when the repository should not be included in formatted instance



---

<a id="m-NIdWriter-omitRepository"></a>

#### omitRepository

```java
@NSetter
NIdWriter omitRepository(boolean value)
```

**Mutator:** Property setter.

update omitRepository

**Parameters:**

- `value` - true when the repository should not be included in formatted instance

**Returns:**

`this` instance



---

<a id="m-NIdWriter-isOmitGroupId"></a>

#### isOmitGroupId

```java
boolean isOmitGroupId()
```

return true when the groupId should not be included in formatted instance

**Returns:**

return true when the groupId should not be included in formatted instance



---

<a id="m-NIdWriter-omitGroupId"></a>

#### omitGroupId

```java
@NSetter
NIdWriter omitGroupId(boolean value)
```

**Mutator:** Property setter.

update omitGroup

**Parameters:**

- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdWriter-isOmitImportedGroupId"></a>

#### isOmitImportedGroupId

```java
boolean isOmitImportedGroupId()
```

return true when the imported groupId should not be included in formatted instance

**Returns:**

return true when the imported groupId should not be included in formatted instance



---

<a id="m-NIdWriter-omitImportedGroupId"></a>

#### omitImportedGroupId

```java
@NSetter
NIdWriter omitImportedGroupId(boolean value)
```

**Mutator:** Property setter.

update omitImportedGroupId

**Parameters:**

- `value` - value

**Returns:**

`this` instance



---

<a id="m-NIdWriter-isOmitOtherProperties"></a>

#### isOmitOtherProperties

```java
boolean isOmitOtherProperties()
```

return true if omit other properties

**Returns:**

true if omit other properties



---

<a id="m-NIdWriter-omitOtherProperties"></a>

#### omitOtherProperties

```java
@NSetter
NIdWriter omitOtherProperties(boolean value)
```

**Mutator:** Property setter.

update omitOtherProperties

**Parameters:**

- `value` - value

**Returns:**

`this` instance



---

<a id="m-NIdWriter-isOmitFace"></a>

#### isOmitFace

```java
boolean isOmitFace()
```

return true when the face should not be included in formatted instance

**Returns:**

return true when the face should not be included in formatted instance



---

<a id="m-NIdWriter-omitFace"></a>

#### omitFace

```java
@NSetter
NIdWriter omitFace(boolean value)
```

**Mutator:** Property setter.

update omitFace

**Parameters:**

- `value` - value

**Returns:**

`this` instance



---

<a id="m-NIdWriter-isHighlightImportedGroupId"></a>

#### isHighlightImportedGroupId

```java
boolean isHighlightImportedGroupId()
```

return true when the imported group should be highlighted in formatted instance

**Returns:**

return true when the imported group should be highlighted in formatted instance



---

<a id="m-NIdWriter-highlightImportedGroupId"></a>

#### highlightImportedGroupId

```java
@NSetter
NIdWriter highlightImportedGroupId(boolean value)
```

**Mutator:** Property setter.

update highlightImportedGroupId

**Parameters:**

- `value` - value

**Returns:**

`this` instance



---

<a id="m-NIdWriter-omitProperties"></a>

#### omitProperties

```java
@NGetter
List<String> omitProperties()
```

**Accessor:** Property getter.

query properties omitted

**Returns:**

query properties omitted



---

<a id="m-NIdWriter-isOmitProperty"></a>

#### isOmitProperty

```java
boolean isOmitProperty(String name)
```

return true if omit query property named `name`

**Parameters:**

- `name` - property name

**Returns:**

true if omit query property named `name`



---

<a id="m-NIdWriter-setOmitProperty"></a>

#### setOmitProperty

```java
NIdWriter setOmitProperty(String name, boolean value)
```

if true omit (do not include) query property named `name` when formatting the value

**Parameters:**

- `name` - property name
- `value` - new value

**Returns:**

`this` instance



---

<a id="m-NIdWriter-configure"></a>

#### configure

```java
@Override
NIdWriter configure(boolean skipUnsupported, String args)
```

configure the current command with the given arguments. This is an
override of the [java.lang.String...)](#cls-NCmdLineConfigurable)
to help return a more specific return type;

**Parameters:**

- `skipUnsupported` - when true, all unsupported options are skipped
- `args` - argument to configure with

**Returns:**

`this` instance



---

<a id="m-NIdWriter-ntf"></a>

#### ntf

```java
NIdWriter ntf(boolean ntf)
```

Ntf.

**Parameters:**

- `ntf` - ntf

**Returns:**

ntf result



---

<a id="m-NIdWriter-isOmitCondition"></a>

#### isOmitCondition

```java
boolean isOmitCondition()
```

Checks if this is omit. condition.

**Returns:**

is omit condition result



---

<a id="m-NIdWriter-omitCondition"></a>

#### omitCondition

```java
NIdWriter omitCondition(boolean omitCondition)
```

Omit condition.

**Parameters:**

- `omitCondition` - omit condition

**Returns:**

omit condition result



---

<a id="m-NIdWriter-isOmitExclusion"></a>

#### isOmitExclusion

```java
boolean isOmitExclusion()
```

Checks if this is omit. exclusion.

**Returns:**

is omit exclusion result



---

<a id="m-NIdWriter-omitExclusion"></a>

#### omitExclusion

```java
NIdWriter omitExclusion(boolean omitExclusion)
```

Omit exclusion.

**Parameters:**

- `omitExclusion` - omit exclusion

**Returns:**

omit exclusion result



---

<a id="cls-net-thevpc-nuts-artifact-NTypedFilters"></a>

## NTypedFilters

```java
public interface NTypedFilters<T extends NFilter> extends NComponent
```

Filters helper

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `T` | [nonnull(NFilter)](#m-NTypedFilters-nonnull) | Nonnull. |
| `T` | [always()](#m-NTypedFilters-always) | Always. |
| `T` | [never()](#m-NTypedFilters-never) | Never. |
| `T` | [all(NFilter)](#m-NTypedFilters-all) | All. |
| `T` | [any(NFilter)](#m-NTypedFilters-any) | Any. |
| `T` | [not(NFilter)](#m-NTypedFilters-not) | Not. |
| `T` | [none(NFilter)](#m-NTypedFilters-none) | None. |
| `T` | [from(NFilter)](#m-NTypedFilters-from) | From. |
| `T` | [as(NFilter)](#m-NTypedFilters-as) | As. |
| `T` | [parse(String)](#m-NTypedFilters-parse) | Parse. |

### Method Details


---

<a id="m-NTypedFilters-nonnull"></a>

#### nonnull

```java
T nonnull(NFilter filter)
```

Nonnull.

**Parameters:**

- `filter` - filter

**Returns:**

nonnull result



---

<a id="m-NTypedFilters-always"></a>

#### always

```java
T always()
```

Always.

**Returns:**

always result



---

<a id="m-NTypedFilters-never"></a>

#### never

```java
T never()
```

Never.

**Returns:**

never result



---

<a id="m-NTypedFilters-all"></a>

#### all

```java
T all(NFilter others)
```

All.

**Parameters:**

- `others` - others

**Returns:**

all result



---

<a id="m-NTypedFilters-any"></a>

#### any

```java
T any(NFilter others)
```

Any.

**Parameters:**

- `others` - others

**Returns:**

any result



---

<a id="m-NTypedFilters-not"></a>

#### not

```java
T not(NFilter other)
```

Not.

**Parameters:**

- `other` - other

**Returns:**

not result



---

<a id="m-NTypedFilters-none"></a>

#### none

```java
T none(NFilter others)
```

None.

**Parameters:**

- `others` - others

**Returns:**

none result



---

<a id="m-NTypedFilters-from"></a>

#### from

```java
T from(NFilter a)
```

From.

**Parameters:**

- `a` - a

**Returns:**

from result



---

<a id="m-NTypedFilters-as"></a>

#### as

```java
T as(NFilter a)
```

As.

**Parameters:**

- `a` - a

**Returns:**

as result



---

<a id="m-NTypedFilters-parse"></a>

#### parse

```java
T parse(String expression)
```

Parse.

**Parameters:**

- `expression` - expression

**Returns:**

parse result



---

<a id="cls-net-thevpc-nuts-artifact-NVersion"></a>

## NVersion

```java
public interface NVersion extends Serializable, Comparable<NVersion>, NBlankable
```

this class represents an **immutable** string representation of a version parsed as a suite of alternating numbers and words.
Parsing algorithm is simply to split whenever word type changes.
Examples:


- 1 = [1]
- 1.2 = [1,'.',2]
- 10.20update3 = [10,'.',20,'update',3]



**Author:** thevpc

**Since:** 0.5.4

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `Pattern` | `PATTERN` |  |
| `NVersion` | `BLANK` |  |

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;NVersion&gt;` | [get(String)](#m-NVersion-get) | parses the version or create error |
| `static NVersion` | [of(String)](#m-NVersion-of) | Creates a new instance of. |
| `boolean` | [isNull()](#m-NVersion-isNull) | return true the version value is a null string |
| `String` | [value()](#m-NVersion-value) | return string representation of the version |
| `int` | [compareTo(String)](#m-NVersion-compareTo) | compare this version to the other version |
| `int` | [compareTo(NVersion)](#m-NVersion-compareTo) | compare this version to the other version |
| `int` | [compareTo(NVersion, NVersionComparator)](#m-NVersion-compareTo) | Compare to. |
| `int` | [compareTo(String, NVersionComparator)](#m-NVersion-compareTo) | Compare to. |
| `NVersion` | [toCanonical()](#m-NVersion-toCanonical) | remove qualifier from version important to check against version intervals |
| `NVersion` | [toNormalized()](#m-NVersion-toNormalized) | remove trailing zeros |
| `NVersionFilter` | [toFilter()](#m-NVersion-toFilter) | parse the current version as new instance of NVersionFilter |
| `NVersionFilter` | [toFilter(NVersionComparator)](#m-NVersion-toFilter) | Converts to filter. |
| `NVersion` | [toAtMost()](#m-NVersion-toAtMost) | when the current version is a single value version X , returns ],X] version that guarantees backward compatibility in all other cases returns the... |
| `NVersion` | [toAtLeast()](#m-NVersion-toAtLeast) | when the current version is a single value version X , returns [X,[ version that guarantees forward compatibility in all other cases returns the current version |
| `NOptional&lt;List&lt;NVersionInterval&gt;&gt;` | [toIntervals()](#m-NVersion-toIntervals) | parse the current version as an interval array |
| `NOptional&lt;List&lt;NVersionInterval&gt;&gt;` | [toIntervals(NVersionComparator)](#m-NVersion-toIntervals) | Converts to intervals. |
| `boolean` | [isSingleValue()](#m-NVersion-isSingleValue) | return true if this version denotes as single value and does not match an interval. |
| `NOptional&lt;String&gt;` | [singleValue()](#m-NVersion-singleValue) | As single value. |
| `boolean` | [isFilter()](#m-NVersion-isFilter) | return true if this is a filter |
| `NVersion` | [inc()](#m-NVersion-inc) | increment the last number in the version with 1 |
| `NVersion` | [inc(int)](#m-NVersion-inc) | increment the number at index in the version with 1 |
| `NVersion` | [inc(int, long)](#m-NVersion-inc) | increment the last number in the version with the given amount |
| `NVersion` | [inc(int, BigInteger)](#m-NVersion-inc) | increment the last number in the version with the given amount |
| `int` | [partCount()](#m-NVersion-partCount) | number of parts in the version. |
| `int` | [numberCount()](#m-NVersion-numberCount) | number of numeric parts in the version. |
| `NOptional&lt;NVersionPart&gt;` | [getPartAt(int)](#m-NVersion-getPartAt) | element at given index. |
| `NOptional&lt;Number&gt;` | [getNumberAt(int)](#m-NVersion-getNumberAt) | number element at given index. |
| `NOptional&lt;Integer&gt;` | [getIntAt(int)](#m-NVersion-getIntAt) | Returns the int at. |
| `NOptional&lt;Long&gt;` | [getLongAt(int)](#m-NVersion-getLongAt) | Returns the long at. |
| `NOptional&lt;BigInteger&gt;` | [getBigIntAt(int)](#m-NVersion-getBigIntAt) | Returns the big int at. |
| `boolean` | [isLatest()](#m-NVersion-isLatest) | Checks if this is latest. |
| `boolean` | [isRelease()](#m-NVersion-isRelease) | Checks if this is release. |
| `boolean` | [isSnapshot()](#m-NVersion-isSnapshot) | Checks if this is snapshot. |
| `List&lt;NVersionPart&gt;` | [parts()](#m-NVersion-parts) | Parts. |

### Method Details


---

<a id="m-NVersion-get"></a>

#### get

```java
static NOptional<NVersion> get(String version)
```

parses the version or create error

**Parameters:**

- `version` - string value

**Returns:**

parsed value



---

<a id="m-NVersion-of"></a>

#### of

```java
static NVersion of(String version)
```

Creates a new instance of.

**Parameters:**

- `version` - version

**Returns:**

of result



---

<a id="m-NVersion-isNull"></a>

#### isNull

```java
boolean isNull()
```

return true the version value is a null string

**Returns:**

true the version value is a null string



---

<a id="m-NVersion-value"></a>

#### value

```java
String value()
```

return string representation of the version

**Returns:**

string representation of the version (never null)



---

<a id="m-NVersion-compareTo"></a>

#### compareTo

```java
int compareTo(String other)
```

compare this version to the other version

**Parameters:**

- `other` - other version

**Returns:**

a negative integer, zero, or a positive integer as this object
is less than, equal to, or greater than the specified object.



---

<a id="m-NVersion-compareTo"></a>

#### compareTo

```java
@Override
int compareTo(NVersion other)
```

compare this version to the other version

**Parameters:**

- `other` - other version

**Returns:**

a negative integer, zero, or a positive integer as this object
is less than, equal to, or greater than the specified object.



---

<a id="m-NVersion-compareTo"></a>

#### compareTo

```java
int compareTo(NVersion other, NVersionComparator comparator)
```

Compare to.

**Parameters:**

- `other` - other
- `comparator` - comparator

**Returns:**

compare to result



---

<a id="m-NVersion-compareTo"></a>

#### compareTo

```java
int compareTo(String other, NVersionComparator comparator)
```

Compare to.

**Parameters:**

- `other` - other
- `comparator` - comparator

**Returns:**

compare to result



---

<a id="m-NVersion-toCanonical"></a>

#### toCanonical

```java
NVersion toCanonical()
```

remove qualifier from version
important to check against version intervals

**Returns:**

new version without qualifier



---

<a id="m-NVersion-toNormalized"></a>

#### toNormalized

```java
NVersion toNormalized()
```

remove trailing zeros

**Returns:**





---

<a id="m-NVersion-toFilter"></a>

#### toFilter

```java
NVersionFilter toFilter()
```

parse the current version as new instance of [NVersionFilter](#cls-NVersionFilter)

**Returns:**

new instance of [NVersionFilter](#cls-NVersionFilter)



---

<a id="m-NVersion-toFilter"></a>

#### toFilter

```java
NVersionFilter toFilter(NVersionComparator comparator)
```

Converts to filter.

**Parameters:**

- `comparator` - comparator

**Returns:**

to filter result



---

<a id="m-NVersion-toAtMost"></a>

#### toAtMost

```java
NVersion toAtMost()
```

when the current version is a single value version X , returns ],X] version that guarantees backward compatibility
in all other cases returns the current version

**Returns:**

when the current version is a single value version X , returns ],X] version that guarantees backward compatibility in all other cases returns the current version



---

<a id="m-NVersion-toAtLeast"></a>

#### toAtLeast

```java
NVersion toAtLeast()
```

when the current version is a single value version X , returns [X,[ version that guarantees forward compatibility
in all other cases returns the current version

**Returns:**

when the current version is a single value version X , returns [X,[ version that guarantees forward compatibility in all other cases returns the current version



---

<a id="m-NVersion-toIntervals"></a>

#### toIntervals

```java
NOptional<List<NVersionInterval>> toIntervals()
```

parse the current version as an interval array

**Returns:**

new interval array



---

<a id="m-NVersion-toIntervals"></a>

#### toIntervals

```java
NOptional<List<NVersionInterval>> toIntervals(NVersionComparator comparator)
```

Converts to intervals.

**Parameters:**

- `comparator` - comparator

**Returns:**

to intervals result



---

<a id="m-NVersion-isSingleValue"></a>

#### isSingleValue

```java
boolean isSingleValue()
```

return true if this version denotes as single value and does not match an interval.

**Returns:**

true if this version denotes as single value and does not match an interval.



---

<a id="m-NVersion-singleValue"></a>

#### singleValue

```java
NOptional<String> singleValue()
```

As single value.

**Returns:**

as single value result



---

<a id="m-NVersion-isFilter"></a>

#### isFilter

```java
boolean isFilter()
```

return true if this is a filter

**Returns:**

true if this is a filter



---

<a id="m-NVersion-inc"></a>

#### inc

```java
NVersion inc()
```

increment the last number in the version with 1

**Returns:**

new version incrementing the last number



---

<a id="m-NVersion-inc"></a>

#### inc

```java
NVersion inc(int index)
```

increment the number at `index`  in the version with 1

**Parameters:**

- `index` - number index

**Returns:**

new version incrementing the last number



---

<a id="m-NVersion-inc"></a>

#### inc

```java
NVersion inc(int index, long amount)
```

increment the last number in the version with the given `amount`

**Parameters:**

- `index` - number index
- `amount` - amount of the increment

**Returns:**

new version incrementing the last number



---

<a id="m-NVersion-inc"></a>

#### inc

```java
NVersion inc(int index, BigInteger amount)
```

increment the last number in the version with the given `amount`

**Parameters:**

- `index` - number index
- `amount` - amount of the increment

**Returns:**

new version incrementing the last number



---

<a id="m-NVersion-partCount"></a>

#### partCount

```java
int partCount()
```

number of parts in the version.


- size(1.22)=3 {'1','.','22'}
- size(1.22_u1)=5 {'1','.','22','_u','1'}

**Returns:**

number of elements in the version.



---

<a id="m-NVersion-numberCount"></a>

#### numberCount

```java
int numberCount()
```

number of numeric parts in the version.


- numberSize(1.22)=2 {1,22}
- numberSize(1.22_u1)=3 {1,22,1}

**Returns:**

number of numeric parts in the version.



---

<a id="m-NVersion-getPartAt"></a>

#### getPartAt

```java
NOptional<NVersionPart> getPartAt(int index)
```

element at given index. if the index is negative will return from right.


- (1.a22).get(0)=1
- (1.a22).get(1)=a
- (1.a22).get(-1)=22

**Parameters:**

- `index` - version part index

**Returns:**

element at given index.



---

<a id="m-NVersion-getNumberAt"></a>

#### getNumberAt

```java
NOptional<Number> getNumberAt(int index)
```

number element at given index. if the index is negative will return from right (-1 is the first starting from the right).
The version is first split (as a suite of numbers and words) then all words are discarded.


- size(1.22)=3 {'1','.','22'}
- size(1.22_u1)=5 {'1','.','22','_u','1'}




- (1.a22).getNumberLiteralAt(0)=1
- (1.a22).getNumberLiteralAt(1)=22
- (1.a22).getNumberLiteralAt(-1)=22

**Parameters:**

- `index` - version part index

**Returns:**

element at given index.



---

<a id="m-NVersion-getIntAt"></a>

#### getIntAt

```java
NOptional<Integer> getIntAt(int index)
```

Returns the int at.

**Parameters:**

- `index` - index

**Returns:**

get int at result



---

<a id="m-NVersion-getLongAt"></a>

#### getLongAt

```java
NOptional<Long> getLongAt(int index)
```

Returns the long at.

**Parameters:**

- `index` - index

**Returns:**

get long at result



---

<a id="m-NVersion-getBigIntAt"></a>

#### getBigIntAt

```java
NOptional<BigInteger> getBigIntAt(int level)
```

Returns the big int at.

**Parameters:**

- `level` - level

**Returns:**

get big int at result



---

<a id="m-NVersion-isLatest"></a>

#### isLatest

```java
boolean isLatest()
```

Checks if this is latest.

**Returns:**

is latest result



---

<a id="m-NVersion-isRelease"></a>

#### isRelease

```java
boolean isRelease()
```

Checks if this is release.

**Returns:**

is release result



---

<a id="m-NVersion-isSnapshot"></a>

#### isSnapshot

```java
boolean isSnapshot()
```

Checks if this is snapshot.

**Returns:**

is snapshot result



---

<a id="m-NVersion-parts"></a>

#### parts

```java
List<NVersionPart> parts()
```

Parts.

**Returns:**

parts result



---

<a id="cls-net-thevpc-nuts-artifact-NVersionComparator"></a>

## NVersionComparator

```java
public interface NVersionComparator extends Comparator<NVersion>
```

NVersionComparator interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NVersionComparator` | [of()](#m-NVersionComparator-of) | Creates a new instance. |
| `static NVersionComparator` | [ofMaven()](#m-NVersionComparator-ofMaven) | Creates a new instance of maven. |
| `static void` | [runWith(NVersionComparator, Runnable)](#m-NVersionComparator-runWith) | Run with. |
| `static V` | [callWith(NVersionComparator, NCallable&lt;V&gt;)](#m-NVersionComparator-callWith) | Call with. |

### Method Details


---

<a id="m-NVersionComparator-of"></a>

#### of

```java
static NVersionComparator of()
```

Creates a new instance.

**Returns:**

of result



---

<a id="m-NVersionComparator-ofMaven"></a>

#### ofMaven

```java
static NVersionComparator ofMaven()
```

Creates a new instance of maven.

**Returns:**

of maven result



---

<a id="m-NVersionComparator-runWith"></a>

#### runWith

```java
static void runWith(NVersionComparator value, Runnable r)
```

Run with.

**Parameters:**

- `value` - value
- `r` - r



---

<a id="m-NVersionComparator-callWith"></a>

#### callWith

```java
static <V> V callWith(NVersionComparator value, NCallable<V> r)
```

Call with.

**Parameters:**

- `value` - value
- `r` - r

**Returns:**

call with result



---

<a id="cls-net-thevpc-nuts-artifact-NVersionFilter"></a>

## NVersionFilter

```java
public interface NVersionFilter extends NFilter
```

version interval is a version filter that accepts interval ranges of
versions.



version intervals can be in one of the following forms
```java
[ version, ]
] version, ] or ( version, ]
[ version, [ or [ version, )
] version, [ or ] version, [

[ ,version ]
] ,version ] or ( ,version ]
[ ,version [ or [ ,version )
] ,version [ or ] ,version [

[ version1 , version2 ]
] version1 , version2 ] or ( version1 , version2 ]
[ version1 , version2 [ or [ version1 , version2 )
] version1 , version2 [ or ] version1 , version2 [

comma or space separated intervals such as :
  [ version1 , version2 ], [ version1 , version2 ]
  [ version1 , version2 ]  [ version1 , version2 ]
```



Created by vpc on 1/8/17.





**Author:** thevpc

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NVersionFilter` | [ofNonnull(NFilter)](#m-NVersionFilter-ofNonnull) | Creates a new instance of nonnull. |
| `static NVersionFilter` | [ofAlways()](#m-NVersionFilter-ofAlways) | Creates a new instance of always. |
| `static NVersionFilter` | [ofNever()](#m-NVersionFilter-ofNever) | Creates a new instance of never. |
| `static NVersionFilter` | [ofAll(NFilter)](#m-NVersionFilter-ofAll) | Creates a new instance of all. |
| `static NVersionFilter` | [ofAny(NFilter)](#m-NVersionFilter-ofAny) | Creates a new instance of any. |
| `static NVersionFilter` | [ofNot(NFilter)](#m-NVersionFilter-ofNot) | Creates a new instance of not. |
| `static NVersionFilter` | [ofNone(NFilter)](#m-NVersionFilter-ofNone) | Creates a new instance of none. |
| `static NVersionFilter` | [ofFrom(NFilter)](#m-NVersionFilter-ofFrom) | Creates a new instance of from. |
| `static NVersionFilter` | [ofAs(NFilter)](#m-NVersionFilter-ofAs) | Creates a new instance of as. |
| `static NVersionFilter` | [of(String)](#m-NVersionFilter-of) | Creates a new instance. |
| `static NOptional&lt;NVersionFilter&gt;` | [ofValue(String)](#m-NVersionFilter-ofValue) | Creates a new instance of value. |
| `static NOptional&lt;NVersionFilter&gt;` | [ofValue(String, NVersionComparator)](#m-NVersionFilter-ofValue) | Creates a new instance of value. |
| `static NVersionFilter` | [of(String, NVersionComparator)](#m-NVersionFilter-of) | Creates a new instance. |
| `boolean` | [acceptVersion(NVersion)](#m-NVersionFilter-acceptVersion) | true if the version is accepted by this instance filter |
| `NVersionFilter` | [or(NVersionFilter)](#m-NVersionFilter-or) | Or. |
| `NVersionFilter` | [and(NVersionFilter)](#m-NVersionFilter-and) | And. |
| `NVersionFilter` | [neg()](#m-NVersionFilter-neg) | Neg. |
| `NOptional&lt;List&lt;NVersionInterval&gt;&gt;` | [intervals()](#m-NVersionFilter-intervals) | Intervals. |
| `default NElement` | [describe()](#m-NVersionFilter-describe) |  |
| `NFilter` | [withDescription(Supplier&lt;NElement&gt;)](#m-NVersionFilter-withDescription) | With description. |

### Method Details


---

<a id="m-NVersionFilter-ofNonnull"></a>

#### ofNonnull

```java
static NVersionFilter ofNonnull(NFilter filter)
```

Creates a new instance of nonnull.

**Parameters:**

- `filter` - filter

**Returns:**

of nonnull result



---

<a id="m-NVersionFilter-ofAlways"></a>

#### ofAlways

```java
static NVersionFilter ofAlways()
```

Creates a new instance of always.

**Returns:**

of always result



---

<a id="m-NVersionFilter-ofNever"></a>

#### ofNever

```java
static NVersionFilter ofNever()
```

Creates a new instance of never.

**Returns:**

of never result



---

<a id="m-NVersionFilter-ofAll"></a>

#### ofAll

```java
static NVersionFilter ofAll(NFilter others)
```

Creates a new instance of all.

**Parameters:**

- `others` - others

**Returns:**

of all result



---

<a id="m-NVersionFilter-ofAny"></a>

#### ofAny

```java
static NVersionFilter ofAny(NFilter others)
```

Creates a new instance of any.

**Parameters:**

- `others` - others

**Returns:**

of any result



---

<a id="m-NVersionFilter-ofNot"></a>

#### ofNot

```java
static NVersionFilter ofNot(NFilter other)
```

Creates a new instance of not.

**Parameters:**

- `other` - other

**Returns:**

of not result



---

<a id="m-NVersionFilter-ofNone"></a>

#### ofNone

```java
static NVersionFilter ofNone(NFilter others)
```

Creates a new instance of none.

**Parameters:**

- `others` - others

**Returns:**

of none result



---

<a id="m-NVersionFilter-ofFrom"></a>

#### ofFrom

```java
static NVersionFilter ofFrom(NFilter a)
```

Creates a new instance of from.

**Parameters:**

- `a` - a

**Returns:**

of from result



---

<a id="m-NVersionFilter-ofAs"></a>

#### ofAs

```java
static NVersionFilter ofAs(NFilter a)
```

Creates a new instance of as.

**Parameters:**

- `a` - a

**Returns:**

of as result



---

<a id="m-NVersionFilter-of"></a>

#### of

```java
static NVersionFilter of(String expression)
```

Creates a new instance.

**Parameters:**

- `expression` - expression

**Returns:**

of result



---

<a id="m-NVersionFilter-ofValue"></a>

#### ofValue

```java
static NOptional<NVersionFilter> ofValue(String version)
```

Creates a new instance of value.

**Parameters:**

- `version` - version

**Returns:**

of value result



---

<a id="m-NVersionFilter-ofValue"></a>

#### ofValue

```java
static NOptional<NVersionFilter> ofValue(String version, NVersionComparator comparator)
```

Creates a new instance of value.

**Parameters:**

- `version` - version
- `comparator` - comparator

**Returns:**

of value result



---

<a id="m-NVersionFilter-of"></a>

#### of

```java
static NVersionFilter of(String expression, NVersionComparator versionComparator)
```

Creates a new instance.

**Parameters:**

- `expression` - expression
- `versionComparator` - version comparator

**Returns:**

of result



---

<a id="m-NVersionFilter-acceptVersion"></a>

#### acceptVersion

```java
boolean acceptVersion(NVersion version)
```

true if the version is accepted by this instance filter

**Parameters:**

- `version` - version to check

**Returns:**

true if the version is accepted by this instance interval



---

<a id="m-NVersionFilter-or"></a>

#### or

```java
NVersionFilter or(NVersionFilter other)
```

Or.

**Parameters:**

- `other` - other

**Returns:**

or result



---

<a id="m-NVersionFilter-and"></a>

#### and

```java
NVersionFilter and(NVersionFilter other)
```

And.

**Parameters:**

- `other` - other

**Returns:**

and result



---

<a id="m-NVersionFilter-neg"></a>

#### neg

```java
NVersionFilter neg()
```

Neg.

**Returns:**

neg result



---

<a id="m-NVersionFilter-intervals"></a>

#### intervals

```java
NOptional<List<NVersionInterval>> intervals()
```

Intervals.

**Returns:**

intervals result



---

<a id="m-NVersionFilter-describe"></a>

#### describe

```java
@Override
default NElement describe()
```



---

<a id="m-NVersionFilter-withDescription"></a>

#### withDescription

```java
NFilter withDescription(Supplier<NElement> description)
```

With description.

**Parameters:**

- `description` - description

**Returns:**

with description result



---

<a id="cls-net-thevpc-nuts-artifact-NVersionInterval"></a>

## NVersionInterval

```java
public interface NVersionInterval extends Serializable
```

**Author:** thevpc

**Since:** 0.5.4

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `static NOptional&lt;NVersionInterval&gt;` | [of(String, NVersionComparator)](#m-NVersionInterval-of) | Creates a new instance. |
| `static NOptional&lt;List&lt;NVersionInterval&gt;&gt;` | [ofList(String, NVersionComparator)](#m-NVersionInterval-ofList) | Creates a new instance of list. |
| `boolean` | [acceptVersion(NVersion)](#m-NVersionInterval-acceptVersion) | Accept version. |
| `boolean` | [isFixedValue()](#m-NVersionInterval-isFixedValue) | Checks if this is fixed. |
| `boolean` | [isIncludeLowerBound()](#m-NVersionInterval-isIncludeLowerBound) | Checks if this is include. |
| `boolean` | [isIncludeUpperBound()](#m-NVersionInterval-isIncludeUpperBound) | Checks if this is include. |
| `String` | [lowerBound()](#m-NVersionInterval-lowerBound) | Lower bound. |
| `String` | [upperBound()](#m-NVersionInterval-upperBound) | Upper bound. |

### Method Details


---

<a id="m-NVersionInterval-of"></a>

#### of

```java
static NOptional<NVersionInterval> of(String s, NVersionComparator versionComparator)
```

Creates a new instance.

**Parameters:**

- `s` - s
- `versionComparator` - version comparator

**Returns:**

of result



---

<a id="m-NVersionInterval-ofList"></a>

#### ofList

```java
static NOptional<List<NVersionInterval>> ofList(String s, NVersionComparator versionComparator)
```

Creates a new instance of list.

**Parameters:**

- `s` - s
- `versionComparator` - version comparator

**Returns:**

of list result



---

<a id="m-NVersionInterval-acceptVersion"></a>

#### acceptVersion

```java
boolean acceptVersion(NVersion version)
```

Accept version.

**Parameters:**

- `version` - version

**Returns:**

accept version result



---

<a id="m-NVersionInterval-isFixedValue"></a>

#### isFixedValue

```java
boolean isFixedValue()
```

Checks if this is fixed. value.

**Returns:**

is fixed value result



---

<a id="m-NVersionInterval-isIncludeLowerBound"></a>

#### isIncludeLowerBound

```java
boolean isIncludeLowerBound()
```

Checks if this is include. lower bound.

**Returns:**

is include lower bound result



---

<a id="m-NVersionInterval-isIncludeUpperBound"></a>

#### isIncludeUpperBound

```java
boolean isIncludeUpperBound()
```

Checks if this is include. upper bound.

**Returns:**

is include upper bound result



---

<a id="m-NVersionInterval-lowerBound"></a>

#### lowerBound

```java
@NGetter
String lowerBound()
```

**Accessor:** Property getter.

Lower bound.

**Returns:**

lower bound result



---

<a id="m-NVersionInterval-upperBound"></a>

#### upperBound

```java
@NGetter
String upperBound()
```

**Accessor:** Property getter.

Upper bound.

**Returns:**

upper bound result



---

<a id="cls-net-thevpc-nuts-artifact-NVersionPart"></a>

## NVersionPart

```java
public interface NVersionPart
```

NVersionPart interface.

**Author:** thevpc

**Since:** 0.8.0

### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `NVersionPartType` | [type()](#m-NVersionPart-type) | Type. |
| `String` | [value()](#m-NVersionPart-value) | Value. |

### Method Details


---

<a id="m-NVersionPart-type"></a>

#### type

```java
@NGetter
NVersionPartType type()
```

**Accessor:** Property getter.

Type.

**Returns:**

type result



---

<a id="m-NVersionPart-value"></a>

#### value

```java
@NGetter
String value()
```

**Accessor:** Property getter.

Value.

**Returns:**

value result



---

<a id="cls-net-thevpc-nuts-artifact-NVersionPartType"></a>

## NVersionPartType

```java
public enum NVersionPartType implements NEnum
```

NVersionPartType enum.

**Author:** thevpc

**Since:** 0.8.0

### Enum Constants

| Constant | Description |
| :--- | :--- |
| `NUMBER` |  |
| `QUALIFIER` |  |
| `SEPARATOR` |  |
| `PREFIX` |  |
| `SUFFIX` |  |

### Fields

| Modifier and Type | Field | Description |
| :--- | :--- | :--- |
| `private final String` | `id` |  |

### Constructors

#### `NVersionPartType()`

```java
NVersionPartType()
```

N version part type.


### Method Summary

| Modifier and Type | Method | Description |
| :--- | :--- | :--- |
| `String` | [id()](#m-NVersionPartType-id) | Id. |
| `static NOptional&lt;NVersionPartType&gt;` | [parse(String)](#m-NVersionPartType-parse) | Parse. |

### Method Details


---

<a id="m-NVersionPartType-id"></a>

#### id

```java
@Override
public String id()
```

Id.

**Returns:**

id result



---

<a id="m-NVersionPartType-parse"></a>

#### parse

```java
public static NOptional<NVersionPartType> parse(String value)
```

Parse.

**Parameters:**

- `value` - value

**Returns:**

parse result


