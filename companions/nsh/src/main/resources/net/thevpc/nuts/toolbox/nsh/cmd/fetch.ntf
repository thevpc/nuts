fetch a ```sh nuts```

##) SYNOPSIS:
==fetch== {{nuts-id}}  ...

fetches (without installing) a ```sh nuts``` package


##) EXAMPLES:

==fetch== ab*
   fetches all ```sh nuts``` that starts with 'ab'
