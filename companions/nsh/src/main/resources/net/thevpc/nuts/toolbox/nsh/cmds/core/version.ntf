show version

##) SYNOPSIS:
```sh version [ -l ] | [ --long ]```

-l --long   : long format

shows version

##) EXAMPLES:

```sh version```
   shows version

```sh version -l```
   shows version in long format

