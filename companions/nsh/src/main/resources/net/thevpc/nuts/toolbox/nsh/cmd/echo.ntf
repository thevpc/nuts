display a line of text

##) SYNOPSIS:
```sh echo [-n|-p] <messages>```

display a line of text

options :
```sh -n ```         : do not output the trailing newline
```sh -p ```         : plain text, do no use "format" escape characters

##) EXAMPLES:

```sh echo hello```
        display hello with bold format


```sh nuts -ZSbkKy -w=test -r==maven-local,maven-central nsh -c echo ##hello## world```
        echo with NFT format